const courseData = [{"chapterName":"01.Django入门与项目初始化","content":"# 目标\n\n- [ ] 理解 Web 框架的作用\n- [ ] 理解 Django 框架的作用\n- [ ] 理解 MTV 架构和交互\n- [ ] 掌握虚拟环境的作用和创建\n- [ ] 掌握 Django 项目的创建\n- [ ] 掌握 Django 项目中应用App的创建\n- [ ] 理解 setting.py 中的基本应用注册和基本配置\n- [ ] 理解ORM思想\n\n\n\n# Web 服务器与Web框架\n\n## Web 服务器\n\nWeb 服务器（HTTP 服务器）是**一个持续监听 IP + 端口的程序**，底层基于 TCP Socket，专门用来和浏览器（客户端）建立网络连接，收发**HTTP/HTTPS 报文**。\n\n> 回顾之前课程：我们自己用 `socket` + 多线程手写的简易 HTTP 程序，本质就是一台简陋的 Web 服务器。\n\n\n\n**浏览器与Web服务器交互**\n\n```mermaid\nsequenceDiagram\n participant Browser as 🌐 浏览器（客户端）\n participant WebServer as 🖥️ Web服务器(Nginx/Uvicorn)\n\n Note over Browser,WebServer:1.建立TCP三次握手连接\n Browser->>WebServer:TCP连接请求\n WebServer-->>Browser:TCP确认报文\n Browser->>WebServer:连接建立完成\n\n Note over Browser,WebServer:2.发送HTTP请求\n Browser->>WebServer:HTTP GET / HTTP/1.1<br/>Host:127.0.0.1:8080/index.html\n\n Note over Browser,WebServer:3.Web服务器处理请求\n alt 静态资源(html/css/图片)\n WebServer->>WebServer:读取本地文件，组装响应\n else 动态接口(登录/查询)\n WebServer->>WebServer:执行业务逻辑，生成响应数据\n end\n\n Note over Browser,WebServer:4.返回HTTP响应\n WebServer-->>Browser:HTTP/1.1 200 OK<br/>Content-Type:text/html<br/><br/>页面正文\n\n Note over Browser,WebServer:5.断开TCP连接（短连接）\n Browser->>WebServer:关闭连接请求\n WebServer-->>Browser:确认关闭\n```\n\n\n\n\n\n## Web 框架\n\n**Web框架** 是一套预先写好的代码库，封装了Web开发中重复性高的工作。没有框架时，你需要从头处理HTTP请求解析、URL路由、数据库连接、会话管理等所有底层细节。有了框架，你只需要关注业务逻辑本身。\n\n**生活化类比**:\n\n- `Web 服务器` = 前台接待员，负责接待访客、接收点菜（HTTP 请求）\n- `Web 框架` = 后台业务员工，看懂需求、处理业务、生成答复\n\n前台不会复杂业务；员工不能主动出去接待客人，二者配合工作。\n\n\n\n**浏览器&Web服务器&Web框架交互图**\n\n```mermaid\nsequenceDiagram\n participant Browser as 🌐 浏览器（客户端）\n participant WebServer as 🖥️ Web服务器(Nginx/Uvicorn)\n\n Note over Browser,WebServer:1.建立TCP三次握手连接\n Browser->>WebServer:TCP连接请求\n WebServer-->>Browser:TCP确认报文\n Browser->>WebServer:连接建立完成\n\n Note over Browser,WebServer:2.发送HTTP请求\n Browser->>WebServer:HTTP GET / HTTP/1.1<br/>Host:127.0.0.1:8080/index.html\n\n Note over Browser,WebServer:3.Web服务器处理请求\n alt 静态资源(html/css/图片)\n WebServer->>WebServer:读取本地文件\n else 动态接口(登录/查询)\n WebServer->>WebFramework:通过WSGI/ASGI转发请求\n WebFramework-->>WebServer:返回业务数据\n end\n\n Note over Browser,WebServer:4.返回HTTP响应\n WebServer-->>Browser:HTTP/1.1 200 OK<br/>Content-Type:text/html<br/><br/>页面正文\n\n Note over Browser,WebServer:5.断开TCP连接（短连接）\n Browser->>WebServer:关闭连接请求\n WebServer-->>Browser:确认关闭\n```\n\n**主流 Python Web 框架**\n\n+ **Django** 重型同步框架\n\n 内置 ORM、后台管理、权限系统；适合大型后台管理系统。\n\n+ **Flask** 轻量同步框架\n\n 核心精简，按需安装拓展；中小型同步项目。\n\n+ **FastAPI** 现代异步框架\n\n 基于 ASGI，支持`async def`协程，高性能、自带 API 文档；高并发接口、异步爬虫服务首选。\n\n\n\n# Django 简介\n\nDjango 是**Python 成熟、重型同步 Web 框架**，诞生于 2003 年，遵循 **Batteries included（内置电池理念）**。开箱大礼包，Web 开发需要的常用组件全部内置，不用大量寻找第三方库。\n\n**遵循架构**：`MVT`（MVC 的变种架构）\n\n**通信标准**：`WSGI`（同步标准），原生不支持 `async def` 异步视图（新版本有限支持，不推荐大量协程业务）\n\n\n\n## Django 内置核心组件\n\n1. **ORM 对象关系映射**\n\n 不用手写原生 SQL，使用 Python 类和对象操作数据库，支持 MySQL、SQLite、PostgreSQL。\n\n2. **Admin 后台管理系统（标志性功能）**\n\n 简单配置模型，自动生成完整管理后台，做管理系统效率极高。\n\n3. **路由系统 urls.py**\n\n URL 地址和视图函数匹配，支持正则 / 路径转换器。\n\n4. **视图 View**：接收请求，处理业务，返回响应\n\n5. **模板引擎 Template**：服务端渲染 HTML 页面\n\n6. **表单系统 Form**：请求数据校验、表单渲染、防 XSS\n\n7. **用户认证系统 Auth**\n\n 内置用户模型、登录、注销、权限、分组管理\n\n8. **Session、缓存、CSRF 防护、XSS 防御**\n\n\n\n## MTV 架构讲解\n\nMVT（Model - View - Template），是 Django 专属架构，属于 MVC 架构的变体。\n\n### 三大核心组件\n\n1. **Model（模型，M）**\n + **职责**：`映射数据库表，负责数据存取（ORM）`\n + 使用 Django ORM，无需手写 SQL\n + 定义字段、约束、表之间关联关系\n + 提供增删改查接口，和数据库交互\n + **只关心数据，不处理请求、不渲染页面**\n + `类比餐厅：厨房里的食材和菜谱`\n\n2. **View（视图，V）【核心调度中枢】**\n\n + **职责**：`接收 HTTP 请求，调用 Model 获取数据，传递给Template，生成响应`\n\n **完整流程**：\n\n 1. 接收 Web 服务器转发的 HTTP 请求\n\n 2. 调用 Model，完成数据查询 / 修改\n\n 3. 将数据传递给 Template\n\n 4. 接收模板渲染结果，封装为 HTTP 响应返回\n\n > 关键点：View 是**中间调度者**，连接数据层和展示层\n\n 5. `类比餐厅：厨师：知道要做什么菜，怎么做`\n\n3. **Template（模板，T）**\n + **职责**：`HTML 页面，负责展示数据，生成用户界面`\n + 存放 HTML 页面，支持模板变量、循环、分支\n + 接收 View 传递过来的数据，动态填充页面\n + 只负责展示，**不能直接操作数据库**\n + `类比餐厅：摆盘和上的菜`\n\n\n\n**请求完整流程**：\n\n![image-20260724145751642](./WebFullStack/image/20260825212623_15c1ea75.png)\n\n\n\n## Django 完整请求流程图\n\n```mermaid\nsequenceDiagram\n participant Browser as 🌐浏览器\n participant Gunicorn as 🖥️WSGI服务器\n participant Django as 🧩Django框架\n participant DB as 🗄数据库\n \n Browser->>Gunicorn:HTTP请求<br>http://127.0.0.1:8888/index.html\n Gunicorn->>Django:WSGI转发请求\n Django->>Django:urls.py 路由匹配视图\n Django->>DB:Model执行数据库查询\n DB-->>Django:返回数据\n Django->>Django:Template渲染HTML\n Django-->>Gunicorn:页面文本\n Gunicorn-->>Browser:HTTP响应页面\n```\n\n\n\n\n\n# 虚拟环境创建\n\n**虚拟环境（Virtual Environment）** 是Python项目的一个\"隔离空间\"。每个虚拟环境有自己独立的 Python解释器和第三方包目录，与系统全局的Python环境以及其他项目的环境互不干扰。\n\n**为什么必须用虚拟环境？**\n\n假设你同时在维护两个项目\n\n- 项目A：使用 Django 4.2\n- 项目B：使用 Django 5.2\n\n如果没有虚拟环境，你只能在全局安装一个版本，另一个项目就无法运行。有了虚拟环境，每个项目各自使用自己的 Django 版本，井水不犯河水。\n\n```mermaid\nflowchart TD\n subgraph 项目B虚拟 环境目录\n B[venv_B]\n B1[Python解释器 v3.10]\n B2[第三方包<br/>项目B依赖]\n end\n \n subgraph 项目A虚拟 环境目录\n C[venv_A]\n C1[Python解释器 v3.20]\n C2[第三方包<br/>项目A依赖]\n end\n \n subgraph 全局 环境目录\n A[全局Python环境]\n A1[Python解释器 v3.10]\n A2[第三方包<br/>requests、numpy...]\n end\n \n GEnv[电脑全局环境] --> A\n A --> A1\n A --> A2\n \n ProjectA[项目A代码] --> C\n C --> C1\n C --> C2\n\n ProjectB[项目B代码] --> B\n B --> B1\n B --> B2\n\n classDef globalEnv fill:#b3d9ff,stroke:#2266bb\n classDef venvBoxA fill:#ffdf99,stroke:#cc8800\n classDef venvBoxB fill:#c9e8b4,stroke:#448822\n classDef inner fill:#f8f2d0,stroke:#aa7700\n\n class A globalEnv\n class C venvBoxA\n class B venvBoxB\n class A1,A2,B1,B2,C1,C2 inner\n```\n\n\n\n**Windows系统创建和激活虚拟环境**\n\n```py\n# 创建虚拟环境（项目根目录下执行）\n# 语法：python -m venv <虚拟环境目录名>\n# 约定俗成：目录名通常叫 .venv 或 venv\npython -m venv .venv\n\n# 激活虚拟环境（PowerShell）\n# 【注意】激活后命令行前面会出现(.venv)前缀\n.venv\\Scripts\\Activate.ps1\n\n# 如果PowerShell禁止执行脚本，使用以下命令解除限制（以管理员身份运行PowerShell）：\nSet-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned\n\n# 激活后，验证当前使用的Python路径\nwhere python\n# 应该显示：...\\.venv\\Scripts\\python.exe\n\n# 退出虚拟环境\ndeactivate\n```\n\n\n\n# Django 项目创建\n\n**核心规范**：所有 Django 项目必须使用独立虚拟环境，避免包版本冲突！\n\n## 环境准备\n\n**1 创建项目总目录**\n\n```bat\n# 在命令行|终端执行项目目录的创建\nmkdir crm_project_root\n# 进入项目目录\ncd crm_project_root\n```\n\n**2 创建项目独有的虚拟环境**\n\n```bat\n# 创建虚拟环境 venv\npython -m venv .venv\n```\n\n**3 激活虚拟环境**\n\n```bat\n# Windows cmd\n.venv\\Scripts\\activate\n\n# Windows PowerShell\n.venv\\Scripts\\Activate.ps1\n\n# Mac / Linux\nsource .venv/bin/activate\n```\n\n**4 安装 Django**\n\n```bat\n# 更新 pip \npython.exe -m pip install --upgrade pip\n\n# 安装稳定版,需要考虑python 解析器的版本兼容问题\npip install Django==5.2.16\n\n# 查看版本\ndjango-admin --version\n```\n\n\n\n## 创建 Django 项目 & 启动服务\n\n**1 在当前目录下创建 Django 项目**\n\n```bat\n# 在当前目录下创建 Django 项目，最终 .venv 虚拟环境会包含在项目下\ndjango-admin startproject crm_project_root .\n```\n\n**注意**：命令末尾的 `.` 表示在当前目录创建项目文件，而不是再套一层额外的目录。如果省略 `.`，Django 会创建 `crm_project/crm_project/` 这样的双层目录结构。\n\n**执行完成后目录结构**\n\n```txt\ncrm_project_root/\n├── .venv\t\t\t\t\t\t# 项目独有虚拟环境 \n├── manage.py \t\t\t# 项目命令行工具（最重要）\n└── crm_project_root/ # 项目核心配置目录\n ├── __init__.py\n ├── asgi.py # 异步网关配置\n ├── settings.py # 全局配置文件\n ├── urls.py # 项目总路由\n └── wsgi.py # WSGI服务入口\n```\n\n\n\n**2 进入项目，启动开发服务器**\n\n```bat\n# 如果不再项目跟路径，需要先进入项目跟路径\n# cd crm_project_root\n# 启动内置WSGI调试服务器\npython manage.py runserver\n```\n\n**访问地址**：`http://127.0.0.1:8000` 看到欢迎页面代表项目创建成功。\n\n<img src=\"./WebFullStack/image/20260825212623_df9b8487.png\" alt=\"image-20260725141758440\" style=\"zoom:100%;\" align=\"left\"/>\n\n**3 其他启动方式**\n\n```bat\n# 指定端口启动\npython manage.py runserver 8080\n# 允许局域网其他设备访问\npython manage.py runserver 0.0.0.0:8000\n```\n\n开发服务器具有热重载功能，修改代码后自动重启。**切勿用于生产环境**。\n\n\n\n## Pycharm 打开项目\n\n<img src=\"./WebFullStack/image/20260825212623_af977c69.png\" alt=\"image-20260725151050627\" style=\"zoom:100%;\" align=\"left\"/>\n\n\n\n# Django应用和创建\n\n项目(project)≠ 应用(app)\n\n+ **项目(Project)**：是一个完整的Web服务，包含配置文件和多个功能模块的集合。一个项目就是一个网站。\n\n+ **应用(App)**：项目内功能模块（客户模块、用户模块、跟进记录模块），一个项目可以拥有多个 app\n\n\n\n## **创建应用**\n\n**创建统一管理所有应用的包**\n\n```bat\n# 创建统一管理应用的文件夹,新建空 __init__.py\nmkdir apps\nNew-Item apps/__init__.py\n```\n\n或者直接在pycharm上创建包\n\n<img src=\"./WebFullStack/image/20260825212623_4f3cd07f.png\" alt=\"image-20260725151207927\" style=\"zoom:80%;\" align=\"left\"/>\n\n\n\n**创建5个应用**\n\n```bat\n# 在 apps 内部创建 4 个应用\npython manage.py startapp users\t\t\t# 用户模块应用\npython manage.py startapp customers\t\t# 客户模块应用\npython manage.py startapp follow_ups\t# 跟进模块应用\npython manage.py startapp comments\t\t# 跟进评论模块\npython manage.py startapp statistic\t\t# 缓存模块应用\n```\n\n\n\n**将4个应用迁移到 apps 包里**\n\n<img src=\"./WebFullStack/image/20260825212623_ec59114c.png\" alt=\"image-20260725152545188\" style=\"zoom:100%;\" align=\"left\"/>\n\n## **应用目录**\n\n```txt\nusers/\n├── __init__.py\n├── admin.py # Admin后台配置\n├── apps.py # app配置\n├── migrations/ # 数据库迁移文件\n├── models.py # Model模型（数据表）\n├── tests.py # 测试文件\n├── urls.py # 子路由（需要手动新建）\n└── views.py # View视图函数\n```\n\n## 应用下 apps.py 修改\n\n```py\nclass XxxConfig(AppConfig):\n default_auto_field = 'django.db.models.BigAutoField'\n name = 'apps.xxxs'\n```\n\n\n\n## 重要操作：注册 app\n\n创建 app 后必须在 `settings.py` 的 `INSTALLED_APPS` 中注册，否则框架无法识别！\n\n\n\n**`BASE_DIR` 之后添加apps 到python模块搜索目录(不推荐)**\n\n由于所有的应用统一放到了 apps 目录，所以需要配置下 apps 目录,`配置之后在引用的位置可以不用带apps. 开头`\n\n```py\nimport os\nimport sys\n\nBASE_DIR = Path(__file__).resolve().parent.parent\n# 将apps加入python模块搜索路径\nsys.path.insert(0,os.path.join(BASE_DIR,\"apps\"))\n```\n\n\n\n**注册应用**\n\n```py\nINSTALLED_APPS = [\n 'django.contrib.admin',\n 'django.contrib.auth',\n 'django.contrib.contenttypes',\n 'django.contrib.sessions',\n 'django.contrib.messages',\n 'django.contrib.staticfiles',\n 'apps.users',\t\t# 新增注册应用\n 'apps.customers',\t# 新增注册应用\n 'apps.follow_ups',\t# 新增注册应用\n 'apps.comments',\t# 新增注册应用\n 'apps.statistics',\t# 新增注册应用\n]\n```\n\n\n\n**其他基础配置调整**\n\n```py\n# crm_project_root/settings.py\n\n# 语言与时区\nLANGUAGE_CODE = 'zh-hans'\nTIME_ZONE = 'Asia/Shanghai'\nUSE_I18N = True\nUSE_TZ = True\n```\n\n\n\n# settings.py 核心配置介绍\n\n`settings.py` 是整个项目的控制中心，核心配置项：\n\n| 配置项 | 作用 |\n| ---------------- | --------------------------------- |\n| `INSTALLED_APPS` | 注册已安装的应用 |\n| `MIDDLEWARE` | 中间件链，处理请求/响应的全局逻辑 |\n| `DATABASES` | 数据库连接配置 |\n| `TEMPLATES` | 模板引擎配置 |\n| `STATIC_URL` | 静态文件访问 URL 前缀 |\n| `LANGUAGE_CODE` | 语言设置 |\n| `TIME_ZONE` | 时区设置 |\n\n\n\n# AI 辅助编程实践\n\n使用 AI 辅助编程和学习\n\n## Prompt 示例\n\n```txt\n你是一位 Python 后端工程师。我正在创建一个基于 Django 5.2 的 CRM 项目\n现在需要你提供项目从零到创建基本应用的流程，只需要跟着流程就成启动项目看到欢迎页面\n每个步骤和命令汇总到一个表格上\n```\n\n\n\n**AI 生成的步骤**\n\n| 操作内容步骤 | 具体命令（按操作系统执行） | 验证/说明 |\n|:-------------------- |:----------------------------------------------------------- |:----------------------------------------------------------- |\n| **确认 Python 版本** | `python --version`（或 `python3 --version`） | 输出必须为 **Python 3.10 ~ 3.14** 中的任一版本 |\n| **创建项目根目录** | `mkdir crm_project` `cd crm_project` | 目录名可自定义（如 `my_crm`），此后所有操作均在此目录下进行 |\n| **创建虚拟环境** | `python -m venv venv`（若步骤1用 `python3`，则此处用 `python3`） | 生成 `venv` 文件夹，用于隔离项目依赖 |\n| **激活虚拟环境** | **Windows**：`venv\\Scripts\\activate` **macOS/Linux**：`source venv/bin/activate` | 激活后终端提示符前出现 `(venv)` 标识 |\n| **安装 Django 5.2** | `pip install Django==5.2` | 安装完成后执行 `python -m django --version`，应显示 `5.2` |\n| **生成 Django 项目** | `django-admin startproject crm_project .` | **末尾必须带 `.`**，否则会产生双层嵌套目录；执行后应出现 `manage.py` |\n| **检查项目结构** | **Windows**：`dir` **macOS/Linux**：`ls` | 当前目录下应有 `manage.py` 和 `crm_project/` 文件夹（内含 `settings.py`） |\n| **启动开发服务器** | `python manage.py runserver` | 终端显示 `Starting development server at http://127.0.0.1:8000/` （若端口被占用，可改用 `python manage.py runserver 8080`） |\n| **浏览器访问** | 打开浏览器，访问地址： **http://127.0.0.1:8000/** | 页面显示 Django 祝贺页（火箭或星球图标），即表示项目创建成功 🎉 |\n| **创建 CRM 基础应用** | `python manage.py startapp customers` | 执行后生成 `customers/` 目录，包含 `models.py`、`views.py` 等骨架文件；此步骤不影响欢迎页面的显示，可视需求后续执行 |\n\n\n\n# 课堂练习\n\n## 练习1：环境搭建与欢迎页（基础）\n\n**目标：** 参照课程步骤搭建一个测试项目，成功访问Django欢迎页。\n\n```bash\n# 步骤1：创建测试目录\ncd E:\\python\nmkdir django_test\ncd django_test\n\n# 步骤2：创建并激活虚拟环境\npython -m venv .venv\n.venv\\Scripts\\Activate.ps1\n\n# 步骤3：安装Django\npip install Django==5.2.16\n\n# 步骤4：创建测试项目\ndjango-admin startproject test_project .\n\n# 步骤5：启动服务器\npython manage.py runserver\n\n# 步骤6：打开浏览器访问 http://127.0.0.1:8000/\n```\n\n**预期输出：** 浏览器显示Django欢迎页面（带小火箭图标）\n\n**常见错误：**\n\n| 错误信息 | 原因 | 解决方法 |\n|---------|------|---------|\n| `No module named django` | 虚拟环境未激活 | 检查命令行是否有 `(.venv)` 前缀 |\n| `Address already in use` | 8000端口被占用 | `python manage.py runserver 8001` |\n| `'django-admin' 不是内部命令` | Django未安装 | 确认在虚拟环境中执行了 `pip install Django` |\n\n## 练习2：语言配置与Admin初见（中等）\n\n**目标：** 修改settings.py配置，创建管理员账号，访问Admin后台。\n\n```bash\n# 步骤1：修改settings.py中的语言配置\n# 将 LANGUAGE_CODE = 'en-us' 改为 LANGUAGE_CODE = 'zh-hans'\n# 将 TIME_ZONE = 'UTC' 改为 TIME_ZONE = 'Asia/Shanghai'\n```\n\n```python\n# settings.py中的修改\nLANGUAGE_CODE = 'zh-hans'\nTIME_ZONE = 'Asia/Shanghai'\n```\n\n```bash\n# 步骤2：创建超级管理员账号\npython manage.py createsuperuser\n# 交互过程：\n# 用户名(leave blank to use 'admin'):admin\n# 电子邮件地址:admin@example.com\n# Password:********\n# Superuser created successfully.\n\n# 步骤3：启动服务器并访问\npython manage.py runserver\n# 浏览器打开 http://127.0.0.1:8000/admin/\n```\n\n**预期输出：** Admin后台以中文界面显示，可以看到\"用户\"、\"组\"两个管理项。\n","order":1},{"chapterName":"02-模型层与MySQL集成","content":"# 模型层与 MySQL 集成\n\n## 学习目标\n\n1. **知识目标**：理解 ORM 的概念与优势，掌握 Django Model 的定义方法、字段类型与选项，理解一对一、一对多、多对多关系\n2. **技能目标**：能配置 Django 连接 MySQL，编写 Model 类，执行 `makemigrations` 与 `migrate` 生成数据库表\n3. **项目目标**：完成 CRM 系统的四个核心模型（User、Customer、FollowUp、Comment）并在 MySQL 中生成对应表\n4. **AI 实践目标**：用 Prompt 引导 AI 根据 Django Model 生成数据库设计文档，学习\"上下文 + 约束\"的 Prompt 结构\n\n## 知识点梳理\n\n### ORM 概念\n\nORM（Object-Relational Mapping，对象关系映射）将`数据库表`映射为 `Python 类`，将`表记录`映射为`对象`，将 `SQL 操作`映射为`方法调用`。\n\n+ **O = Object 对象（Python 面向对象类）**\n\n+ **R = Relational 关系（关系型数据库表）**\n\n+ **M = Mapping 映射（自动转换桥梁）**\n\n```mermaid\ngraph LR\n classDef green fill:#d4edda,stroke:#28a745;\n classDef blue fill:#cce5ff,stroke:#007bff;\n classDef orange fill:#ffe5cc,stroke:#e65c00;\n\n subgraph Python[\"🐍 Python/Django 世界\"]\n A[📦 模型类<br/>Customer 类] -->|① 元类构建| A1[(📋 数据表<br/>crm_customer)]\n B[🧑 模型实例<br/>customer 对象] -->|② 数据持久化| B1[(📄 表记录<br/>id:1,name:张三)]\n C[🏷️ 类属性<br/>name 字段] -->|③ 字段定义| C1[(📑 表字段<br/>name VARCHAR 50)]\n D[🔧 方法调用<br/>save 或 filter] -->|④ 编译执行| D1[📝 SQL 语句<br/>INSERT 或 SELECT]\n end\n```\n\n**Django ORM 和 SQL 关联**\n\n| 传统 SQL 方式 | Django ORM 方式 |\n|---------------|-----------------|\n| `SELECT * FROM customers WHERE status='won'` | `Customer.objects.filter(status='won')` |\n| `INSERT INTO customers ...` | `Customer.objects.create(...)` |\n| `UPDATE customers SET ... WHERE id=1` | `Customer.objects.filter(id=1).update(...)` |\n\n**ORM 的优势**：不依赖特定数据库方言、避免 SQL 注入、提高开发效率。代价是复杂的查询，性能可能不如手写 SQL。\n\n\n\n### Model 定义\n\nModel 是继承 `django.db.models.Model` 的 Python 类，类属性对应数据库字段：\n\n```python\nfrom django.db import models\n\nclass Customer(models.Model):\n name = models.CharField('客户姓名',max_length=100)\n created_at = models.DateTimeField('创建时间',auto_now_add=True)\n```\n\n\n\n#### 常用字段类型\n\n| 字段类 | 对应数据库类型 | 用途 |\n|--------|---------------|------|\n| `CharField` | VARCHAR | 短文本 |\n| `TextField` | LONGTEXT | 长文本 |\n| `IntegerField` / `BigIntegerField` | INT / BIGINT | 整数 |\n| `FloatField` | FLOAT | 浮点数 |\n| `BooleanField` | TINYINT(1)| 布尔值 |\n| `DateTimeField` | DATETIME | 日期时间 |\n| `DateField` | DATE | 日期 |\n| `EmailField` | VARCHAR(254)| 邮箱（带校验） |\n| `URLField` | VARCHAR(200)| URL（带校验） |\n| `ForeignKey` | INT + 外键约束 | 一对多关系 |\n| `AutoField` / `BigAutoField` | AUTO_INCREMENT | 自增主键 |\n\n#### 字段约束\n\n| 选项 | 作用 | 示例 |\n|------|------|------|\n| `max_length` | 最大长度 | `max_length=100` |\n| `null` | 数据库是否允许 NULL | `null=True` |\n| `blank` | 表单是否允许为空 | `blank=True` |\n| `default` | 默认值 | `default='lead'` |\n| `choices` | 枚举选项 | `choices=STATUS_CHOICES` |\n| `verbose_name` | 人类可读名称（标识） | `'客户姓名'` |\n| `unique` | 唯一约束 | `unique=True` |\n| `on_delete` | 外键删除策略 | `on_delete=models.CASCADE` |\n\n**外键删除策略对比**：\n\n| 策略 | 行为 |\n|------|------|\n| `CASCADE` | 删除被引用对象时，一并删除引用它的记录 |\n| `PROTECT` | 禁止删除被引用的对象（抛出 ProtectedError） |\n| `SET_NULL` | 设为 NULL（字段必须允许 null） |\n| `SET_DEFAULT` | 设为默认值 |\n| `DO_NOTHING` | 不处理（可能导致数据库错误） |\n\n\n\n### 关系字段\n\n**一对多（ForeignKey）**：最常用的关系。一个客户有多条跟进记录\n\n```python\nclass FollowUp(models.Model):\n customer = models.ForeignKey(Customer,on_delete=models.CASCADE,related_name='follow_ups')\n```\n\n`related_name` 定义反向查询的名称：`customer.follow_ups.all()` 获取该客户的所有跟进记录。\n\n\n\n**自关联**：评论的回复通过自引用 ForeignKey 实现：\n\n```python\nclass Comment(models.Model):\n parent = models.ForeignKey('self',on_delete=models.CASCADE,null=True,blank=True,related_name='replies')\n```\n\n\n\n### MySQL 连接配置\n\n```python\n# settings.py 配置\nDATABASES ={\n 'default':{\n 'ENGINE':'django.db.backends.mysql',\n 'NAME':'crm_project_db',\n 'USER':'root',\n 'PASSWORD':'admin',\n 'HOST':'127.0.0.1',\n 'PORT':'3306',\n}\n}\n```\n\n需要安装 MySQL 驱动：`pip install mysqlclient`（推荐，性能好）。\n\n如果安装困难，可使用 `pip install PyMySQL` 并在 `__init__.py` 中添加：\n\n```python\nimport pymysql\npymysql.install_as_MySQLdb()\n```\n\n\n\n### 迁移命令\n\n迁移命令的作用是将 Django 中配置好的模型类以及属性同步到数据库中\n\n```bash\n# 生成迁移文件（在 migrations/ 目录下创建 .py 文件）\npython manage.py makemigrations\n\n# 执行迁移（在数据库中创建/修改表）\npython manage.py migrate\n\n# 查看迁移状态\npython manage.py showmigrations\n```\n\n迁移是 Django 版本控制数据库结构的方式。每次修改 Model 后都需要 `makemigrations` + `migrate`。\n\n\n\n### MySQL 连接数据抽取\n\n抽取 setting.py 中的数据库连接数据到 .env 的原因：\n\n+ **安全隔离**：敏感信息与业务代码分离\n\n 数据库账号、密码、密钥放在独立`.env`，不写入代码文件。配合`.gitignore`，禁止把.env 上传代码仓库，防止隐私配置泄露。\n\n+ **多环境配置隔离**：同一套项目源码，可以搭配不同.env 文件：\n\n 本地开发环境、测试环境、正式生产环境使用不同数据库，**不用改动 settings.py 源码**，只更换.env 配置。\n\n+ **团队协作友好**\n\n 开发团队每个人本地数据库账号密码不一致。所有人共用同一套 settings.py，每个人维护自己本机的.env，不会产生代码冲突。\n\n+ **遵循企业开发规范（12 因素应用）**\n\n 业务逻辑代码不硬写环境参数；配置由环境变量提供，后期服务器部署、Docker 容器部署能够无缝兼容。\n\n\n\n**创建 `.env` 文件并填写数据**\n\n```\nDB_NAME=crm_project_db\nDB_USER=root\nDB_PASSWORD=admin\nDB_HOST=127.0.0.1\nDB_PORT=3306\n```\n\n\n\n **`settings.py` 中配置关联**\n\n**安装 env 文件解析包**\n\n```bat\npip install python-dotenv==1.2.2\n```\n\n\n\n**settings.py 配置**\n\n```py\nimport os\nfrom dotenv import load_dotenv\nfrom pathlib import Path\n\nBASE_DIR = Path(__file__).resolve().parent.parent\n# 加载项目下的 .env 文件中的数据\nload_dotenv(BASE_DIR / '.env')\n\n# 引用 .env 下的 DB_NAME，DB_USER，DB_PASSWORD，DB_HOST，DB_PORT\nDATABASES ={\n 'default':{\n 'ENGINE':'django.db.backends.mysql',\n 'NAME':os.environ.get('DB_NAME','crm_db'),\n 'USER':os.environ.get('DB_USER','root'),\n 'PASSWORD':os.environ.get('DB_PASSWORD','root'),\n 'HOST':os.environ.get('DB_HOST','127.0.0.1'),\n 'PORT':os.environ.get('DB_PORT','3306'),\n 'OPTIONS':{'charset':'utf8mb4'},\n}\n}\n```\n\n**`注意`**： settings.py 中引用 pathlib 可能会报错，实际上不是错，直接忽略错误即可\n\n\n\n## CRM 项目开发\n\n### 配置 MySQL 连接\n\n首先在 MySQL 中创建数据库：\n\n```sql\nCREATE DATABASE crm_project_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;\n```\n\n\n\n然后在 `settings.py` 中配置（实际项目使用 `.env` 管理密码）：\n\n```python\nimport os\nfrom dotenv import load_dotenv\nfrom pathlib import Path\n\nBASE_DIR = Path(__file__).resolve().parent.parent\nload_dotenv(BASE_DIR / '.env')\n\nDATABASES ={\n 'default':{\n 'ENGINE':'django.db.backends.mysql',\n 'NAME':os.environ.get('DB_NAME','crm_db'),\n 'USER':os.environ.get('DB_USER','root'),\n 'PASSWORD':os.environ.get('DB_PASSWORD','root'),\n 'HOST':os.environ.get('DB_HOST','127.0.0.1'),\n 'PORT':os.environ.get('DB_PORT','3306'),\n 'OPTIONS':{'charset':'utf8mb4'},\n}\n}\n```\n\n\n\n### CRM 模型分析\n\n#### **模型图**\n\n![image-20260731141347702](./WebFullStack/image/20260825212623_4705fd51.png)\n\n#### 模型字段\n\n+ **用户User模型**\n\n 手机号，头像，角色，部门名称，Django 内置 User 基本信息\n\n+ **客户Customer模型**\n\n 客户姓名、公司名称、联系电话、邮箱、地址、负责人、客户来源（网络，推荐，电话营销，展会）、客户状态（线索，已联系，有意向，已成交，已流失）、备注、创建时间、更新时间\n\n+ **跟进记录 FllowUp 模型**\n\n 客户、联系方式、跟进人、跟进结果、下次跟进日期、创建时间、更新时间\n\n+ **跟进评价 Comment 模型**\n\n 评论内容、评论人、关联客户、关联跟进记录、父评论、是否有效、创建时间、更新时间\n\n\n\n### 自定义 User 模型\n\n从 Django 内置的 `AbstractUser` 继承，增加业务字段：手机号，头像，角色，部门字段 \n\n```python\n# apps/users/models.py\nfrom django.contrib.auth.models import AbstractUser\nfrom django.db import models\n\n\nclass User(AbstractUser):\n \"\"\"自定义用户模型：增加手机号、头像、角色、部门字段。\"\"\"\n\n class Role(models.TextChoices):\n ADMIN = 'admin','管理员'\n MANAGER = 'manager','经理'\n SALES = 'sales','销售'\n\n phone = models.CharField('手机号',max_length=20,blank=True,null=True)\n avatar = models.URLField('头像',max_length=200,blank=True,null=True)\n role = models.CharField(\n '角色',max_length=20,choices=Role.choices,default=Role.SALES\n)\n department = models.CharField('部门',max_length=50,blank=True,null=True)\n\n class Meta:\n db_table = 'users_user'\n verbose_name = '用户'\n verbose_name_plural = verbose_name\n\n def __str__(self):\n return self.username\n```\n\n> 关键：`AUTH_USER_MODEL = 'users.User'` 必须在第一次 `migrate` 之前在 `settings.py` 中设置，否则需要复杂的迁移过程。\n\n### Customer 模型\n\n**客户信息**:客户姓名、公司名称、联系电话、邮箱、地址、负责人、客户来源（网络，推荐，电话营销，展会）、客户状态（线索，已联系，有意向，已成交，已流失）、备注、创建时间、更新时间\n\n```python\n# apps/customers/models.py\nfrom django.conf import settings\nfrom django.db import models\n\n\nclass Customer(models.Model):\n \"\"\"客户信息。\"\"\"\n\n class Source(models.TextChoices):\n ONLINE = 'online','网络'\n REFERRAL = 'referral','推荐'\n COLD_CALL = 'cold_call','电话营销'\n EXHIBITION = 'exhibition','展会'\n\n class Status(models.TextChoices):\n LEAD = 'lead','线索'\n CONTACTED = 'contacted','已联系'\n QUALIFIED = 'qualified','有意向'\n WON = 'won','已成交'\n LOST = 'lost','已流失'\n\n name = models.CharField('客户姓名',max_length=100)\n company = models.CharField('公司名称',max_length=200,blank=True,default='')\n phone = models.CharField('联系电话',max_length=20)\n email = models.EmailField('邮箱',blank=True,default='')\n address = models.CharField('地址',max_length=300,blank=True,default='')\n source = models.CharField(\n '客户来源',max_length=20,choices=Source.choices,default=Source.ONLINE\n)\n status = models.CharField(\n '客户状态',max_length=20,choices=Status.choices,default=Status.LEAD\n)\n owner = models.ForeignKey(\n settings.AUTH_USER_MODEL,\n on_delete=models.PROTECT,\n related_name='customers',\n verbose_name='负责人',\n)\n description = models.TextField('备注',blank=True,default='')\n created_at = models.DateTimeField('创建时间',auto_now_add=True)\n updated_at = models.DateTimeField('更新时间',auto_now=True)\n\t\n # 模型映射到数据库表的配置\n class Meta:\n db_table = 'customers_customer'\n verbose_name = '客户'\n verbose_name_plural = verbose_name\n ordering = ['-created_at']\n\n def __str__(self):\n return self.name\n```\n\n### FollowUp 模型\n\n**客户跟进记录**： 客户、联系方式、跟进人、跟进结果、下次跟进日期、创建时间、更新时间\n\n```python\n# apps/follow_ups/models.py\nfrom django.conf import settings\nfrom django.db import models\nfrom apps.customers.models import Customer\n\n\nclass FollowUp(models.Model):\n \"\"\"客户跟进记录。\"\"\"\n\n class ContactType(models.TextChoices):\n PHONE = 'phone','电话'\n EMAIL = 'email','邮件'\n VISIT = 'visit','拜访'\n MEETING = 'meeting','会议'\n\n class Result(models.TextChoices):\n PENDING = 'pending','待跟进'\n IN_PROGRESS = 'in_progress','进行中'\n COMPLETED = 'completed','已完成'\n\n customer = models.ForeignKey(\n Customer,on_delete=models.CASCADE,\n related_name='follow_ups',verbose_name='客户'\n)\n user = models.ForeignKey(\n settings.AUTH_USER_MODEL,on_delete=models.SET_NULL,\n null=True,related_name='follow_ups',verbose_name='跟进人'\n)\n contact_type = models.CharField(\n '联系方式',max_length=20,choices=ContactType.choices,default=ContactType.PHONE\n)\n content = models.TextField('跟进内容')\n result = models.CharField(\n '跟进结果',max_length=20,choices=Result.choices,default=Result.PENDING\n)\n next_follow_date = models.DateField('下次跟进日期',null=True,blank=True)\n created_at = models.DateTimeField('创建时间',auto_now_add=True)\n updated_at = models.DateTimeField('更新时间',auto_now=True)\n\n class Meta:\n db_table = 'follow_ups_followup'\n verbose_name = '跟进记录'\n verbose_name_plural = verbose_name\n ordering = ['-created_at']\n```\n\n### Comment 模型（含自关联）\n\n**跟进评价**：评论内容、评论人、关联客户、关联跟进记录、父评论、是否有效、创建时间、更新时间\n\n```python\n# apps/comments/models.py\nfrom django.conf import settings\nfrom django.db import models\nfrom apps.customers.models import Customer\nfrom apps.follow_ups.models import FollowUp\n\n\nclass Comment(models.Model):\n \"\"\"评论：可对客户或跟进记录发表，通过 parent 自关联实现层级回复。\"\"\"\n content = models.TextField('评论内容')\n user = models.ForeignKey(\n settings.AUTH_USER_MODEL,on_delete=models.CASCADE,\n related_name='comments',verbose_name='评论人'\n)\n customer = models.ForeignKey(\n Customer,on_delete=models.CASCADE,null=True,blank=True,\n related_name='comments',verbose_name='关联客户'\n)\n follow_up = models.ForeignKey(\n FollowUp,on_delete=models.CASCADE,null=True,blank=True,\n related_name='comments',verbose_name='关联跟进记录'\n)\n parent = models.ForeignKey(\n 'self',on_delete=models.CASCADE,null=True,blank=True,\n related_name='replies',verbose_name='父评论'\n)\n is_active = models.BooleanField('是否有效',default=True)\n created_at = models.DateTimeField('创建时间',auto_now_add=True)\n updated_at = models.DateTimeField('更新时间',auto_now=True)\n\n class Meta:\n db_table = 'comments_comment'\n verbose_name = '评论'\n verbose_name_plural = verbose_name\n ordering = ['-created_at']\n```\n\n### 执行迁移\n\n```bash\n# 生成迁移文件\npython manage.py makemigrations\n\n# 执行迁移，在 MySQL 中创建表\npython manage.py migrate\n```\n\n迁移成功可以在数据库中看到对应的表\n\n\n\n### shell插入数据\n\n**打开终端**\n\n```shell\npython manage.py shell\n```\n\n**创建普通用户（create_user，密码自动加密，推荐）**\n\n```bat\nfrom apps.users.models import User\n\nuser = User.objects.create_user(\n username=\"xiaodong\",\n password=\"dong@123456\",\n first_name=\"xiaodon\",\n phone=\"13900008888\",\n role=User.Role.SALES,\n department=\"销售部\"\n)\nprint(user.id,user.username,user.role)\n```\n\n**退出shell**\n\n```bat\nexit()\n```\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n完成四个核心模型的定义并生成数据库表。\n\n### 实现步骤\n1. 在 MySQL 中创建 `crm_project_db` 数据库\n2. 配置 `settings.py` 的 `DATABASES` 连接 MySQL\n3. 设置 `AUTH_USER_MODEL = 'users.User'`\n4. 编写 User 模型（自定义 AbstractUser）\n5. 编写 Customer 模型（含 ForeignKey 到 User）\n6. 编写 FollowUp 模型（含 ForeignKey 到 Customer 和 User）\n7. 编写 Comment 模型（含 ForeignKey 到 Customer、FollowUp 和自关联）\n8. 执行 `makemigrations` 和 `migrate`\n9. 查看表是否生成\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n用 Prompt 引导 AI 根据 Django Model 生成数据库表结构设计文档。\n\n### Prompt 示例\n\n**模型生成**：\n\n```\n你是一个Django 专家。以下是我的 Django Model 描述，请帮我生成所有模板的设计代码\n+ 用户User模型\n 手机号，头像，角色，部门名称，Django 内置 User 基本信息\n+ 客户Customer模型\n 客户姓名、公司名称、联系电话、邮箱、地址、负责人、客户来源（网络，推荐，电话营销，展会）、客户状态（线索，已联系，有意向，已成交，已流失）、备注、创建时间、更新时间\n+ 跟进记录 FllowUp 模型\n 客户、联系方式、跟进人、跟进结果、下次跟进日期、创建时间、更新时间\n+ 跟进评价 Comment 模型\n 评论内容、评论人、关联客户、关联跟进记录、父评论、是否有效、创建时间、更新时间\n```\n\n**数据库表设计：**\n\n```\n你是一位数据库设计专家。以下是我的 Django Model 定义，请帮我生成一份\n数据库表结构设计文档，包含：表名、字段名、类型、约束、索引建议、ER 关系说明。\n\n[粘贴 User、Customer、FollowUp、Comment 四个 Model 的代码]\n请以 Markdown 表格格式输出每张表的设计，并附上 ER 关系图（文字描述）。\n```\n\n### 实践要点\n- Prompt 中包含\"角色 + 任务 + 上下文（代码）+ 输出格式\"四个要素\n- AI 生成的文档需与实际 migrate 结果对照验证\n- 可以追问\"这个设计有没有潜在的性能问题？\"让 AI 做深度分析\n\n## 课堂练习\n\n**练习 1**（基础）：在 Django Shell 中创建一个 sales 角色的用户\n\n## 课后作业\n\n### 必做\n1. 完成四个模型的编写并成功迁移，确保 MySQL 中能看到四张表\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[模型层与MySQL] --> B[ORM 概念]\n A --> C[Model 定义]\n A --> D[字段类型]\n A --> E[关系字段]\n A --> F[迁移]\n C --> C1[类继承 Model]\n C --> C2[Meta 内部类]\n D --> D1[CharField/TextField]\n D --> D2[DateTimeField]\n D --> D3[choices 枚举]\n E --> E1[ForeignKey 一对多]\n E --> E2[自关联 回复]\n E --> E3[related_name]\n F --> F1[makemigrations]\n F --> F2[migrate]\n F --> F3[MySQL 配置]\n```\n\n## 下节预告\n\n- 下一节主题：Django Admin 后台管理\n- 需要预习的内容：\n - 阅读 Django 文档中关于 Admin 的部分\n - 思考：如果要管理客户列表，你希望在列表页看到哪些信息？需要哪些筛选条件？\n","order":2},{"chapterName":"03-Django-Admin","content":"# Day 3 - Django Admin 后台管理\n\n## 学习目标\n\n1. **知识目标**：理解 Django Admin 的作用与定位，掌握 ModelAdmin 的配置方法，理解 Inline 关联编辑机制\n2. **技能目标**：能将模型注册到 Admin，配置列表显示/筛选/搜索字段，使用 TabularInline 实现关联编辑，自定义 Admin Action\n3. **项目目标**：完成 CRM 系统的 Admin 后台，实现客户/跟进/评论的可视化管理\n4. **AI 实践目标**：用 Few-shot Prompt 方式让 AI 快速生成多个模型的 Admin 注册代码\n\n## 知识点梳理\n\n### Django Admin 简介\n\nDjango Admin 是框架内置的后台管理系统，只需少量代码即可获得完整的 CRUD 界面。它的定位是**内容管理工具**，适合运营人员管理数据，不是面向终端用户的前台。\n\n**启用 Admin 只需三步**：\n\n1. 在 `settings.py` 的 `INSTALLED_APPS` 中确认 `django.contrib.admin` 已注册\n2. 在 `urls.py` 中确认 `path('admin/',admin.site.urls)` 已配置\n3. 执行 `python manage.py createsuperuser` 创建管理员账号\n\n\n\n### ModelAdmin 注册\n\n将模型注册到 Admin 有两种方式：\n\n**方式一：`@admin.register` 装饰器（推荐）**\n\n```python\nfrom django.contrib import admin\nfrom .models import Customer\n\n@admin.register(Customer)\nclass CustomerAdmin(admin.ModelAdmin):\n list_display =('name','phone','status','owner')\n```\n\n**方式二：`admin.site.register`**\n\n```python\nadmin.site.register(Customer,CustomerAdmin)\n```\n\n\n\n### 常用 ModelAdmin 属性\n\n| 属性 | 作用 | 示例 |\n|------|------|------|\n| `list_display` | 列表页显示的字段 | `('name','phone','status')` |\n| `list_filter` | 右侧筛选器 | `('status','source')` |\n| `search_fields` | 搜索框搜索的字段 | `('name','company','phone')` |\n| `list_editable` | 列表页可直接编辑的字段 | `('status',)` |\n| `list_per_page` | 每页显示条数 | `20` |\n| `ordering` | 默认排序 | `('-created_at',)` |\n| `readonly_fields` | 详情页只读字段 | `('created_at',)` |\n| `date_hierarchy` | 按日期层级筛选 | `'created_at'` |\n\n\n\n### Inline 关联编辑\n\n在父模型的编辑页面中直接编辑子模型，避免来回切换页面。常用两种 Inline：\n\n| Inline 类型 | 布局 | 适用场景 |\n|-------------|------|----------|\n| `TabularInline` | 表格行布局 | 紧凑展示，字段较少 |\n| `StackedInline` | 堆叠块布局 | 字段较多，需要更多空间 |\n\n```python\nclass FollowUpInline(admin.TabularInline):\n model = FollowUp\t# 关联要操作的模型\n extra = 1 \t\t\t# 默认显示1个空行供新增\n\n@admin.register(Customer)\nclass CustomerAdmin(admin.ModelAdmin):\n list_display =('name','phone','status','owner')\n inlines = [FollowUpInline] \t# 在客户编辑页内联编辑跟进记录\n```\n\n\n\n### 自定义 Action\n\nAdmin 列表页可以自定义批量操作：\n\n```python\n@admin.register(Customer)\nclass CustomerAdmin(admin.ModelAdmin):\n actions = ['mark_as_won']\n\n @admin.action(description='标记为已成交')\n def mark_as_won(self,request,queryset):\n queryset.update(status='won')\n```\n\n选中记录后从 Action 下拉菜单执行，`queryset` 是选中的记录集。\n\n\n\n## 核心代码示例\n\n### 创建超级用户\n\n```bash\npython manage.py createsuperuser\n# 按提示输入用户名、邮箱、密码\n```\n\n### User Admin 注册\n\n```python\n# apps/users/admin.py\nfrom django.contrib import admin\nfrom django.contrib.auth.admin import UserAdmin\nfrom .models import User\n\n\n@admin.register(User)\nclass CustomUserAdmin(UserAdmin):\n \"\"\"用户后台管理。\"\"\"\n\n list_display =('username','phone','role','department','is_staff','is_active')\n list_filter =('role','is_staff','is_active')\n search_fields =('username','phone','department')\n # 在默认字段组之外增加扩展信息分组\n fieldsets = UserAdmin.fieldsets +(\n('扩展信息',{'fields':('phone','avatar','role','department')}),\n)\n add_fieldsets = UserAdmin.add_fieldsets +(\n('扩展信息',{'fields':('phone','avatar','role','department')}),\n)\n```\n\n> 说明：`UserAdmin` 是 Django 内置的用户管理 Admin 类，继承它可以保留密码管理等默认功能，同时通过 `fieldsets` 扩展自定义字段。\n> **固定语法**：\n> 编辑：` fieldsets = UserAdmin.fieldsets +(('标题信息',{'fields':('修改字段名1','修改字段名2')}),)`\n>\n> 添加：`add_fieldsets = UserAdmin.add_fieldsets +(('标题信息',{'fields':('添加字段名1','添加字段名2')}),)`\n\n### Customer Admin（含 Inline）\n\n```python\n# apps/customers/admin.py\nfrom django.contrib import admin\nfrom .models import Customer\nfrom apps.follow_ups.models import FollowUp\n\n\nclass FollowUpInline(admin.TabularInline):\n \"\"\"在客户编辑页内联编辑跟进记录。\"\"\"\n model = FollowUp\n extra = 1 # 默认显示1个空行\n\n\n@admin.register(Customer)\nclass CustomerAdmin(admin.ModelAdmin):\n list_display =('name','company','phone','source','status','owner','created_at')\n list_filter =('source','status','owner')\n search_fields =('name','company','phone')\n list_editable =('status',)# 列表页可直接修改状态\n inlines = [FollowUpInline]\n date_hierarchy = 'created_at'\n list_per_page = 20\n\n # 自定义批量操作\n actions = ['mark_as_won','mark_as_lost']\n\n @admin.action(description='标记为已成交')\n def mark_as_won(self,request,queryset):\n updated = queryset.update(status='won')\n self.message_user(request,f'成功标记{updated}个客户为已成交')\n\n @admin.action(description='标记为已流失')\n def mark_as_lost(self,request,queryset):\n updated = queryset.update(status='lost')\n self.message_user(request,f'成功标记{updated}个客户为已流失')\n```\n\n`注意`：如果程序访问出现时区问题，需要给 `mysql 库`导入时区表 `timezone_posix.sql`\n\n**解决步骤**： 下载到时区sql文件 --> 打开mysql库，拖入sql文件 --> 执行\n\n**时区表下载**：\n\n1. **公众号**：【`知未AI`】 --> 聊天位置发送 --> mysql时区表 --> \n\n2. **官网**：https://dev.mysql.com/downloads/timezones.html 官网在国外，打开和下载都比较慢，需要等\n\n\n\n### FollowUp Admin\n\n```python\n# apps/follow_ups/admin.py\nfrom django.contrib import admin\nfrom .models import FollowUp\n\n\n@admin.register(FollowUp)\nclass FollowUpAdmin(admin.ModelAdmin):\n list_display =('customer','user','contact_type','result','next_follow_date','created_at')\n list_filter =('contact_type','result')\n search_fields =('content',)\n date_hierarchy = 'created_at'\n```\n\n### Comment Admin\n\n```python\n# apps/comments/admin.py\nfrom django.contrib import admin\nfrom .models import Comment\n\n\n@admin.register(Comment)\nclass CommentAdmin(admin.ModelAdmin):\n list_display =('user','content','customer','follow_up','parent','is_active','created_at')\n list_filter =('is_active',)\n search_fields =('content',)\n list_editable =('is_active',)\n```\n\n### Admin 站点配置\n\n```python\n# crm_project_root/urls.py（已默认配置）\nfrom django.contrib import admin\nfrom django.urls import path\n\nurlpatterns = [\n path('admin/',admin.site.urls),\n]\n```\n\n自定义 Admin 标题和头部：\n\n```python\n# 任意 admin.py 中添加\nadmin.site.site_header = 'CRM 管理系统'\nadmin.site.site_title = 'CRM 后台'\nadmin.site.index_title = '数据管理'\n```\n\n\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n完成 CRM 系统全部模型的 Admin 注册，实现可视化数据管理。\n\n### 实现步骤\n1. 创建超级用户账号\n2. 注册 User 模型到 Admin，配置角色/部门筛选\n3. 注册 Customer 模型，配置列表显示/筛选/搜索/可编辑状态\n4. 创建 FollowUpInline，在客户编辑页内联编辑跟进记录\n5. 注册 FollowUp 和 Comment 模型\n6. 添加\"标记为已成交\"和\"标记为已流失\"自定义 Action\n7. 自定义 Admin 站点标题\n8. 启动服务器，在浏览器中访问 `http://127.0.0.1:8000/admin/` 测试\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n用 Few-shot(少样本)Prompt 方式让 AI 批量生成 Admin 注册代码。\n\n### 场景\n四个模型都需要注册到 Admin，配置项相似。可以通过提供一个示例让 AI 学习模式后批量生成。\n\n### Prompt 示例\n\n```\n你是一位 Django 开发工程师。请根据以下 Model 定义，为每个模型生成\n对应的 admin.py 注册代码。\n\n参考以下示例风格：\n\n[粘贴 Customer 模型代码 + 对应的 CustomerAdmin 代码作为示例]\n\n现在请为以下模型生成相同风格的 Admin 注册代码：\n[粘贴 User、FollowUp、Comment 模型代码]\n\n要求：\n1. 使用 @admin.register 装饰器方式\n2. list_display 包含所有关键字段\n3. list_filter 选择适合筛选的字段\n4. search_fields 选择适合搜索的文本字段\n5. 中文注释\n```\n\n### 实践要点\n- Few-shot 方式的核心是\"提供一个完整示例，让 AI 模仿\"\n- 提供的示例质量直接决定 AI 输出的质量\n- 生成后需逐个检查 `list_display` 中的字段名是否与模型匹配\n\n## 课堂练习\n\n**练习 1**（基础）：为 Comment Admin 添加一个自定义 Action \"隐藏评论\"，将选中评论的 `is_active` 设为 `False`。\n\n**练习 2**（中等）：在 FollowUp Admin 中添加 `next_follow_date` 的筛选器，使运营人员可以快速查看\"今天需要跟进\"的记录。\n\n思路提示：可以使用 `list_filter` 配合自定义 Filter 类实现日期范围筛选。\n\n## 课后作业\n\n### 必做\n1. 完成四个模型的 Admin 注册并测试所有功能\n2. 为 Customer Admin 添加至少两个自定义 Action\n3. 在 Admin 后台中创建 10 个客户、20 条跟进记录和若干评论\n\n### 选做（进阶）\n1. 尝试使用 `StackedInline` 替代 `TabularInline`，观察差异\n2. 用 AI 生成 Admin 搜索的高级配置（如自定义搜索方法）\n\n### 提交方式\n附上 Admin 后台各页面截图（列表页、编辑页、内联编辑页、Action 执行结果）。\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[Django Admin] --> B[注册模型]\n A --> C[ModelAdmin 配置]\n A --> D[Inline]\n A --> E[自定义 Action]\n B --> B1[admin.register]\n B --> B2[createsuperuser]\n C --> C1[list_display]\n C --> C2[list_filter]\n C --> C3[search_fields]\n C --> C4[list_editable]\n D --> D1[TabularInline]\n D --> D2[StackedInline]\n E --> E1[actions]\n E --> E2[message_user]\n```\n\n## 下节预告\n\n- 下一节主题：DRF 基础与 API 视图\n- 需要预习的内容：\n - 复习 HTTP 方法（GET/POST/PUT/DELETE）与状态码\n - 了解 JSON 数据格式\n - 思考：什么是 RESTful API？它的设计原则是什么？\n","order":3},{"chapterName":"04-DRF基础与API视图","content":"# Day 4 - DRF 基础与 API 视图\n\n## 学习目标\n\n1. **知识目标**：理解 RESTful API 设计原则（HTTP 方法语义、状态码规范、URL 命名约定），掌握 DRF 的核心组件及其协作关系\n2. **技能目标**：能独立编写 ModelSerializer 序列化器，使用 APIView 实现 GET/POST/PUT/DELETE 接口，使用 Postman 完成 API 测试\n3. **项目目标**：为 Customer 模型创建 Serializer 和 APIView，实现 GET（列表/详情）/POST/PUT/DELETE 五个端点，并统一响应格式\n4. **AI 实践目标**：用 AI 生成 Serializer 和基础 APIView 代码，学习如何通过 Prompt 准确描述 API 需求\n\n## 知识点梳理\n\n### RESTful API 设计原则\n\n`REST`（Representational State Transfer）是一种 `Web API `设计风格，核心思想是：用 URL 表示资源，用 HTTP 方法表示操作。\n\n**HTTP 方法语义对照表：**\n\n| HTTP 方法 | 语义 | 安全 | 幂等 | 典型 URL | 示例 |\n|-----------|------|------|------|----------|------|\n| GET | 获取资源 | 是 | 是 | `GET /api/customers/` | 获取客户列表 |\n| POST | 创建资源 | 否 | 否 | `POST /api/customers/` | 新建客户 |\n| PUT | 全量更新 | 否 | 是 | `PUT /api/customers/1/` | 更新客户（全字段） |\n| PATCH | 部分更新 | 否 | 是 | `PATCH /api/customers/1/` | 更新客户（部分字段） |\n| DELETE | 删除资源 | 否 | 是 | `DELETE /api/customers/1/` | 删除客户 |\n\n**优势**：\n\n+ 安全：请求**不会修改服务器资源数据**；\n\n+ 幂等：多次发起相同请求，服务器最终效果和执行 1 次完全一致。\n\n\n\n**常用状态码：**\n\n| 状态码 | 含义 | 使用场景 |\n|--------|------|----------|\n| 200 OK | 请求成功 | GET/PUT/DELETE 成功 |\n| 201 Created | 创建成功 | POST 成功 |\n| 400 Bad Request | 请求参数错误 | 字段校验失败 |\n| 401 Unauthorized | 未认证 | 缺少或无效 Token |\n| 403 Forbidden | 无权限 | 已认证但无操作权限 |\n| 404 Not Found | 资源不存在 | 查询的对象不存在 |\n| 500 Internal Server Error | 服务器错误 | 代码异常 |\n\n**URL 命名规范：**\n- 使用名词复数：`/api/customers/`（不用 `/api/customer/`）\n- 列表与详情通过路径区分：`/api/customers/`（列表）、`/api/customers/1/`（详情）\n- 层级关系体现资源关联：`/api/customers/1/follow-ups/`\n\n### DRF 安装与配置\n\nDjango REST Framework（简称 DRF）是 Django 的第三方扩展，专为快速构建 RESTful API 而设计。\n\n**安装：**\n\n```bash\npip install djangorestframework==3.16.1\n```\n\n**注册应用：**\n\n在 `settings.py` 的 `INSTALLED_APPS` 中添加 `'rest_framework'`：\n\n```python\nINSTALLED_APPS = [\n # ... Django 内置应用 ...\n # 第三方\n 'rest_framework',\n # 本项目应用\n 'apps.users',\n 'apps.customers',\n # ...\n]\n```\n\n**DRF 核心组件关系图：**\n\n```mermaid\ngraph LR\n Client[客户端] --> APIView[APIView 基础视图]\n APIView --> Request[Request 请求解析]\n Request --> Auth[认证 Authentication]\n Auth --> Permission[权限 Permission]\n Permission --> Throttle[限流 Throttle]\n Throttle --> Serializer[Serializer 序列化器]\n Serializer --> Model[ORM Model]\n Serializer --> Response[Response]\n Response --> Renderer[Renderer渲染器]\n Renderer --> Client\n\n APIView -.拓展.-> ViewSet[ViewSet/ModelViewSet]\n ViewSet -.使用.-> Pagination[分页]\n ViewSet -.使用.-> Filter[过滤器]\n```\n\n\n\n### Serializer 序列化器\n\nSerializer 是 DRF 的核心组件，负责两个方向的数据转换：\n\n**序列化 Serialize**\n\n后端从数据库查出 Model 模型对象，经过 Serializer 转换成 JSON 字符串，作为响应传给前端。查询接口（GET 详情 / 列表）使用。\n\n**反序列化 Deserialize**\n\n前端发送 JSON 请求，Serializer 接收数据、**执行字段校验**，合法数据转换成 Model 对象，即可保存到数据库。新增、修改接口（POST/PUT）使用。\n\n```mermaid\ngraph LR\n Frontend[\"前端<br/>JSON 请求数据\"]\n BackendModel[\"后端<br/>Model ORM对象\"]\n Ser[Serializer<br/>序列化器]\n\n %% 反序列化链路\n Frontend --①反序列化+数据校验--> Ser\n Ser --生成/更新--> BackendModel\n\n %% 序列化链路\n BackendModel --②序列化--> Ser\n Ser --输出JSON--> Frontend\n\n style Ser fill:#82c1ff,stroke:#0055aa\n```\n\n\n\n**Serializer vs ModelSerializer 对比：**\n\n| 特性 | Serializer | ModelSerializer |\n|------|-----------|-----------------|\n| 字段定义 | 手动逐个声明 | 根据 model 自动生成 |\n| create/update | 必须手动实现 | 自动生成 |\n| 适用场景 | 复杂自定义逻辑 | 标准 CRUD（推荐） |\n\n**ModelSerializer 关键属性：**\n\n| 属性 | 作用 | 示例 |\n|------|------|------|\n| `model` | 关联的模型类 | `model = Customer` |\n| `fields` | 包含的字段 | `'__all__'` 或 `('name','phone')` |\n| `read_only_fields` | 只读字段（不可写） | `('owner','created_at')` |\n| `extra_kwargs` | 字段额外参数 | `{'phone':{'required':True}}` |\n\n\n\n**序列化两种模式**\n\n```py\n# 模式1：序列化（查数据，Model对象 → JSON）\nser = XxxSerializer(instance=obj)\n\n# 模式2：反序列化（接收前端JSON，准备入库）\nser = XxxSerializer(data=request.data)\n\n# 模式3：更新操作（同时传入实例+数据）\nser = XxxSerializer(instance=obj,data=request.data)\n```\n\n**属性**\n\n- `.data`：序列化结果（instance 模式使用）\n\n**方法**：\n\n+ `.is_valid(raise_exception=False)`：执行校验\n+ `.save(**kwargs)`：调用 create/update 保存数据\n+ `.create(validated_data)`：自定义创建逻辑（重写）\n+ `.update(instance,validated_data)`：自定义更新逻辑（重写）\n\n\n\n### APIView 视图类\n\nAPIView 是 DRF 最基础视图基类，**对原生 Django 请求（HttpRequest）、响应（HttpResponse）进行包装**，提供扩展的`Request`、`Response`对象；\n\n并且内置认证、权限、限流、异常处理整套 API 通用逻辑。\n\n\n\n**APIView 与 Django View 的区别：**\n\n| 特性 | Django View | DRF APIView |\n|------|-------------|-------------|\n| 请求对象 | HttpRequest | Request（支持解析 JSON） |\n| 响应对象 | HttpResponse | Response（自动协商内容类型） |\n| 认证 | 需手动实现 | 内置认证类 |\n| 权限 | 需手动实现 | 内置权限类 |\n| 异常处理 | 需手动实现 | 自动转换为合适的状态码 |\n\n**APIView 处理流程：**\n\n```txt\nHTTP 请求\n → 初始化 Request 对象（解析 body、认证用户）\n → 执行权限检查 （permission_classes）\n → 分发到对应方法 （get/post/put/delete）\n → 返回 Response 对象\n → 自动渲染为 JSON\n```\n\n```mermaid\nsequenceDiagram\n participant Client as 客户端\n participant APIView as DRF APIView\n \n Client->>APIView:HTTP 请求\n APIView->>APIView:【初始化 Request 对象】<br/>解析请求体、执行认证Authentication\n APIView->>APIView:【执行权限检查 Permission】\n APIView->>APIView:【执行限流 Throttling】\n APIView->>APIView:【dispatch分发路由】<br/>调用对应方法 get/post/put/delete\n APIView->>APIView:【业务逻辑处理】（Serializer、ORM操作）\n APIView->>APIView:【构造 Response 对象】\n APIView->>APIView:【Renderer渲染器自动转为JSON】\n APIView->>Client:返回JSON响应\n```\n\n\n\n### Request 与 Response 对象\n\n**Request 对象关键属性：**\n\n| 属性 | 说明 | 示例 |\n|------|------|------|\n| `request.data` | 解析后的`请求体`数据（dict），(POST、PUT)| `request.data['name']` |\n| `request.query_params` | `URL` 查询`参数`，（GET、DELETE） | `request.query_params.get('page')` |\n| `request.user` | 认证后的用户对象 | `request.user.username` |\n| `request.method` | HTTP 方法 | `'GET'`、`'POST'` |\n\n**Response 对象：**\n\n```python\nfrom rest_framework.response import Response\n\n# Response 会根据请求头 Accept 自动选择渲染器（默认 JSON）\nResponse(data,status=200)\n```\n\n\n\n### 统一响应格式\n\n在企业项目中，API 响应格式应统一。本项目采用如下结构：\n\n```json\n{\n \"code\":200,\n \"msg\":\"success\",\n \"data\":{...}\n}\n```\n\n- `code`：业务状态码，200 表示成功，失败时取 HTTP 状态码\n- `msg`：提示信息\n- `data`：实际数据，可为对象、列表或 null\n\n\n\n### ORM 常用方法大全\n\n在 views.py 中是用Django 的 ORM常用方法，可以CRUD 表中的数据\n\n**示例模型**：Customer\n\n| 分类 | 方法 | 语法示例 | 返回值 | 核心说明 & 教学要点 |\n| ---------------- | ------------------ | --------------------------------------------------- | ----------------- | ------------------------------------------------------------ |\n| **查询多条数据** | all()| `Customer.objects.all()` | QuerySet | 查询全部数据；惰性查询，不会立即执行 SQL |\n| | filter()| `Customer.objects.filter(status=\"qualified\")` | QuerySet | 条件筛选，等价 WHERE；支持链式调用 |\n| | exclude()| `Customer.objects.exclude(status=\"lost\")` | QuerySet | 排除条件，等价 NOT WHERE |\n| | order_by()| `Customer.objects.order_by(\"-created_at\")` | QuerySet | 排序，`-字段`代表倒序 |\n| | values()| `Customer.objects.values(\"id\",\"name\")` | 字典列表 QuerySet | 只查询指定字段，节省内存 |\n| | values_list()| `Customer.objects.values_list(\"id\",\"name\")` | 元组列表 QuerySet | 获取元组形式数据 |\n| **查询单条数据** | get()| `Customer.objects.get(id=1)` | 模型实例 | 要求**有且仅有 1 条**；找不到 / 多条都会抛异常 |\n| | first()| `Customer.objects.filter(...).first()` | 实例 / None | 获取第一条，无数据返回 None，安全 |\n| | last()| `Customer.objects.filter(...).last()` | 实例 / None | 获取最后一条，无数据返回 None |\n| **统计判断** | count()| `Customer.objects.filter(...).count()` | int | 统计匹配数据条数 |\n| | exists()| `Customer.objects.filter(...).exists()` | bool | 判断是否存在数据；性能优于 count()>0 |\n| **关联查询优化** | select_related()| `Customer.objects.select_related(\"owner\")` | QuerySet | 正向 FK / 一对一；SQL JOIN 联表查询，解决 N+1；**不支持一对多、多对多** |\n| | prefetch_related()| `User.objects.prefetch_related(\"customers\")` | QuerySet | 一对多反向、多对多；多次查询，内存合并数据，不使用 JOIN |\n| **新增数据** | save()| `obj=Customer(...);obj.save()` | — | 先创建对象，调用保存；触发模型 save 信号 |\n| | create()| `Customer.objects.create(name=\"xiaodong\")` | 模型实例 | 快速创建并保存，一步到位 |\n| | bulk_create()| `Customer.objects.bulk_create([obj1,obj2])` | 实例列表 | 批量插入多条，高效；**不触发 save()** |\n| **更新数据** | save()| `obj.name=\"xxx\";obj.save()` | — | 单条更新；触发 save 信号 |\n| | update()| `Customer.objects.filter(...).update(status=\"won\")` | int | 批量更新；返回受影响行数；**不触发 save 信号** |\n| **删除数据** | delete()| `Customer.objects.filter(...).delete()` |(总条数，字典)| 支持单条、批量删除；默认级联删除 |\n\n\n\n**常用查询筛选器（双下划线查询）**\n\n| 筛选写法 | 示例 | SQL 含义 |\n| ------------ | -------------------------------- | ---------------------- |\n| __contains | `name__contains=\"小\"` | LIKE '% 小 %' 模糊匹配 |\n| __startswith | `name__startswith=\"王\"` | LIKE ' 王 %' |\n| __gt | `id__gt=5` | id > 5 |\n| __lt | `id__lt=10` | id < 10 |\n| __gte | `id__gte=5` | id >= 5 |\n| __lte | `id__lte=10` | id <= 10 |\n| __in | `status__in=[\"won\",\"qualified\"]` | IN(...)|\n| __isnull | `phone__isnull=True` | phone IS NULL |\n\n**使用注意**：\n\n+ QuerySet 惰性加载：代码定义查询，不会访问数据库，遍历 / 取值时才执行 SQL\n\n+ get()严禁直接用于接口，推荐 `filter().first()` 避免 500 报错\n\n+ bulk_create /update/delete 批量操作**不会触发模型 save()、信号**\n\n+ QuerySet 支持切片 `[:10]`，不支持负索引 `[:-5]`\n\n+ filter 可以链式连续调用：`filter().exclude().order_by()`\n\n\n\n### Django 路由配置\n\n路由（URLconf）：**将浏览器访问的 URL 地址，匹配到对应的视图函数 / 视图类**。\n\n```mermaid\nflowchart LR\n A[浏览器输入/访问URL] --> B[Django urls.py 路由匹配]\n B --> C{匹配成功?}\n C --否--> D[返回404页面]\n C --是--> E[分发到对应视图View]\n E --> F[视图执行业务逻辑<br/>ORM操作、序列化处理]\n F --> G[构造Response响应]\n G --> H[数据返回浏览器]\n```\n\n#### **两大路由文件**\n\n1. 项目根 urls.py（总路由）\n\n 负责分发，使用`include()`把子路由交给 apps 内部 urls\n\n2. apps 内 urls.py（子路由，如 customers/urls.py）\n\n 编写当前模块的接口地址\n\n\n\n#### **基本语法**\n\n**根路由示例（crm_project_root/urls.py）**\n\n```py\nfrom django.contrib import admin\nfrom django.urls import path,include\n\nurlpatterns = [\n path('admin/',admin.site.urls),\n # 分发客户相关接口到app内部路由\n path('api/',include('apps.customers.urls')),\n]\n```\n\n**app 子路由（apps/customers/urls.py）**\n\n```py\nfrom django.urls import path\nfrom .views import CustomerListCreateView\n\nurlpatterns = [\n # 客户列表&新增接口\n path('customers/',CustomerListCreateView.as_view()),\n]\n```\n\n访问路径为： http://1270.0.1:8000/api/customers\n\n\n\n**path 路劲配置**\n\n```py\n# 地址不带参数\npath('customers/',CustomerListCreateView.as_view()),\n# 地址带 int 类型的名字为 pk 的参数\npath('customers/<int:pk>',CustomerListCreateView.as_view()),\n```\n\n\n\n\n\n## 核心代码示例\n\n### DRF 在 settings.py 中的配置\n\n```python\n# crm_project_root/settings.py（DRF 相关配置）\n\nINSTALLED_APPS = [\n 'django.contrib.admin',\n 'django.contrib.auth',\n 'django.contrib.contenttypes',\n 'django.contrib.sessions',\n 'django.contrib.messages',\n 'django.contrib.staticfiles',\n # 第三方\n 'rest_framework',# DRF 核心\n # 本项目应用\n 'apps.users',\n 'apps.customers',\n 'apps.follow_ups',\n 'apps.comments',\n 'apps.statistics',\n]\n\n# DRF 全局配置\nREST_FRAMEWORK ={\n # 默认所有接口需登录访问\n 'DEFAULT_PERMISSION_CLASSES':(\n 'rest_framework.permissions.IsAuthenticated',\n),\n # 统一分页\n # 'DEFAULT_PAGINATION_CLASS':'utils.pagination.StandardResultsSetPagination',\n # 'PAGE_SIZE':10,\n # 统一异常处理\n # 'EXCEPTION_HANDLER':'utils.exceptions.custom_exception_handler',\n}\n```\n\n> 说明：`DEFAULT_PERMISSION_CLASSES` 设为 `IsAuthenticated` 表示默认所有接口都需要登录。\n>\n> 公开接口（如注册）在视图(views.py)内通过 `permission_classes = [AllowAny]` 单独放行。\n>\n> ```py\n> from rest_framework.permissions import AllowAny\n> ```\n\n\n\n### 统一响应格式工具\n\n```python\n# utils/response.py\n\"\"\"\n统一响应格式工具。\n全站接口统一返回：{\"code\":200,\"msg\":\"success\",\"data\":...}\n- code == 200 表示业务成功，前端据此判断\n- 失败时 code 取 HTTP 状态码，msg 为错误信息\n\"\"\"\nfrom rest_framework.response import Response\nfrom rest_framework import status as http_status\n\n\ndef success(data=None,msg='success',http_code=http_status.HTTP_200_OK,headers=None):\n \"\"\"成功响应。\"\"\"\n return Response(\n{'code':200,'msg':msg,'data':data},\n status=http_code,\n headers=headers,\n)\n\n\ndef fail(msg='error',http_code=http_status.HTTP_400_BAD_REQUEST,data=None):\n \"\"\"失败响应。\"\"\"\n return Response(\n{'code':http_code,'msg':msg,'data':data},\n status=http_code,\n)\n```\n\n**使用方式**：\n\nviews.py 中使用\n\n```python\nfrom utils.response import success,fail\n\n# 成功返回数据\nreturn success(data={'id':1,'name':'张三'})\n\n# 成功返回列表\nreturn success(data=customer_list)\n\n# 创建成功（HTTP 201）\nreturn success(data=serializer.data,msg='创建成功',http_code=201)\n\n# 参数错误\nreturn fail(msg='客户姓名不能为空',http_code=400)\n\n# 资源不存在\nreturn fail(msg='客户不存在',http_code=404)\n```\n\n\n\n### CustomerSerializer 完整代码\n\n```python\n# apps/customers/serializers.py\nfrom rest_framework import serializers\nfrom .models import Customer\n\n\nclass CustomerSerializer(serializers.ModelSerializer):\n \"\"\"客户序列化器，附带负责人姓名便于展示。\"\"\"\n\n # 通过 source 跨表取 owner 的 username，read_only=True 表示只输出不接收输入\n owner_name = serializers.CharField(source='owner.username',read_only=True)\n\n class Meta:\n model = Customer\n fields = '__all__'\n # owner 由后端根据登录用户自动写入，created/updated 由数据库维护\n read_only_fields =('owner','created_at','updated_at')\n```\n\n**代码解析：**\n\n| 代码 | 作用 |\n|------|------|\n| `owner_name = serializers.CharField(source='owner.username',read_only=True)` | 新增一个 `owner_name` 字段，值取自关联用户的 `username`，前端只读 |\n| `fields = '__all__'` | 序列化模型的所有字段 |\n| `read_only_fields` | 这些字段仅输出（序列化），不接受前端传入（反序列化），由后端自动处理 |\n\n**终端测试**：\n\n```bat\npython manage.py shell\n```\n\n```py\nfrom apps.customers.models import Customer\nfrom apps.customers.serializers import CustomerSerializer\n\n# 1. 获取单条模型对象\ncust = Customer.objects.first()\nser = CustomerSerializer(cust)\n\n# 核心：ser.data 就是序列化输出字典\nprint(ser.data)\n```\n\n\n\n**序列化输出示例（JSON）：**\n\n```json\n{\n \"id\":1,\n \"name\":\"张三\",\n \"company\":\"ABC科技有限公司\",\n \"phone\":\"13800138000\",\n \"email\":\"zhangsan@abc.com\",\n \"address\":\"北京市朝阳区\",\n \"source\":\"online\",\n \"status\":\"lead\",\n \"owner\":1,\n \"owner_name\":\"admin\",\n \"description\":\"\",\n \"created_at\":\"2025-07-15T10:00:00+08:00\",\n \"updated_at\":\"2025-07-15T10:00:00+08:00\"\n}\n```\n\n\n\n### CustomerListCreateView（列表 & 创建）\n\n```python\n# apps/customers/views.py（Day 4 版本：使用 APIView）\nfrom rest_framework.views import APIView\nfrom rest_framework.permissions import AllowAny\n\nfrom utils.response import success,fail\nfrom .models import Customer\nfrom .serializers import CustomerSerializer\n\n\nclass CustomerListCreateView(APIView):\n \"\"\"客户列表查询 & 新建客户。\n\n GET /api/customers/ → 获取客户列表\n POST /api/customers/ → 新建客户\n \"\"\"\n\t# 先放开登录权限，正式运行需要注释\n permission_classes = [AllowAny]\n\n def get(self,request):\n \"\"\"获取客户列表。\"\"\"\n customers = Customer.objects.select_related('owner').all()\n serializer = CustomerSerializer(customers,many=True)\n return success(data=serializer.data)\n\t\n \n def post(self,request):\n \"\"\"新建客户。\"\"\"\n serializer = CustomerSerializer(data=request.data)\n if serializer.is_valid():\n # ==== 读取 settings.AUTH_USER_MODEL 应用\n # 模拟用户：直接查询固定用户对象，替代 request.user\n from django.contrib.auth import get_user_model\n User = get_user_model()\n # 选择数据库中已存在的用户ID，根据你的环境修改id\n mock_user = User.objects.filter(id=1).first()\n \n # owner 字段为只读，需手动设置为当前登录用户\n serializer.save(owner=mock_user)\n return success(data=serializer.data,msg='创建成功',http_code=201)\n return fail(msg=serializer.errors,http_code=400)\n```\n\n\n\n### CustomerDetailView（详情 & 修改 & 删除）\n\n```python\n# apps/customers/views.py（续）\nclass CustomerDetailView(APIView):\n \"\"\"客户详情查询 & 修改 & 删除。\n\n GET /api/customers/<int:pk>/ → 获取单个客户详情\n PUT /api/customers/<int:pk>/ → 全量更新客户\n DELETE /api/customers/<int:pk>/ → 删除客户\n \"\"\"\n \n\t# 先放开登录权限，正式运行需要注释\n permission_classes = [AllowAny]\n\n\n def get(self,request,pk):\n \"\"\"获取客户详情。\"\"\"\n customer = Customer.objects.filter(pk=pk).first()\n if customer is None:\n return fail(msg='客户不存在',http_code=404)\n serializer = CustomerSerializer(customer)\n return success(data=serializer.data)\n\n def put(self,request,pk):\n \"\"\"全量更新客户信息。\"\"\"\n customer = Customer.objects.filter(pk=pk).first()\n if customer is None:\n return fail(msg='客户不存在',http_code=404)\n serializer = CustomerSerializer(customer,data=request.data)\n if serializer.is_valid():\n serializer.save()\n return success(data=serializer.data,msg='更新成功')\n return fail(msg=serializer.errors,http_code=400)\n\n def delete(self,request,pk):\n \"\"\"删除客户。\"\"\"\n customer = Customer.objects.filter(pk=pk).first()\n if customer is None:\n return fail(msg='客户不存在',http_code=404)\n customer.delete()\n return success(msg='删除成功')\n```\n\n\n\n### urls.py 路由配置\n\n```python\n# apps/customers/urls.py（Day 4 版本：使用 path 方式）\nfrom django.urls import path\n\nfrom .views import CustomerListCreateView,CustomerDetailView\n\nurlpatterns = [\n # 列表 & 创建\n path('customers/',CustomerListCreateView.as_view(),name='customer-list'),\n # 详情 & 修改 & 删除\n path('customers/<int:pk>/',CustomerDetailView.as_view(),name='customer-detail'),\n]\n```\n\n\n\n**根路由挂载：**\n\n```python\n# crm_project_root/urls.py\nfrom django.contrib import admin\nfrom django.urls import path,include\n\nurlpatterns = [\n path('admin/',admin.site.urls),\n # 各业务应用统一以 /api/ 为前缀\n path('api/',include('apps.customers.urls')),\n]\n```\n\n最终 API 端点一览：\n\n| 方法 | URL | 说明 |\n|------|-----|------|\n| GET | `/api/customers/` | 获取客户列表 |\n| POST | `/api/customers/` | 新建客户 |\n| GET | `/api/customers/1/` | 获取客户详情 |\n| PUT | `/api/customers/1/` | 更新客户 |\n| DELETE | `/api/customers/1/` | 删除客户 |\n\n\n\n### 接口测试步骤\n\n#### 基本操作\n\n**步骤一：创建集合与环境**\n\n打开 Apifox ，我的团队 --> 个人团队 --> 点击 新建项目，命名为 \"crm_project_root\"\n\n\n\n**步骤二**： 新建项目环境\n\n右上角三横杆 --> 新建环境 --> crm_project_root --> 填写共用api地址头:默认模块 --> http://127.0.0.1:8000/api\n\n\n\n**步骤三**：根据模块创建目录，用来存放模块的测试案例\n\n接口 --> 添加目录 --> 客户管理\n\n\n\n#### GET 列表测试\n\n1. 添加 \"HTTP接口\"，选择环境为 “crm_project_root” \n2. 选择 `GET`，URL 输入 `/customers/`\n3. 点击 \"发送\"，预期返回：\n\n```json\n{\n \"code\":200,\n \"msg\":\"success\",\n \"data\":[\n{\n \"id\":2,\n \"owner_name\":\"root\",\n \"name\":\"xiaodong\",\n ...\n},\n ...\n ]\n}\n```\n\n4. 保存测试案例 --> 客户列表查询\n\n\n\n#### POST 创建测试\n\n1. 项目 settings.py 中注释跨域限制\n\n ```py\n MIDDLEWARE = [\n 'django.middleware.security.SecurityMiddleware',\n 'django.contrib.sessions.middleware.SessionMiddleware',\n 'django.middleware.common.CommonMiddleware',\n # 注释 csrf 跨域认证，前后端分离使用token 认证\n # 'django.middleware.csrf.CsrfViewMiddleware',\n 'django.contrib.auth.middleware.AuthenticationMiddleware',\n 'django.contrib.messages.middleware.MessageMiddleware',\n 'django.middleware.clickjacking.XFrameOptionsMiddleware',\n ]\n ```\n\n2. 添加 \"HTTP接口\"，选择环境为 “crm_project_root”\n\n3. 选择 `POST` URL 输入 `/customers/`\n\n4. 点击 \"Body\" → 选择 \"JSON\"\n\n5. 输入请求体：\n\n```json\n{\n \"name\":\"李四\",\n \"company\":\"DEF贸易\",\n \"phone\":\"13900139000\",\n \"email\":\"lisi@def.com\",\n \"source\":\"referral\",\n \"status\":\"lead\",\n \"description\":\"老客户推荐\"\n}\n```\n\n5. 点击 \"发送\"，预期 HTTP 201 返回新建的客户数据\n\n\n\n#### GET 详情测试\n\n1. 新建 Request，选择 `GET` 方法\n2. URL 输入 `/customers/1/`\n3. 点击 \"Send\"，预期返回 id=1 的客户详情\n\n\n\n#### PUT 更新测试\n\n1. 新建 Request，选择 `PUT` 方法\n2. URL 输入 `/customers/1/`\n3. Body 输入完整客户数据（修改 status 为 `contacted`）：\n\n```json\n{\n \"name\":\"张三\",\n \"company\":\"ABC科技有限公司\",\n \"phone\":\"13800138000\",\n \"email\":\"zhangsan@abc.com\",\n \"address\":\"北京市朝阳区\",\n \"source\":\"online\",\n \"status\":\"contacted\",\n \"description\":\"已联系\"\n}\n```\n\n4. 点击 \"Send\"，预期返回更新后的数据\n\n\n\n#### DELETE 删除测试\n\n1. 新建 Request，选择 `DELETE` 方法\n2. URL 输入 `/customers/3/`\n3. 点击 \"Send\"，预期返回 `{\"code\":200,\"msg\":\"删除成功\",\"data\":null}`\n\n> 注意：本项目默认所有接口需认证（`IsAuthenticated`）。Day 6 学习 JWT 后，需在 Postman Headers 中添加 `Authorization:Bearer <token>`。\n>\n> Day 4 演示阶段可临时将视图的 `permission_classes` 设为 `[AllowAny]` 以便测试。\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n\n为 Customer 模型创建完整的 RESTful API，实现五个标准端点，并采用统一响应格式。\n\n### 实现步骤\n\n1. 在 `settings.py` 的 `INSTALLED_APPS` 中注册 `rest_framework`，添加 `REST_FRAMEWORK` 全局配置\n2. 创建 `utils/response.py`，编写 `success` 和 `fail` 统一响应函数\n3. 在 `apps/customers/serializers.py` 中创建 `CustomerSerializer`，包含 `owner_name` 只读字段\n4. 在 `apps/customers/views.py` 中编写 `CustomerListCreateView`（GET 列表 / POST 创建）\n5. 在 `apps/customers/views.py` 中编写 `CustomerDetailView`（GET 详情 / PUT 更新 / DELETE 删除）\n6. 在 `apps/customers/urls.py` 中配置两条 path 路由\n7. 在 `crm_project/urls.py` 中挂载 customers 子路由到 `/api/` 前缀\n8. 在 `settings.py` 的 `INSTALLED_APPS` 中注释跨域验证 `CsrfViewMiddleware`\n9. 启动服务器，使用 Apifox逐一测试五个端点\n\n```bash\n# 启动开发服务器\npython manage.py runserver\n```\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n\n用 AI 生成 Serializer 和基础 APIView 代码，学习如何通过 Prompt 描述 API 需求。\n\n### 场景\n\n为 Customer 模型创建序列化器和 CRUD 接口时，代码结构高度模式化。通过编写精准的 Prompt，让 AI 一次性生成高质量的代码骨架。\n\n### Prompt 示例\n\n```\n你是一位 Django REST Framework 开发工程师。\n\n请根据以下模型定义，生成：\n1. 一个 ModelSerializer，包含所有字段，owner/created_at/updated_at 设为只读，\n 额外增加 owner_name 字段（source='owner.username'，read_only=True）\n2. 两个 APIView：\n - ListCreateView：GET 返回列表，POST 创建（owner 自动设为当前用户）\n - DetailView：GET 返回详情，PUT 全量更新，DELETE 删除\n3. 一个 utils.response.py 统一使用 success/fail 函数返回{code,msg,data}格式\n\n模型定义：\n[粘贴 Customer 模型代码]\n\n要求：\n1. 中文注释\n2. 使用 select_related 优化查询\n3. 当前登录用户使用查询pk=1的用户来模拟\n3. 错误处理完善（对象不存在返回 404，校验失败返回 400）\n4. 代码可直接放入 Django 项目运行\n```\n\n### 实践要点\n\n- Prompt 中明确列出需要的字段、只读字段和额外字段，AI 才能准确生成 `Meta` 配置\n- 提供模型定义作为上下文，AI 能正确识别字段类型和关联关系\n- 指定统一响应格式（`success/fail`），确保生成的代码与项目风格一致\n- 生成后需检查：`read_only_fields` 是否正确、`save(owner=request.user)` 是否遗漏、错误处理是否完善\n\n\n\n## 课堂练习\n\n**练习 1**（基础）：为 FollowUp 模型创建 `FollowUpSerializer`，包含 `customer_name` 和 `user_name` 两个只读字段（分别取 `customer.name` 和 `user.username`），`user` 和时间字段设为只读。\n\n思路提示：参照 `CustomerSerializer` 的写法，`source` 参数支持跨表取值。\n\n**练习 2**（中等）：为 FollowUp 模型创建 `FollowUpListCreateView`（APIView），实现 GET 返回跟进记录列表、POST 创建跟进记录（`user` 自动设为当前登录用户）。\n\n思路提示：\n- GET 方法中查询 `FollowUp.objects.select_related('customer','user').all()`\n- POST 方法中 `serializer.save(user=mock_user)`\n- 使用 `many=True` 序列化列表数据\n\n## 课后作业\n\n### 必做\n\n1. 完成 Customer 的五个 API 端点并通过 Apifox 测试\n2. 为 FollowUp 模型创建完整的 Serializer 和五个 APIView 端点\n3. 配置 FollowUp 的路由并挂载到 `/api/` 前缀\n4. 使用 Apifox 测试 FollowUp 的全部接口，截图保存\n\n### 选做（进阶）\n\n1. 在 `CustomerListCreateView` 的 GET 方法中，支持通过 `?status=lead` 查询参数筛选客户状态\n2. 尝试用 AI 生成 Comment 模型的 Serializer 和 APIView，检查生成质量并修正\n\n### 提交方式\n\n- 提交 serializers.py、views.py、urls.py 代码文件\n- 附上 Apifox 测试截图（每个端点至少一张）\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[DRF 基础与 API 视图] --> B[RESTful 设计原则]\n A --> C[DRF 安装配置]\n A --> D[Serializer]\n A --> E[APIView]\n A --> F[统一响应格式]\n\n B --> B1[HTTP 方法语义]\n B --> B2[状态码规范]\n B --> B3[URL 命名]\n\n C --> C1[INSTALLED_APPS]\n C --> C2[REST_FRAMEWORK 配置]\n\n D --> D1[ModelSerializer]\n D --> D2[fields/__all__]\n D --> D3[read_only_fields]\n D --> D4[source 跨表取值]\n\n E --> E1[Request 对象]\n E --> E2[Response 对象]\n E --> E3[get/post/put/delete]\n E --> E4[permission_classes]\n\n F --> F1[success 函数]\n F --> F2[fail 函数]\n F --> F3[code/msg/data 结构]\n```\n\n## 下节预告\n\n- 下一节主题：DRF 进阶（ViewSet & Router）\n- 需要预习的内容：\n - 复习今天 APIView 的写法，思考：列表和详情视图的代码有哪些重复？\n - 了解 Django 的 as_view()机制\n - 思考：能否用更少的代码实现同样功能？ViewSet 是如何做到的？\n - 了解分页、筛选、搜索在 API 中的作用\n","order":4},{"chapterName":"05-DRF进阶ViewSet和Router","content":"# Day 5 - DRF 进阶（ViewSet & Router）\n\n## 学习目标\n\n1. **知识目标**：理解 ViewSet 与 ModelViewSet 的设计理念，掌握 Router 自动路由机制，理解分页/筛选/搜索/排序的工作原理\n2. **技能目标**：能用 ModelViewSet 替代 APIView 大幅精简代码，使用 DefaultRouter 自动生成路由，配置 DjangoFilterBackend/SearchFilter/OrderingFilter，编写自定义 @action\n3. **项目目标**：将 Customer 从 APIView 重构为 ModelViewSet + Router，为 FollowUp 创建完整 ViewSet，添加分页和筛选功能\n4. **AI 实践目标**：让 AI 对比生成 APIView 与 ViewSet 两种写法，理解重构差异和代码精简的本质\n\n## 知识点梳理\n\n### ViewSet 与 ModelViewSet\n\nDay 4 中我们用两个 APIView（ListCreateView + DetailView）实现了五个端点，代码中存在大量重复：序列化器实例化、错误处理等。ViewSet 通过将操作映射到 actions 来消除这些重复。\n\n**APIView vs ViewSet vs ModelViewSet 对比：**\n\n| 特性 | APIView | ViewSet | ModelViewSet |\n|------|---------|---------|--------------|\n| 需定义的方法 | get/post/put/delete | list/create/retrieve/update/destroy | 全部自动提供 |\n| 路由配置 | 手动写 path | 需手动绑定 action | Router 自动生成 |\n| 代码量 | 最多（~80 行） | 中等（~40 行） | 最少（~15 行） |\n| 灵活性 | 最高 | 高 | 中（可覆写任意方法） |\n| 适用场景 | 高度定制接口 | 需要自定义 action | 标准 CRUD（推荐） |\n\n**ModelViewSet 自动提供的 7 个 action：**\n\n| Action | HTTP 方法 | URL | 对应方法 | 内部重点钩子方法 | 方法描述 |\n| -------------- | --------- | ------------------ | ------------------ | ---------------------------- | ------------------------------------ |\n| list | GET | `/customers/` | `list()` | `get_queryset()` | 查询多条资源，返回资源列表 |\n| create | POST | `/customers/` | `create()` | `perform_create(serializer)` | 创建一条新资源 |\n| retrieve | GET | `/customers/{id}/` | `retrieve()` | `get_queryset()` | 查询单条指定 id 资源详情 |\n| update | PUT | `/customers/{id}/` | `update()` | `perform_update(serializer)` | **全量更新**资源，必须传全部必填字段 |\n| partial_update | PATCH | `/customers/{id}/` | `partial_update()` | `perform_update(serializer)` | **局部更新**，只传需要修改字段 |\n| destroy | DELETE | `/customers/{id}/` | `destroy()` | `perform_destroy(instance)` | 删除指定 id 资源 |\n\n**ModelViewSet 请求处理流程：**\n\n```mermaid\nflowchart TD\n A[\"HTTP请求\"] --> B[\"Router路由匹配，识别action\"]\n B --> C[\"dispatch()总调度\"]\n C --> C1[\"认证｜权限校验\"]\n C1 --> D{Action分支}\n\n D -->|list| L[\"list()<br/>get_queryset →过滤→分页→序列化\"] --> Z[\"Response响应\"]\n D -->|create| CR[\"create()<br/>序列化校验 → perform_create()\"] --> Z\n D -->|retrieve| R[\"retrieve()<br/>get_object()→序列化\"] --> Z\n D -->|update<br/>partial_update| U[\"update()<br/>get_object()→校验→perform_update()\"] --> Z\n D -->|destroy| DE[\"destroy()<br/>get_object()→perform_destroy()\"] --> Z\n\n Z --> E[\"返回HTTP响应\"]\n```\n\n\n\n### Router 自动路由\n\nRouter 负责将 URL 自动映射到 ViewSet 的各个 action，无需手动编写每条 path。\n\n**DefaultRouter 的优势：**\n\n| 特性 | 手动 path | DefaultRouter |\n|------|-----------|---------------|\n| 路由数量 | 每个端点一条 | 一行注册全部 |\n| URL 尾部斜杠 | 需手动处理 | 自动支持有/无斜杠 |\n| API 根视图 | 无 | 自动生成 `/api/` 根视图 |\n| 可浏览 API | 需手动配置 | 自动支持 |\n\n```py\n# customers/urls.py\nfrom rest_framework.routers import DefaultRouter\nfrom .views import CustomerViewSet\n\nrouter = DefaultRouter()\nrouter.register(r'customers',CustomerViewSet)\n\nurlpatterns = router.urls\n```\n\n\n\n### 分页（Pagination）\n\n当列表数据量大时，需要分页返回。DRF 提供多种分页类：\n\n| 分页类 | 方式 | 参数 | 适用场景 |\n|--------|------|------|----------|\n| PageNumberPagination | 页码分页 | `?page=2&page_size=10` | 最常用 |\n| LimitOffsetPagination | 偏移分页 | `?limit=10&offset=20` | 需精确控制偏移 |\n| CursorPagination | 游标分页 | `?cursor=xxx` | 大数据集、无限滚动 |\n\n本项目使用自定义的 `StandardResultsSetPagination`，将分页结果包装为统一响应格式。\n\n\n\n### 筛选（DjangoFilterBackend）\n\n`DjangoFilterBackend` 配合 `FilterSet` 实现精确字段筛选：\n\n```\nGET /api/customers/?status=lead&source=online&owner=1\n```\n\n**三种 Filter 后端对比：**\n\n| 后端 | 用途 | 参数格式 | 示例 |\n|------|------|----------|------|\n| DjangoFilterBackend | 精确字段筛选 | `?字段名=值` | `?status=lead` |\n| SearchFilter | 模糊搜索 | `?search=关键词` | `?search=张三` |\n| OrderingFilter | 排序 | `?ordering=字段名` | `?ordering=-created_at` |\n\n\n\n### 搜索与排序\n\nDjangoFilterBackend 通过 `fields ` 配置字段筛选，使用 `?字段名=值`：\n\n```py\n# 完整写法示范，XxxFilter.py\nclass CustomerFilter(django_filters.FilterSet)\n class Meta:\n model = FollowUp\n fields ={\n 'status':['exact'],# 精确等于\n}\n# GET /api/customers/?status=lead → 匹配 status=lead 的记录\n```\n\n\n\n**SearchFilter** 通过 `search_fields` 配置搜索范围，使用 `?search=` 参数：\n\n```python\n# XxxViewSet.py\nsearch_fields = ['name','company','phone'] # 在这三个字段中搜索\n# GET /api/customers/?search=张 → 匹配 name/company/phone 中包含\"张\"的记录\n```\n\n**OrderingFilter** 通过 `ordering_fields` 配置可排序字段，使用 `?ordering=` 参数：\n\n```python\n# XxxViewSet.py\nordering_fields = ['created_at','updated_at']\n# GET /api/customers/?ordering=-created_at → 按创建时间倒序\n# GET /api/customers/?ordering=created_at → 按创建时间正序\n```\n\n\n\n### 自定义 Action（@action 装饰器）\n\n当标准 CRUD 之外需要额外接口时，使用 `@action` 装饰器自定义 action：\n\n```python\nfrom rest_framework.decorators import action\n\nclass CustomerViewSet(viewsets.ModelViewSet):\n # ...\n\n @action(detail=True,methods=['post'])\n def change_status(self,request,pk=None):\n \"\"\"修改单个客户状态。\"\"\"\n # detail=True → URL 为 /customers/{id}/change_status/\n # detail=False → URL 为 /customers/change_status/\n ...\n```\n\n| 参数 | 说明 | 示例 |\n|------|------|------|\n| `detail` | True=针对单个对象，False=针对列表 | `detail=True` |\n| `methods` | 允许的 HTTP 方法 | `methods=['post']` |\n| `url_path` | 自定义 URL 路径（默认方法名） | `url_path='change-status'` |\n\n\n\n### DefaultRouter+ModelViewSet完成流程\n\n```mermaid\nflowchart TD\n A[\"HTTP请求\"] --> B[\"Router路由匹配，确定action\"]\n B --> C[\"dispatch()总调度入口\"]\n \n C --> C1[\"get_authenticators()认证，解析Token得到request.user\"]\n C1 --> C2[\"get_permissions()获取权限类，执行权限校验<br/>不通过直接返回401/403\"]\n \n C2 --> D{判断action类型}\n \n %% list分支\n D -->|查询列表list| D1[\"list()\"]\n D1 --> D11[\"get_queryset()获取基础数据集<br/>行级数据权限过滤\"]\n D11 --> D12[\"filter_queryset()<br/>filterset_class、search、ordering\"]\n D12 --> D13[\"paginate_queryset()分页\"]\n D13 --> D14[\"get_serializer()序列化\"]\n D14 --> Z[\"返回Response响应\"]\n\n %% create分支\n D -->|新增数据create| D2[\"create()\"]\n D2 --> D21[\"get_serializer(data=request.data)\"]\n D21 --> D22[\"serializer.is_valid()参数校验\"]\n D22 --> D23[\"perform_create()保存钩子\"]\n D23 --> Z\n\n %% retrieve分支\n D -->|查询单条数据retrieve| D3[\"retrieve()\"]\n D3 --> D31[\"get_object()\"]\n D31 --> D311[\"内部调用 get_queryset()\"]\n D311 --> D32[\"get_serializer(instance)\"]\n D32 --> Z\n\n %% update / partial_update分支\n D -->|修改数据update / partial_update| D4[\"update()/ partial_update()\"]\n D4 --> D41[\"get_object()\"]\n D41 --> D411[\"内部调用 get_queryset()\"]\n D411 --> D42[\"get_serializer(partial=?)参数校验\"]\n D42 --> D43[\"perform_update()更新钩子\"]\n D43 --> Z\n\n %% destroy分支\n D -->|删除数据destroy| D5[\"destroy()\"]\n D5 --> D51[\"get_object()\"]\n D51 --> D511[\"内部调用 get_queryset()\"]\n D511 --> D52[\"perform_destroy()删除钩子\"]\n D52 --> Z\n\n Z --> E[\"HTTP响应返回客户端\"]\n```\n\n\n\n\n\n## 核心代码示例\n\n### 环境准备\n\n```bat\n# 安装筛选器插件\npython -m pip install django-filter\n\n# 警告解决\npython -m pip install -r requirements.txt\n\n# 写代码提示库安装\npip install djangorestframework-stubs[compatible-mypy]\n# 加清华源\npip install \"djangorestframework-stubs[compatible-mypy]\" -i https://pypi.tuna.tsinghua.edu.cn/simple\n```\n\n\n\n### settings.py 中的 REST_FRAMEWORK 配置\n\n```python\n# crm_project/settings/base.py\n\nINSTALLED_APPS = [\n 'django.contrib.admin',\n 'django.contrib.auth',\n 'django.contrib.contenttypes',\n 'django.contrib.sessions',\n 'django.contrib.messages',\n 'django.contrib.staticfiles',\n # 第三方\n 'rest_framework',\n 'django_filters',# django-filter，提供 DjangoFilterBackend\n # 本项目应用\n 'apps.users',\n 'apps.customers',\n 'apps.follow_ups',\n 'apps.comments',\n 'apps.statistics',\n]\n\n# DRF 全局配置\nREST_FRAMEWORK ={\n # 默认所有接口需登录访问，公开接口在视图内单独放行\n 'DEFAULT_PERMISSION_CLASSES':(\n 'rest_framework.permissions.IsAuthenticated',\n),\n # 统一分页\n 'DEFAULT_PAGINATION_CLASS':'utils.pagination.StandardResultsSetPagination',\n 'PAGE_SIZE':10,\n # 筛选与排序\n 'DEFAULT_FILTER_BACKENDS':(\n 'django_filters.rest_framework.DjangoFilterBackend',\n 'rest_framework.filters.SearchFilter',\n 'rest_framework.filters.OrderingFilter',\n),\n # 统一异常处理\n 'EXCEPTION_HANDLER':'utils.exceptions.custom_exception_handler',\n}\n```\n\n**配置项说明：**\n\n| 配置项 | 作用 |\n|--------|------|\n| `DEFAULT_PERMISSION_CLASSES` | 全局默认权限（需登录） |\n| `DEFAULT_PAGINATION_CLASS` | 全局分页器 |\n| `DEFAULT_FILTER_BACKENDS` | 全局筛选后端（精确筛选 + 搜索 + 排序） |\n| `EXCEPTION_HANDLER` | 统一异常处理函数 |\n\n### 统一分页器\n\n```python\n# utils/pagination.py\n\"\"\"\n统一分页器：将 DRF 默认分页结果包装为统一响应格式。\n返回结构：\n{\n \"code\":200,\n \"msg\":\"success\",\n \"data\":{\n \"count\":100,\n \"next\":\"http://.../?page=2\",\n \"previous\":null,\n \"results\":[...]\n}\n}\n\"\"\"\nfrom rest_framework.pagination import PageNumberPagination\nfrom rest_framework.response import Response\n\n\nclass StandardResultsSetPagination(PageNumberPagination):\n page_size = 10 # 默认每页条数\n page_size_query_param = 'page_size' # 允许前端通过 ?page_size=20 自定义\n max_page_size = 100 # 每页最大条数限制\n page_query_param = 'page'\n\n def get_paginated_response(self,data):\n return Response({\n 'code':200,\n 'msg':'success',\n 'data':{\n 'count':self.page.paginator.count,\n 'next':self.get_next_link(),\n 'previous':self.get_previous_link(),\n 'results':data,\n},\n})\n```\n\n**分页参数使用方式：**\n\n```\nGET /api/customers/?page=1 \t→ 第1页，每页10条\nGET /api/customers/?page=2&page_size=20 \t→ 第2页，每页20条\nGET /api/customers/?page_size=100 \t→ 每页100条（最大限制）\n```\n\n**分页响应示例：**\n\n```json\n{\n \"code\":200,\n \"msg\":\"success\",\n \"data\":{\n \"count\":35,\n \"next\":\"http://127.0.0.1:8000/api/customers/?page=2\",\n \"previous\":null,\n \"results\":[\n{\"id\":1,\"name\":\"张三\",...},\n{\"id\":2,\"name\":\"李四\",...}\n ]\n}\n}\n```\n\n### CustomerFilter 筛选器\n\n```python\n# apps/customers/filters.py\nimport django_filters\n\nfrom .models import Customer\n\n\nclass CustomerFilter(django_filters.FilterSet):\n \"\"\"客户筛选器：按状态、来源、负责人精确筛选。\"\"\"\n class Meta:\n model = Customer\n fields ={'status':['exact'],'source':['exact'],'owner':['exact']}\n```\n\n**筛选使用方式：**\n\n```\nGET /api/customers/?status=lead → 筛选状态为\"线索\"的客户\nGET /api/customers/?source=online → 筛选来源为\"网络\"的客户\nGET /api/customers/?status=won&owner=1 → 组合筛选：已成交 + 负责人ID为1\n```\n\n**FilterSet 中 fields 的语法说明：**\n\n```python\nfields ={\n '字段名':['查找方式'],# exact=精确匹配，contains=包含，gte=大于等于...\n}\n```\n\n**AI辅助学**： Django 中 DRF 里的 FilterSet 中 fields 的语法详解\n\n\n\n### CustomerViewSet 完整代码\n\n```python\n# apps/customers/views.py\nfrom rest_framework import viewsets\n# from rest_framework.permissions import IsAuthenticated\n\nfrom utils.response import success\nfrom .models import Customer\nfrom .serializers import CustomerSerializer\nfrom .filters import CustomerFilter\n# from apps.users.permissions import IsOwnerOrManager\n\n\nclass CustomerViewSet(viewsets.ModelViewSet):\n \"\"\"客户增删改查。\"\"\"\n serializer_class = CustomerSerializer # 序列化器，ModelViewSet自行调用\n filterset_class = CustomerFilter # 自定义的筛选器\n search_fields = ['name','company','phone'] # 模糊搜索数据\n ordering_fields = ['created_at','updated_at'] # 日期排序字段\n\n def get_queryset(self):\n # 数据权限：sales 仅能看自己负责的客户\n # user = self.request.user\n # 未实现登录，调整为模拟用户\n # 模拟用户：直接查询固定用户对象，替代 request.user\n from django.contrib.auth import get_user_model\n User = get_user_model()\n user = User.objects.filter(id=1).first()\n\n qs = Customer.objects.select_related('owner')\n if user.role == 'sales':\n qs = qs.filter(owner=user)\n return qs\n\n def perform_create(self,serializer):\n # 新建客户时负责人默认为当前登录用户\n # serializer.save(owner=self.request.user)\n\n # 模拟用户：直接查询固定用户对象，替代 request.user\n from django.contrib.auth import get_user_model\n User = get_user_model()\n user = User.objects.filter(id=1).first()\n serializer.save(owner=user)\n\n\n DEBUG_TEST = True # 给 Apifox 调试用\n def get_permissions(self):\n # 调试模式：所有action都不做权限校验，方便Apifox测试\n if self.DEBUG_TEST:\n return []\n\n # -------------------- 生产环境逻辑（正式上线DEBUG_TEST=False才会执行） -------------------\n\n # 修改与删除需通过对象级权限校验\n # if self.action in('update','partial_update','destroy'):\n # return [IsAuthenticated(),IsOwnerOrManager()]\n # return super().get_permissions()\n\n def destroy(self,request,*args,**kwargs):\n instance = self.get_object()\n self.perform_destroy(instance)\n return success(msg='删除成功')\n```\n\n**代码逐行解析：**\n\n| 代码 | 作用 |\n|------|------|\n| `serializer_class = CustomerSerializer` | 指定序列化器，ModelViewSet 自动调用 |\n| `filterset_class = CustomerFilter` | 绑定筛选器，自动处理 `?status=lead` 等参数 |\n| `search_fields = ['name','company','phone']` | 配置 SearchFilter 搜索范围 |\n| `ordering_fields = ['created_at','updated_at']` | 配置 OrderingFilter 可排序字段 |\n| `get_queryset()` | 根据用户角色动态过滤数据（数据隔离） |\n| `perform_create()` | 创建时自动写入 owner 字段 |\n| `get_permissions()` | 按 action 动态返回权限类 |\n| `destroy()` | 覆写删除方法，返回统一响应格式 |\n\n**与 Day 4 APIView 的对比（代码量）：**\n\n```\nDay 4 APIView：CustomerListCreateView（24行）+ CustomerDetailView（44行）= 68 行\nDay 5 ModelViewSet：CustomerViewSet = 35 行（含数据权限和权限控制）\n\n代码量减少约 50%，同时增加了分页、筛选、搜索、排序功能。\n```\n\n### FollowUpViewSet 完整代码\n\n```python\n# apps/follow_ups/views.py\nfrom rest_framework import viewsets\n\nfrom utils.response import success\nfrom .models import FollowUp\nfrom .serializers import FollowUpSerializer\n# from apps.users.permissions import IsFollowUpOwnerOrManager\n\n\nclass FollowUpViewSet(viewsets.ModelViewSet):\n \"\"\"跟进记录增删改查。\"\"\"\n serializer_class = FollowUpSerializer\n search_fields = ['content']\n ordering_fields = ['created_at','next_follow_date']\n\n def get_queryset(self):\n # sales 仅能看到自己负责客户的跟进记录\n # user = self.request.user\n # 未实现登录，调整为模拟用户\n # 模拟用户：直接查询固定用户对象，替代 request.user\n from django.contrib.auth import get_user_model\n User = get_user_model()\n user = User.objects.filter(id=1).first()\n\n # 关联用户和客户，查询跟进记录\n qs = FollowUp.objects.select_related('customer','user')\n if user.role == 'sales':\n qs = qs.filter(customer__owner=user)\n return qs\n\n\n def perform_create(self,serializer):\n # serializer.save(user=self.request.user)\n\n # 未实现登录，调整为模拟用户\n # 模拟用户：直接查询固定用户对象，替代 request.user\n from django.contrib.auth import get_user_model\n User = get_user_model()\n user = User.objects.filter(id=1).first()\n serializer.save(user=user)\n\n \n def destroy(self,request,*args,**kwargs):\n instance = self.get_object()\n self.perform_destroy(instance)\n return success(msg='删除成功')\n\n DEBUG_TEST = True # 给 Apifox 调试用\n def get_permissions(self):\n # 调试模式：所有action都不做权限校验，方便Apifox测试\n if self.DEBUG_TEST:\n return []\n\n # -------------------- 生产环境逻辑（正式上线DEBUG_TEST=False才会执行） -------------------\n # if self.action in('update','partial_update','destroy'):\n # from rest_framework.permissions import IsAuthenticated\n # return [IsAuthenticated(),IsFollowUpOwnerOrManager()]\n # return super().get_permissions()\n\n\n \n```\n\n\n\n**FollowUpSerializer：**\n\n```python\n# apps/follow_ups/serializers.py\nfrom rest_framework import serializers\n\nfrom .models import FollowUp\n\n\nclass FollowUpSerializer(serializers.ModelSerializer):\n \"\"\"跟进记录序列化器。\"\"\"\n\n customer_name = serializers.CharField(source='customer.name',read_only=True)\n user_name = serializers.CharField(source='user.username',read_only=True)\n\n class Meta:\n model = FollowUp\n fields = '__all__'\n read_only_fields =('user','created_at','updated_at')\n```\n\n\n\n### DefaultRouter 路由注册\n\n```python\n# apps/customers/urls.py\nfrom django.urls import path,include\nfrom rest_framework.routers import DefaultRouter\n\nfrom .views import CustomerViewSet\n\nrouter = DefaultRouter()\nrouter.register(r'customers',CustomerViewSet,basename='customer')\n\nurlpatterns = [\n path('',include(router.urls)),\n]\n```\n\n```python\n# apps/follow_ups/urls.py\nfrom django.urls import path,include\nfrom rest_framework.routers import DefaultRouter\n\nfrom .views import FollowUpViewSet\n\nrouter = DefaultRouter()\nrouter.register(r'follow-ups',FollowUpViewSet,basename='followup')\n\nurlpatterns = [\n path('',include(router.urls)),\n]\n```\n\n**根路由挂载：**\n\n```python\n# crm_project_root/urls.py\nfrom django.contrib import admin\nfrom django.urls import path,include\nfrom django.http import JsonResponse\nfrom django.db import connection\n\n# 检查接口健康情况\ndef health(request):\n \"\"\"简单健康检查接口，用于部署探活。\"\"\"\n return JsonResponse({'code':200,'msg':'success','data':{'status':'ok'}})\n\n\nurlpatterns = [\n path('admin/',admin.site.urls),\n path('health/',health),\n # 各业务应用自行定义子路由，统一以 /api/ 为前缀\n path('api/',include('apps.users.urls')),\n path('api/',include('apps.customers.urls')),\n path('api/',include('apps.follow_ups.urls')),\n path('api/',include('apps.comments.urls')),\n path('api/',include('apps.statistics.urls')),\n]\n```\n\n**Router 自动生成的路由一览：**\n\n| 方法 | URL | ViewSet Action | 说明 |\n|------|-----|----------------|------|\n| GET | `/api/customers/` | list | 客户列表（分页） |\n| POST | `/api/customers/` | create | 新建客户 |\n| GET | `/api/customers/{id}/` | retrieve | 客户详情 |\n| PUT | `/api/customers/{id}/` | update | 全量更新 |\n| PATCH | `/api/customers/{id}/` | partial_update | 部分更新 |\n| DELETE | `/api/customers/{id}/` | destroy | 删除客户 |\n| GET | `/api/follow-ups/` | list | 跟进列表（分页） |\n| POST | `/api/follow-ups/` | create | 新建跟进 |\n| GET | `/api/follow-ups/{id}/` | retrieve | 跟进详情 |\n| PUT | `/api/follow-ups/{id}/` | update | 全量更新 |\n| PATCH | `/api/follow-ups/{id}/` | partial_update | 部分更新 |\n| DELETE | `/api/follow-ups/{id}/` | destroy | 删除跟进 |\n\n\n\n### 统一异常处理\n\n```python\n# utils/exceptions.py\n\"\"\"\n统一异常处理：把 DRF 抛出的异常统一包装为{code,msg,data}结构。\n\"\"\"\nfrom rest_framework.views import exception_handler\nfrom rest_framework import status\n\n\ndef custom_exception_handler(exc,context):\n # 先调用 DRF 默认处理器，拿到标准响应\n response = exception_handler(exc,context)\n if response is not None:\n detail = response.data\n # 单条错误（如认证失败 detail:\"...\"）\n if isinstance(detail,dict)and 'detail' in detail:\n msg = str(detail['detail'])\n elif isinstance(detail,list):\n msg = '；'.join(str(d)for d in detail)\n else:\n # 字段校验错误（dict，键为字段名）\n msg = str(detail)\n response.data ={\n 'code':response.status_code,\n 'msg':msg,\n 'data':None,\n}\n return response\n```\n\n> 说明：当 DRF 抛出异常（如404 对象未找到、400 校验错误、401 未认证）时，此处理器会自动将响应包装为统一的 `{code,msg,data}` 结构，前端无需针对不同错误格式做特殊处理。\n\n\n\n### 自定义 Action 示例\n\n虽然项目中 CustomerViewSet 暂未使用自定义 action，以下是教学示例：\n\n```python\n# 教学示例：在 CustomerViewSet 中添加自定义 action\nfrom rest_framework.decorators import action\n\nclass CustomerViewSet(viewsets.ModelViewSet):\n # ... 原有代码 ...\n\n @action(detail=True,methods=['post'])\n def change_status(self,request,pk=None):\n \"\"\"修改单个客户状态。\n\n URL:POST /api/customers/{id}/change_status/\n Body:{\"status\":\"won\"}\n \"\"\"\n customer = self.get_object()\n new_status = request.data.get('status')\n if new_status not in dict(Customer.Status.choices):\n return fail(msg='无效的状态值',http_code=400)\n customer.status = new_status\n customer.save()\n return success(data=CustomerSerializer(customer).data,msg='状态更新成功')\n\n \n @action(detail=False,methods=['get'])\n def statistics(self,request):\n \"\"\"客户统计信息（列表级 action）。\n\n URL:GET /api/customers/statistics/\n \"\"\"\n qs = self.get_queryset()\n data ={\n 'total':qs.count(),\n 'lead':qs.filter(status='lead').count(),\n 'won':qs.filter(status='won').count(),\n 'lost':qs.filter(status='lost').count(),\n}\n return success(data=data)\n```\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n\n将 Day 4 的 Customer APIView 重构为 ModelViewSet + Router，为 FollowUp 创建完整 ViewSet，配置全局分页、筛选、搜索、排序。\n\n### 实现步骤\n\n1. 在 `settings.py` 中完善 `REST_FRAMEWORK` 配置，添加 `DEFAULT_FILTER_BACKENDS`（DjangoFilterBackend + SearchFilter + OrderingFilter）\n2. 注册 `django_filters` 到 `INSTALLED_APPS`\n3. 创建 `utils/pagination.py`，编写 `StandardResultsSetPagination` 自定义分页器\n4. 创建 `utils/exceptions.py`，编写 `custom_exception_handler` 统一异常处理\n5. 创建 `apps/customers/filters.py`，编写 `CustomerFilter`\n6. 将 `apps/customers/views.py` 从 APIView 重构为 `CustomerViewSet(ModelViewSet)`，实现 `get_queryset`（数据权限）、`perform_create`、`get_permissions`\n7. 创建 `apps/follow_ups/serializers.py` 和 `apps/follow_ups/views.py`（FollowUpViewSet）\n8. 将 `apps/customers/urls.py` 和 `apps/follow_ups/urls.py` 改为 DefaultRouter 方式\n9. 在 `crm_project/urls.py` 中挂载 follow_ups 子路由\n10. 启动服务器，使用 Postman 测试分页、筛选、搜索、排序功能\n\n**测试命令：**\n\n```bash\n# 启动开发服务器\npython manage.py runserver\n\n# 测试分页\n# GET http://127.0.0.1:8000/api/customers/?page=1&page_size=5\n\n# 测试筛选\n# GET http://127.0.0.1:8000/api/customers/?status=lead\n\n# 测试搜索\n# GET http://127.0.0.1:8000/api/customers/?search=张\n\n# 测试排序\n# GET http://127.0.0.1:8000/api/customers/?ordering=-created_at\n\n# 组合使用\n# GET http://127.0.0.1:8000/api/customers/?status=lead&search=张&page=1&page_size=5&ordering=-created_at\n```\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n\n让 AI 对比生成 APIView 与 ViewSet 两种写法，理解重构差异和代码精简的本质。\n\n### 场景\n\nDay 4 用 APIView 写了约 68 行代码实现 Customer 的 CRUD。现在要重构为 ModelViewSet，需要理解哪些代码被框架自动处理了，哪些需要保留。\n\n### Prompt 示例\n\n```\n你是一位 Django REST Framework 开发工程师。\n\n我有一段基于 APIView 的客户 CRUD 代码，现在需要重构为 ModelViewSet。\n\n请完成以下任务：\n1. 用 ModelViewSet 重写这段代码，保持功能不变\n2. 逐一列出 APIView 中哪些代码被 ModelViewSet 自动替代了\n3. 列出哪些逻辑需要手动保留（如数据权限、自动设置 owner、自定义响应）\n\n原有 APIView 代码：\n[粘贴 Day 4 的 CustomerListCreateView 和 CustomerDetailView 代码]\n\n模型定义：\n[粘贴 Customer 模型代码]\n\n要求：\n1. 中文注释\n2. 保持 get_queryset 数据权限逻辑（sales 只看自己的客户）\n3. 保持 perform_create 自动设置 owner\n4. destroy 返回统一 success 响应\n5. 输出重构前后代码行数对比\n```\n\n\n\n### 实践要点\n\n- 让 AI 输出\"哪些代码被自动替代\"的对比分析，比单纯生成新代码更有学习价值\n- 重构后必须验证：数据权限（`get_queryset`）、自动写入（`perform_create`）、权限控制（`get_permissions`）是否保留\n- AI 可能会遗漏 `destroy` 方法的覆写（统一响应格式），需检查返回格式是否一致\n- 可以让 AI 进一步解释 `get_serializer_class`、`get_permissions` 等方法的动态分发机制\n\n\n\n## 课堂练习\n\n**练习 1**（基础）：为 `FollowUpViewSet` 添加一个自定义 action `by_customer`，接收客户 ID 参数，返回该客户的所有跟进记录。\n\n```\nURL:GET /api/follow-ups/by_customer/?customer_id=1\n```\n\n思路提示：使用 `@action(detail=False,methods=['get'])`，在方法内通过\n\n1. `request.query_params.get('customer_id')` 获取参数`customer_id`，\n2. 用 `self.get_queryset().filter(customer_id=customer_id)` 过滤得到 `queryset`。\n3. 用 ` self.paginate_queryset(queryset)` 获取分页之后的数据`page`\n4. 用 `self.get_serializer(page,many=True)` 得到序列化对象`serializer`\n5. 用 `success(data=serializer.data)` 返回统一格式的结果数据\n\n\n\n**练习 2**（中等）：为 `CustomerFilter` 增加按创建日期范围筛选的功能，支持 `?created_at_after=2025-01-01&created_at_before=2025-12-31`。\n\n思路提示：使用 `django_filters.DateTimeFilter` 的 `lookup_expr='gte'` 和 `lookup_expr='lte'`\n\n**例子**：\n\n```py\n# 大于等于起始日期 created_at_after=2025-01-01\ncreated_at_after = django_filters.DateTimeFilter(\n field_name=\"created_at\",\n lookup_expr=\"gte\"\n)\n```\n\n\n\n## 课后作业\n\n### 必做\n\n1. 完成 Customer 从 APIView 到 ModelViewSet 的重构，确保所有接口正常工作\n2. 完成 FollowUpViewSet 的全部代码并通过 Apifox 测试\n3. 测试分页、筛选、搜索、排序功能，分别截图保存\n4. 编写 `custom_exception_handler`，测试 404 和 400 错误返回统一格式\n\n### 选做（进阶）\n\n1. 为 CustomerViewSet 添加 `change_status` 自定义 action，实现快速修改客户状态\n2. 为 FollowUp 创建 `FollowUpFilter`，支持按 `contact_type` 和 `result` 筛选\n3. 用 AI 生成 Comment 的 ViewSet，检查是否正确处理了层级评论的 parent 字段\n\n### 提交方式\n\n- 提交 views.py、urls.py、filters.py、pagination.py、exceptions.py 代码文件\n- 附上 Apifox 测试截图（分页、筛选、搜索、排序各一张）\n- 附上重构前后代码行数对比说明\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[DRF 进阶] --> B[ViewSet & ModelViewSet]\n A --> C[Router]\n A --> D[分页]\n A --> E[筛选/搜索/排序]\n A --> F[自定义 Action]\n\n B --> B1[ModelViewSet 7个action]\n B --> B2[get_queryset 数据权限]\n B --> B3[perform_create]\n B --> B4[get_permissions]\n\n C --> C1[DefaultRouter]\n C --> C2[router.register]\n C --> C3[自动生成路由]\n\n D --> D1[PageNumberPagination]\n D --> D2[StandardResultsSetPagination]\n D --> D3[统一分页响应]\n\n E --> E1[DjangoFilterBackend]\n E --> E2[SearchFilter]\n E --> E3[OrderingFilter]\n E --> E4[FilterSet]\n\n F --> F1[@action 装饰器]\n F --> F2[detail=True/False]\n F --> F3[methods 参数]\n```\n\n## 下节预告\n\n- 下一节主题：用户认证与权限\n- 需要预习的内容：\n - 复习 HTTP 无状态特性，思考为什么需要认证机制\n - 了解 Cookie/Session 与 Token 的区别\n - 思考：什么是 JWT？为什么前后端分离项目通常选择 JWT 而非 Session？\n - 了解 RBAC（基于角色的访问控制）的概念\n - 预习 DRF 的 Authentication 和 Permission 机制\n","order":5},{"chapterName":"06-用户认证与权限","content":"\n\n# Day 6 - 用户认证与权限\n\n## 学习目标\n\n1. **知识目标**：理解认证（Authentication）与授权（Authorization）的区别，掌握 JWT 原理（access token / refresh token 机制），理解 DRF 权限类的执行流程\n2. **技能目标**：能配置 SimpleJWT 实现登录/注册/刷新/注销接口，编写自定义 Permission 类实现对象级权限，实现基于角色的数据隔离\n3. **项目目标**：配置 JWT 认证体系，实现登录/注册/刷新 Token/注销/用户信息接口，创建 IsOwnerOrManager 权限类保护客户 API（仅负责人可编辑）\n4. **AI 实践目标**：用 AI 生成权限类模板，学习如何通过 Prompt 描述业务权限规则\n\n## 知识点讲解\n\n### 认证与授权的区别\n\n认证和授权是安全体系的两个阶段，容易混淆但职责完全不同：\n\n```\n请求到达 → ① 认证（Authentication）：你是谁？→ ② 授权（Authorization）：你能做什么？\n```\n\n| 维度 | 认证（Authentication） | 授权（Authorization） |\n|------|----------------------|---------------------|\n| 核心问题 | \"你是谁？\" | \"你能做什么？\" |\n| 执行顺序 | 先执行 | 后执行（认证通过后） |\n| DRF 组件 | AuthenticationClasses | PermissionClasses |\n| 常见方式 | Session、JWT、OAuth | RBAC、ABAC、ACL |\n| 失败状态码 | 401 Unauthorized | 403 Forbidden |\n| 本项目实现 | JWTAuthentication | IsAuthenticated + IsOwnerOrManager |\n\n**DRF 请求处理中的执行顺序：**\n\n```mermaid\nflowchart LR\n A[HTTP请求到达DRF视图] --> B[① 认证 Authentication:<br/>你是谁？<br/><br/>解析token/session，识别用户]\n B -->|认证成功：<br/><br/>得到request.user| C[② 授权 Authorization:<br/>你能做什么？<br/><br/>执行权限类判断：<br/>是否允许操作]\n B -->|认证失败：<br/><br/>匿名用户| E[返回401<br/>Unauthorized<br/><br/>未认证]\n\n C -->|授权通过| H[③ Throttling 限流：<br/>控制访问频率<br/><br/>检查访问频次]\n C -->|权限不足| F[返回403<br/>Forbidden<br/><br/>禁止访问]\n\n H -->|未超限| D[执行视图业务逻辑<br/>list/create/retrieve...]\n H -->|访问频率超限| I[返回429<br/> Too Many Requests<br/><br/>请求过多]\n\n D --> G[序列化处理 <br/>→ 返回响应]\n```\n\n\n\n### JWT 原理\n\nJWT（JSON Web Token）是一种无状态的认证方案。服务器不保存 Token，而是通过签名验证 Token 的有效性。\n\n\n\n**JWT 结构（三段式，用 . 分隔）：**\n\n```\nHeader.Payload.Signature\neyJhbGciOiJIUzI1NiJ9.eyJ1c2VyX2lkIjoxfQ.SflKxwRJSMeKKF2QT4f\n```\n\n| 部分 | 内容 | 说明 |\n|------|------|------|\n| Header | `{\"alg\":\"HS256\",\"typ\":\"JWT\"}` | 算法类型 |\n| Payload | `{\"user_id\":1,\"exp\":1721000000,...}` | 载荷数据（用户ID、过期时间） |\n| Signature | HMAC-SHA256 签名 | 防篡改签名 |\n\n\n\n**Access Token 与 Refresh Token 机制：**\n\n| Token 类型 | 有效期 | 用途 | 本项目配置 |\n| ------------- | ------------- | ------------------------- | ------------------------------ |\n| Access Token | 短（60 分钟） | 每个 API 请求携带 | `ACCESS_TOKEN_LIFETIME:60min` |\n| Refresh Token | 长（7 天） | 过期后换取新 Access Token | `REFRESH_TOKEN_LIFETIME:7d` |\n\n**执行流程**：\n\n```\n用户登录 → 服务器签发 access_token（短期）+ refresh_token（长期）\n前端请求 → Header 携带 access_token → 服务器验证\naccess_token 过期 → 前端用 refresh_token 换取新的 access_token\n用户注销 → refresh_token 加入黑名单 → 无法再换取新 access_token\n```\n\n**交互图**：\n\n```mermaid\nsequenceDiagram\n participant Client\n participant AuthServer as 认证服务器\n participant ResourceServer as 资源服务器\n\n Note over Client,AuthServer:1. 获取令牌（首次或刷新）\n Client->>AuthServer:用户名/密码 或 授权码\n AuthServer-->>Client:access_token(短期，如1小时)+ refresh_token(长期)\n\n Note over Client,ResourceServer:2. 使用 access_token 请求资源\n Client->>ResourceServer:请求资源（携带 access_token）\n ResourceServer->>ResourceServer:验证 access_token\n alt token有效\n ResourceServer-->>Client:返回资源数据\n else token过期或无效\n ResourceServer-->>Client:401 Unauthorized\n end\n\n Note over Client,AuthServer:3. 通过 refresh_token 续期\n Client->>AuthServer:请求刷新（携带 refresh_token）\n AuthServer->>AuthServer:验证 refresh_token（检查有效期、是否撤销等）\n alt refresh_token有效\n AuthServer-->>Client:新的 access_token（可选同时更新 refresh_token）\n else refresh_token无效/过期\n AuthServer-->>Client:401 或错误，需重新登录\n end\n\n Note over Client,ResourceServer:4. 使用新 access_token 重试请求\n Client->>ResourceServer:携带新 access_token 再次请求\n ResourceServer-->>Client:返回资源数据（成功）\n```\n\n\n\n**JWT vs Session 对比：**\n\n| 特性 | Session | JWT |\n|------|---------|-----|\n| 状态 | 有状态（服务器存储） | 无状态（服务器不存储） |\n| 扩展性 | 需共享 Session（Redis） | 天然支持分布式 |\n| 前后端分离 | 不便（依赖 Cookie） | 友好（Header 传 Token） |\n| 注销 | 删除 Session 即可 | 需黑名单机制 |\n| 适用场景 | 传统 Web 应用 | 前后端分离 API（推荐） |\n\n### SimpleJWT 配置\n\nSimpleJWT 是 Django REST Framework 最流行的 JWT 库，提供开箱即用的 Token 签发与验证。\n\n**安装：**\n\n```bash\npip install djangorestframework-simplejwt==5.3.1\n# 使用清华源安装\npip install djangorestframework-simplejwt==5.3.1 -i https://pypi.tuna.tsinghua.edu.cn/simple\n```\n\n**需注册的应用：**\n\n```python\nINSTALLED_APPS = [\n ...\n 'rest_framework',\n 'rest_framework_simplejwt',# JWT 核心\n 'rest_framework_simplejwt.token_blacklist',# Token 黑名单（注销功能）\n ...\n]\n```\n\n### 自定义 Permission 类\n\nDRF 的权限类继承 `BasePermission`，通过实现两个方法控制访问：\n\n| 方法 | 执行时机 | 返回 True 的含义 | 典型用途 |\n|------|----------|-----------------|----------|\n| `has_permission()` | 视图级，每次请求 | 允许访问该接口 | 角色判断（如仅 admin 可访问） |\n| `has_object_permission()` | 对象级，操作单个对象时 | 允许操作该对象 | 数据归属判断（如仅 owner 可编辑） |\n\n**权限检查流程：**\n\n```\n请求到达\n → has_permission()：是否有权访问此 ViewSet/APIView？\n → False → 403 Forbidden\n → True → 继续\n → 获取对象（get_object()）\n → has_object_permission()：是否有权操作此对象？\n → False → 403 Forbidden\n → True → 执行操作\n```\n\n> 注意：`has_object_permission` 仅在 `retrieve`/`update`/`partial_update`/`destroy` 等需要获取单个对象的 action 中触发，`list`/`create` 不会触发。\n\n### 角色权限控制\n\n本项目定义三种角色，实现数据隔离和操作权限分级：\n\n| 角色 | 数据范围 | 可查看 | 可创建 | 可编辑 | 可删除 |\n|------|----------|--------|--------|--------|--------|\n| admin | 全部客户 | 是 | 是 | 是 | 是 |\n| manager | 全部客户 | 是 | 是 | 是 | 否 |\n| sales | 仅自己负责的客户 | 是 | 是 | 仅自己的 | 否 |\n\n**权限矩阵（完整）：**\n\n| 操作 | admin | manager | sales |\n|------|-------|---------|-------|\n| GET /customers/（列表） | 全部 | 全部 | 仅自己的 |\n| GET /customers/{id}/（详情） | 全部 | 全部 | 仅自己的 |\n| POST /customers/（创建） | 是 | 是 | 是（自动设为 owner） |\n| PUT /customers/{id}/（更新） | 全部 | 全部 | 仅自己的 |\n| PATCH /customers/{id}/（部分更新） | 全部 | 全部 | 仅自己的 |\n| DELETE /customers/{id}/（删除） | 全部 | 否 | 否 |\n\n\n\n## 核心代码示例\n\n### settings.py 中的 SIMPLE_JWT 与认证配置\n\n```python\n# crm_project/settings/base.py\n\nINSTALLED_APPS = [\n 'django.contrib.admin',\n 'django.contrib.auth',\n 'django.contrib.contenttypes',\n 'django.contrib.sessions',\n 'django.contrib.messages',\n 'django.contrib.staticfiles',\n # 第三方\n 'rest_framework',\n 'rest_framework_simplejwt',# JWT 认证核心\n 'rest_framework_simplejwt.token_blacklist',# Token 黑名单（支持注销）\n #'corsheaders',\n 'django_filters',\n #'django_redis',\n # 本项目应用\n 'apps.users',\n 'apps.customers',\n 'apps.follow_ups',\n 'apps.comments',\n 'apps.statistics',\n]\n\n# DRF 全局配置\nREST_FRAMEWORK ={\n # 默认采用 JWT 认证\n 'DEFAULT_AUTHENTICATION_CLASSES':(\n 'rest_framework_simplejwt.authentication.JWTAuthentication',\n),\n # 默认所有接口需登录访问，公开接口在视图内单独放行\n 'DEFAULT_PERMISSION_CLASSES':(\n 'rest_framework.permissions.IsAuthenticated',\n),\n # 统一分页\n 'DEFAULT_PAGINATION_CLASS':'utils.pagination.StandardResultsSetPagination',\n 'PAGE_SIZE':10,\n # 筛选与排序\n 'DEFAULT_FILTER_BACKENDS':(\n 'django_filters.rest_framework.DjangoFilterBackend',\n 'rest_framework.filters.SearchFilter',\n 'rest_framework.filters.OrderingFilter',\n),\n # 统一异常处理\n 'EXCEPTION_HANDLER':'utils.exceptions.custom_exception_handler',\n}\n\n# SimpleJWT 配置：access 有效期较短，refresh 较长\nfrom datetime import timedelta\nSIMPLE_JWT ={\n 'ACCESS_TOKEN_LIFETIME':timedelta(minutes=60),\t# access token 有效期 60 分钟\n 'REFRESH_TOKEN_LIFETIME':timedelta(days=7),# refresh token 有效期 7 天\n 'ROTATE_REFRESH_TOKENS':False,# 刷新时不轮换 refresh token\n 'AUTH_HEADER_TYPES':('Bearer',),# Authorization:Bearer <token>\n}\n```\n\n**配置项说明：**\n\n| 配置项 | 作用 | 本项目值 |\n|--------|------|----------|\n| `DEFAULT_AUTHENTICATION_CLASSES` | 全局认证方式 | JWTAuthentication |\n| `DEFAULT_PERMISSION_CLASSES` | 全局默认权限 | IsAuthenticated（默认需登录） |\n| `ACCESS_TOKEN_LIFETIME` | Access Token 有效期 | 60 分钟 |\n| `REFRESH_TOKEN_LIFETIME` | Refresh Token 有效期 | 7 天 |\n| `ROTATE_REFRESH_TOKENS` | 刷新时是否签发新 refresh | False（保留原 refresh） |\n| `AUTH_HEADER_TYPES` | 认证头前缀 | `Bearer` |\n\n> 重要：`rest_framework_simplejwt.token_blacklist` 必须注册才能实现注销功能（将 refresh token 加入黑名单）。\n> 注册后需执行 `python manage.py migrate` 创建黑名单相关的数据库表。\n\n\n\n### UserRegisterSerializer（密码哈希处理）\n\n```python\n# apps/users/serializers.py\nfrom rest_framework import serializers\n\nfrom .models import User\n\n\nclass UserSerializer(serializers.ModelSerializer):\n \"\"\"用户信息序列化（读取）。\"\"\"\n\n class Meta:\n model = User\n fields =('id','username','phone','avatar','role','department',\n 'email','date_joined')\n read_only_fields =('id','username','date_joined')\n\n\nclass UserRegisterSerializer(serializers.ModelSerializer):\n \"\"\"注册序列化器：对密码做哈希处理。\"\"\"\n\n password = serializers.CharField(write_only=True,required=True,min_length=8)\n\n class Meta:\n model = User\n fields =('id','username','password','phone','role','department','email')\n\n def create(self,validated_data):\n # create_user 会自动对密码哈希并规范化用户名\n return User.objects.create_user(**validated_data)\n```\n\n**关键点解析：**\n\n| 代码 | 作用 |\n|------|------|\n| `password = serializers.CharField(write_only=True,...)` | 密码仅写入（不返回给前端），最小长度 8 位 |\n| `User.objects.create_user(**validated_data)` | 使用 `create_user` 而非 `create`，自动对密码做哈希处理 |\n| `write_only=True` | 确保密码永远不会出现在响应数据中 |\n\n> 为什么不能用 `User.objects.create()`？因为 `create` 不会对密码哈希，存入数据库的是明文密码，无法用于登录验证。`create_user` 内部调用 `set_password()` 对密码进行哈希。\n\n### RegisterView、UserInfoView、LogoutView\n\n```python\n# apps/users/views.py\nfrom rest_framework.views import APIView\nfrom rest_framework.permissions import AllowAny\nfrom rest_framework_simplejwt.tokens import RefreshToken\nfrom rest_framework_simplejwt.exceptions import TokenError\n\nfrom utils.response import success,fail\nfrom .serializers import UserSerializer,UserRegisterSerializer\n\n\nclass RegisterView(APIView):\n \"\"\"用户注册。\"\"\"\n\n permission_classes = [AllowAny] # 注册接口公开，无需登录\n\n def post(self,request):\n serializer = UserRegisterSerializer(data=request.data)\n serializer.is_valid(raise_exception=True)\n serializer.save()\n return success(data=UserSerializer(serializer.instance).data,msg='注册成功')\n\n\nclass UserInfoView(APIView):\n \"\"\"获取当前登录用户信息。\"\"\"\n\n def get(self,request):\n return success(data=UserSerializer(request.user).data)\n\n\nclass LogoutView(APIView):\n \"\"\"注销：将 refresh token 加入黑名单，使其失效。\"\"\"\n\n def post(self,request):\n refresh_token = request.data.get('refresh')\n if not refresh_token:\n return fail(msg='缺少 refresh token',http_code=400)\n try:\n RefreshToken(refresh_token).blacklist()\n except TokenError:\n return fail(msg='无效的 refresh token',http_code=400)\n return success(msg='注销成功')\n```\n\n**三个视图说明：**\n\n| 视图 | 权限 | 方法 | 功能 |\n|------|------|------|------|\n| `RegisterView` | `AllowAny`（公开） | POST | 用户注册，返回用户信息 |\n| `UserInfoView` | `IsAuthenticated`（默认） | GET | 获取当前登录用户的详细信息 |\n| `LogoutView` | `IsAuthenticated`（默认） | POST | 将 refresh token 加入黑名单 |\n\n**注销流程原理：**\n\n```\n前端 → POST /api/logout/，Body:{\"refresh\":\"xxx\"}\n后端 → RefreshToken(refresh).blacklist()→ 将 token 加入数据库黑名单表\n结果 → 该 refresh token 无法再换取新 access token，实现\"注销\"\n```\n\n### users/urls.py 路由配置\n\n```python\n# apps/users/urls.py\nfrom django.urls import path\nfrom rest_framework_simplejwt.views import TokenObtainPairView,TokenRefreshView\n\nfrom .views import RegisterView,UserInfoView,LogoutView\n\nurlpatterns = [\n path('login/',TokenObtainPairView.as_view(),name='login'),\n path('token/refresh/',TokenRefreshView.as_view(),name='token_refresh'),\n path('register/',RegisterView.as_view(),name='register'),\n path('logout/',LogoutView.as_view(),name='logout'),\n path('user/info/',UserInfoView.as_view(),name='user_info'),\n]\n```\n\n**接口端点一览：**\n\n| 方法 | URL | 视图 | 权限 | 功能 |\n|------|-----|------|------|------|\n| POST | `/api/login/` | TokenObtainPairView | 公开 | 登录，返回 access + refresh token |\n| POST | `/api/token/refresh/` | TokenRefreshView | 公开 | 用 refresh token 换取新 access token |\n| POST | `/api/register/` | RegisterView | 公开 | 用户注册 |\n| POST | `/api/logout/` | LogoutView | 需登录 | 注销（refresh token 加黑名单） |\n| GET | `/api/user/info/` | UserInfoView | 需登录 | 获取当前用户信息 |\n\n> `TokenObtainPairView` 和 `TokenRefreshView` 是 SimpleJWT 内置视图，无需自己编写。`TokenObtainPairView` 验证用户名密码后签发 access + refresh 两个 token。\n\n### 登录响应示例\n\n**请求：**\n\n```\nPOST /api/login/\nContent-Type:application/json\n\n{\n \"username\":\"admin\",\n \"password\":\"admin123456\"\n}\n```\n\n**响应（SimpleJWT 默认格式）：**\n\n```json\n{\n \"refresh\":\"eyJhbGciOiJIUzI1NiJ9...\",\n \"access\":\"eyJhbGciOiJIUzI1NiJ9...\"\n}\n```\n\n> 注意：SimpleJWT 的 `TokenObtainPairView` 默认返回 `{access,refresh}` 格式，没有包装为统一的 `{code,msg,data}`。如需统一格式，可自定义序列化器或视图。\n\n### 自定义 Permission 类\n\n```python\n# apps/users/permissions.py\nfrom rest_framework import permissions\n\n\nclass IsOwnerOrManager(permissions.BasePermission):\n \"\"\"\n 客户对象级权限：\n - admin：全部可操作（含删除）\n - manager：可读写，不可删除\n - sales：仅可操作自己负责的客户，且不可删除\n \"\"\"\n\n def has_object_permission(self,request,view,obj):\n user = request.user\n # 安全方法（GET/HEAD/OPTIONS）允许所有已认证用户查看\n if request.method in permissions.SAFE_METHODS:\n return True\n # DELETE 操作仅 admin 可执行\n if request.method == 'DELETE':\n return user.role == 'admin'\n # PUT/PATCH：owner 本人 或 admin/manager 可操作\n return obj.owner == user or user.role in('admin','manager')\n\n\nclass IsFollowUpOwnerOrManager(permissions.BasePermission):\n \"\"\"跟进记录对象级权限：以所属客户的负责人判定。\"\"\"\n def has_object_permission(self,request,view,obj):\n user = request.user\n if request.method in permissions.SAFE_METHODS:\n return True\n if request.method == 'DELETE':\n return user.role == 'admin'\n # obj.customer.owner 是该跟进记录所属客户的负责人\n return obj.customer.owner == user or user.role in('admin','manager')\n```\n\n**代码逐行解析：**\n\n| 代码 | 作用 |\n|------|------|\n| `permissions.SAFE_METHODS` | 等于 `('GET','HEAD','OPTIONS')`，即不修改数据的\"安全\"方法 |\n| `request.method in permissions.SAFE_METHODS` | 查看操作放行，所有已认证用户可查看 |\n| `request.method == 'DELETE'` | 删除操作仅 admin |\n| `obj.owner == user` | 对象的负责人是当前用户（sales 编辑自己的客户） |\n| `user.role in('admin','manager')` | 管理员和经理可编辑所有客户 |\n| `obj.customer.owner == user` | FollowUp 通过 `customer.owner` 间接判断归属 |\n\n**SAFE_METHODS 说明：**\n\n```python\n# DRF 源码定义\nSAFE_METHODS =('GET','HEAD','OPTIONS')\n```\n\n这些方法不修改服务器数据，因此对已认证用户全部放行，实现\"所有人可看，受限人可改\"的效果。\n\n### CustomerViewSet 中的 get_permissions 应用\n\n```python\n# apps/customers/views.py\nfrom rest_framework import viewsets\nfrom rest_framework.permissions import IsAuthenticated\n\nfrom utils.response import success\nfrom .models import Customer\nfrom .serializers import CustomerSerializer\nfrom .filters import CustomerFilter\nfrom apps.users.permissions import IsOwnerOrManager\n\n\nclass CustomerViewSet(viewsets.ModelViewSet):\n \"\"\"客户增删改查。\"\"\"\n\n serializer_class = CustomerSerializer\n filterset_class = CustomerFilter\n search_fields = ['name','company','phone']\n ordering_fields = ['created_at','updated_at']\n\n def get_queryset(self):\n # 数据权限：sales 仅能看自己负责的客户\n user = self.request.user\n qs = Customer.objects.select_related('owner')\n if user.role == 'sales':\n qs = qs.filter(owner=user)\n return qs\n\n def perform_create(self,serializer):\n # 新建客户时负责人默认为当前登录用户\n serializer.save(owner=self.request.user)\n\n def get_permissions(self):\n # 修改与删除需通过对象级权限校验\n if self.action in('update','partial_update','destroy'):\n return [IsAuthenticated(),IsOwnerOrManager()]\n return super().get_permissions()\n\n def destroy(self,request,*args,**kwargs):\n instance = self.get_object()\n self.perform_destroy(instance)\n return success(msg='删除成功')\n```\n\n**get_permissions 方法详解：**\n\n```python\ndef get_permissions(self):\n if self.action in('update','partial_update','destroy'):\n return [IsAuthenticated(),IsOwnerOrManager()]\n return super().get_permissions()\n```\n\n| 代码 | 作用 |\n|------|------|\n| `self.action` | 当前请求对应的 action 名称（如 `'list'`、`'create'`、`'update'`） |\n| `self.action in('update','partial_update','destroy')` | 仅修改和删除操作需要对象级权限 |\n| `[IsAuthenticated(),IsOwnerOrManager()]` | 先验证已登录，再验证对象级权限 |\n| `super().get_permissions()` | 其他 action（list/create/retrieve）使用默认权限（IsAuthenticated） |\n\n**两层权限的协作关系：**\n\n```\n修改/删除请求 → views.py\n → get_permissions()返回 [IsAuthenticated(),IsOwnerOrManager()]\n → IsAuthenticated.has_permission()：是否已登录？\n → IsOwnerOrManager.has_permission()：是否已登录？（继承默认，返回 True）\n → get_object()获取对象\n → IsOwnerOrManager.has_object_permission()：是否有权操作此对象？\n → GET/HEAD/OPTIONS → True\n → DELETE → 仅 admin\n → PUT/PATCH → owner 本人 或 admin/manager\n```\n\n### utils/permissions.py 通用权限类\n\n项目中还有一组通用的视图级权限类，用于控制接口级别的访问：\n\n```python\n# utils/permissions.py\n\"\"\"\n通用权限类。\n业务相关的权限（如 IsOwnerOrManager）定义在 apps/users/permissions.py。\n\"\"\"\nfrom rest_framework import permissions\n\n\nclass IsAdminUser(permissions.BasePermission):\n \"\"\"仅管理员（role == 'admin'）可访问。\"\"\"\n\n def has_permission(self,request,view):\n user = request.user\n return bool(user and user.is_authenticated and user.role == 'admin')\n\n\nclass IsAdminOrManager(permissions.BasePermission):\n \"\"\"管理员或经理可访问。\"\"\"\n\n def has_permission(self,request,view):\n user = request.user\n return bool(\n user and user.is_authenticated and user.role in('admin','manager')\n)\n```\n\n**通用权限类 vs 业务权限类：**\n\n| 权限类 | 位置 | 检查级别 | 用途 |\n|--------|------|----------|------|\n| `IsAdminUser` | `utils/permissions.py` | 视图级 | 限制整个接口仅 admin 访问 |\n| `IsAdminOrManager` | `utils/permissions.py` | 视图级 | 限制接口仅 admin/manager 访问 |\n| `IsOwnerOrManager` | `apps/users/permissions.py` | 对象级 | 限制单个对象的修改和删除权限 |\n| `IsFollowUpOwnerOrManager` | `apps/users/permissions.py` | 对象级 | 跟进记录的对象级权限 |\n\n### Apifox 带 JWT 认证测试\n\n**步骤一：注册用户**\n\n```\nPOST http://127.0.0.1:8000/api/register/\nContent-Type:application/json\n\n{\n \"username\":\"sales01\",\n \"password\":\"12345678\",\n \"phone\":\"13800138001\",\n \"role\":\"sales\",\n \"department\":\"销售一部\"\n}\n```\n\n预期返回：\n\n```json\n{\n \"code\":200,\n \"msg\":\"注册成功\",\n \"data\":{\n \"id\":2,\n \"username\":\"sales01\",\n \"phone\":\"13800138001\",\n \"avatar\":null,\n \"role\":\"sales\",\n \"department\":\"销售一部\",\n \"email\":\"\",\n \"date_joined\":\"2025-07-15T10:00:00+08:00\"\n}\n}\n```\n\n\n\n**步骤二：登录获取 Token**\n\n```\nPOST http://127.0.0.1:8000/api/login/\nContent-Type:application/json\n\n{\n \"username\":\"sales01\",\n \"password\":\"12345678\"\n}\n```\n\n预期返回：\n\n```json\n{\n \"refresh\":\"eyJhbGciOiJIUzI1NiJ9...\",\n \"access\":\"eyJhbGciOiJIUzI1NiJ9...\"\n}\n```\n\n**步骤三：在 Apifox 中配置认证**\n\n1. 复制上一步返回的 `access` 值\n2. 在 Apifox 的 HTTP接口 中点击 \"Auth\" 标签\n3. Type 选择 \"Bearer Token\"\n4. Token 粘贴 access 值\n5. 此后该 Request 的所有请求都会自动携带 `Authorization:Bearer <token>` 头\n\n\n\n**步骤四：访问需要认证的接口**\n\n```\nGET http://127.0.0.1:8000/api/user/info/\nAuthorization:Bearer eyJhbGciOiJIUzI1NiJ9...\n```\n\n预期返回：\n\n```json\n{\n \"code\":200,\n \"msg\":\"success\",\n \"data\":{\n \"id\":2,\n \"username\":\"sales01\",\n \"phone\":\"13800138001\",\n \"role\":\"sales\",\n ...\n}\n}\n```\n\n\n\n**步骤五：测试权限控制**\n\n1. 用 sales01 用户创建一个客户（owner 自动设为 sales01）\n2. 用 admin 用户登录，尝试修改该客户 → 应成功（admin 有全部权限）\n3. 用另一个 sales 用户登录，尝试修改该客户 → 应返回 403（非 owner）\n4. 用 manager 用户登录，尝试修改该客户 → 应成功（manager 可编辑全部）\n5. 用 manager 用户登录，尝试删除该客户 → 应返回 403（manager 不可删除）\n6. 用 admin 用户登录，尝试删除该客户 → 应成功（admin 可删除）\n\n\n\n**步骤六：刷新 Token**\n\n```\nPOST http://127.0.0.1:8000/api/token/refresh/\nContent-Type:application/json\n\n{\n \"refresh\":\"eyJhbGciOiJIUzI1NiJ9...\"\n}\n```\n\n预期返回新的 access token：\n\n```json\n{\n \"access\":\"eyJhbGciOiJIUzI1NiJ9...(新的 access token)\"\n}\n```\n\n**步骤七：注销**\n\n```\nPOST http://127.0.0.1:8000/api/logout/\nAuthorization:Bearer eyJhbGciOiJIUzI1NiJ9...(当前 access token)\nContent-Type:application/json\n\n{\n \"refresh\":\"eyJhbGciOiJIUzI1NiJ9...(refresh token)\"\n}\n```\n\n预期返回：\n\n```json\n{\n \"code\":200,\n \"msg\":\"注销成功\",\n \"data\":null\n}\n```\n\n注销后，该 refresh token 无法再换取新 access token。\n\n## CRM 项目实战\n\n### 本节项目目标\n\n配置完整的 JWT 认证体系，实现登录/注册/刷新/注销/用户信息接口，创建权限类保护客户和跟进记录 API，实现基于角色的数据隔离。\n\n### 实现步骤\n\n1. 安装 `djangorestframework-simplejwt` 并注册 `rest_framework_simplejwt` 和 `rest_framework_simplejwt.token_blacklist` 到 `INSTALLED_APPS`\n2. 在 `settings.py` 中配置 `DEFAULT_AUTHENTICATION_CLASSES`（JWTAuthentication）和 `SIMPLE_JWT` 参数\n3. 执行 `python manage.py migrate` 创建 token 黑名单相关数据库表\n4. 编写 `UserRegisterSerializer`，使用 `create_user` 实现密码哈希\n5. 编写 `RegisterView`、`UserInfoView`、`LogoutView`\n6. 在 `users/urls.py` 中配置 5 个路由（login/token_refresh/register/logout/user_info）\n7. 编写 `IsOwnerOrManager` 和 `IsFollowUpOwnerOrManager` 权限类\n8. 在 `CustomerViewSet` 和 `FollowUpViewSet` 中配置 `get_permissions` 方法\n9. 在 Postman 中测试完整的认证流程（注册→登录→带 Token 访问→权限控制→刷新→注销）\n\n```bash\n# 创建 token 黑名单数据库表\npython manage.py migrate\n\n# 创建测试用户（也可通过 /api/register/ 接口注册）\npython manage.py createsuperuser\n\n# 启动开发服务器\npython manage.py runserver\n```\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n\n用 AI 生成权限类模板，学习如何通过 Prompt 描述业务权限规则。\n\n### 场景\n\n权限类的逻辑高度模式化，但业务规则因项目而异。通过编写清晰的 Prompt，让 AI 理解业务权限规则并生成准确的权限类代码。\n\n### Prompt 示例\n\n```\n你是一位 Django REST Framework 开发工程师。\n\n请根据以下业务权限规则，编写一个自定义 Permission 类：\n\n规则描述：\n1. 模型名称：Customer，有 owner 字段（外键关联 User）\n2. 用户角色：admin、manager、sales\n3. 查看权限：所有已认证用户均可查看\n4. 编辑权限：admin 和 manager 可编辑所有客户，sales 仅能编辑自己负责的客户\n5. 删除权限：仅 admin 可删除\n6. 权限类名：IsOwnerOrManager\n\n技术要求：\n1. 继承 permissions.BasePermission\n2. 实现 has_object_permission 方法\n3. 使用 permissions.SAFE_METHODS 判断安全方法\n4. 中文注释和 docstring\n\n请先输出权限逻辑的伪代码，再输出完整实现。\n```\n\n### 实践要点\n\n- Prompt 中用\"规则描述\"形式列出业务权限，比直接说\"写一个权限类\"效果更好\n- 让 AI \"先输出伪代码再实现\"，便于检查逻辑是否正确\n- 明确指出模型名称、字段名、角色枚举值，AI 才能正确引用\n- 生成后需验证：SAFE_METHODS 的判断是否正确、角色值是否与模型定义一致、has_object_permission 的参数是否正确\n- 可以让 AI 额外生成权限矩阵表格，帮助验证逻辑完整性\n\n**进阶 Prompt：让 AI 生成权限矩阵**\n\n```\n请根据以下权限类代码，生成一个权限矩阵表格，\n列出 admin/manager/sales 三种角色对 Customer 的 GET/POST/PUT/DELETE 操作权限。\n\n权限类代码：\n[粘贴 IsOwnerOrManager 代码]\n\n输出格式：Markdown 表格，标注\"是/否\"及条件说明。\n```\n\n## 课堂练习\n\n**练习 1**（基础）：在 `utils/permissions.py` 中编写 `IsManagerOnly` 权限类，仅允许 `role == 'manager'` 的用户访问。提示：参照 `IsAdminUser` 的写法。\n\n**练习 2**（中等）：为 Comment 模型编写 `IsCommentOwnerOrAdmin` 权限类。Comment 模型有 `user` 字段（评论作者）和 `is_active` 字段。规则：所有已认证用户可查看；编辑和删除仅限评论作者本人或 admin；manager 无编辑权限。\n\n思路提示：\n- `has_object_permission` 中判断 `obj.user == request.user`\n- DELETE 操作限制为 `request.user.role == 'admin'`\n- 编辑操作限制为 `obj.user == request.user`\n\n## 课后作业\n\n### 必做\n\n1. 完成 JWT 认证配置，通过 Postman 测试完整的认证流程（注册→登录→带 Token 访问→刷新→注销）\n2. 确认 `IsOwnerOrManager` 和 `IsFollowUpOwnerOrManager` 权限类正常工作，用不同角色用户测试权限控制\n3. 绘制完整的权限矩阵表格（admin/manager/sales 对 Customer 和 FollowUp 的所有操作权限）\n4. 测试注销后 refresh token 是否真的失效（尝试用已注销的 refresh token 刷新，应返回错误）\n\n\n\n### 选做（进阶）\n\n1. 自定义 `TokenObtainPairView`，使登录接口返回统一格式 `{code,msg,data:{access,refresh,user_info}}`\n2. 为 Comment 模型创建完整的 ViewSet 和权限类\n3. 用 AI 生成 RBAC 权限系统的设计文档，对比当前硬编码角色判断的优缺点\n\n\n\n### 提交方式\n\n- 提交 settings.py（SIMPLE_JWT 配置）、serializers.py、views.py、urls.py、permissions.py 代码文件\n- 附上 Postman 测试截图（登录、带 Token 访问、权限拒绝 403、刷新 Token、注销）\n- 附上权限矩阵表格\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[用户认证与权限] --> B[认证 Authentication]\n A --> C[授权 Authorization]\n A --> D[JWT 机制]\n A --> E[自定义 Permission]\n A --> F[角色权限控制]\n\n B --> B1[JWTAuthentication]\n B --> B2[DEFAULT_AUTHENTICATION_CLASSES]\n\n C --> C1[has_permission 视图级]\n C --> C2[has_object_permission 对象级]\n C --> C3[IsAuthenticated]\n\n D --> D1[Access Token 60min]\n D --> D2[Refresh Token 7d]\n D --> D3[Token 黑名单注销]\n D --> D4[SimpleJWT 配置]\n\n E --> E1[IsOwnerOrManager]\n E --> E2[IsFollowUpOwnerOrManager]\n E --> E3[IsAdminUser]\n E --> E4[IsAdminOrManager]\n E --> E5[SAFE_METHODS]\n\n F --> F1[admin 全部权限]\n F --> F2[manager 读写不可删]\n F --> F3[sales 仅自己数据]\n F --> F4[get_queryset 数据隔离]\n F --> F5[get_permissions 动态分发]\n```\n\n## 下节预告\n\n- 下一节主题：Vue 3 基础入门\n- 需要预习的内容：\n - 复习 HTML/CSS/JavaScript 基础语法\n - 了解 ES6+ 常用特性（箭头函数、解构赋值、模板字符串、Promise/async-await）\n - 思考：前后端分离中前端扮演什么角色？\n - 了解 Vue 3 的 Composition API 与 Options API 的区别\n - 安装 Node.js 20 LTS 和 VS Code\n","order":6},{"chapterName":"07-Vue3基础","content":"# Day 7 - Vue 3 基础入门\n\n## 课程信息\n## 学习目标\n\n1. **知识目标**：理解 Vue 3 的核心思想与优势，掌握 Composition API 与 Options API 的区别，理解响应式原理（ref/reactive/computed/watch）\n2. **技能目标**：能使用 Vite 创建 Vue 3 项目，熟练运用 `<script setup>` 语法编写组件，掌握条件渲染、列表渲染、事件处理、表单双向绑定等核心指令\n3. **项目目标**：创建 CRM 系统 `frontend/` 前端工程（Vite + Vue 3 + Element Plus），编写登录页面静态原型\n4. **AI 实践目标**：用 AI 快速生成 Vue 组件骨架，学习组件级 Prompt 编写技巧\n\n## Vue 3 简介与优势\n\n[Vue](https://cn.vuejs.org/)是一套**前端渐进式 JavaScript 框架**，用来开发网页界面。\n\n**渐进式** ：按需取用，由简到繁。小项目轻量用，大项目完整用；老项目局部嵌入，新项目全套开发。\n\n**可以做什么？**\n\n1. 简单网页交互（CDN 直接引入）\n\n2. SPA 单页前后端分离项目（CRM、管理后台、网站）\n\n3. 搭配 Vite 工程化工具、Element‑Plus 组件库快速做后台系统\n\n**项目两种书写语法都支持**：\n\n+ 兼容旧的 **Options API（选项式）**\n\n+ 全新 **Composition API 组合式 API + `<script setup>`**（新项目首选）\n\n\n\n**Vue 3 相比 Vue 2 的主要改进**：\n\n| 对比项 | Vue2 | Vue3 |\n| ---------- | --------------------- | ---------------------------------- |\n| 响应式原理 | Object.defineProperty | Proxy |\n| 推荐写法 | Options API | Composition API + `<script setup>` |\n| TS 支持 | 一般 | 优秀原生支持 |\n| 构建工具 | Webpack | Vite（主流） |\n| 状态管理 | Vuex | Pinia |\n| 体积 | 整体打包 | 按需导入，体积更小 |\n\n`补充：Vue3 向下兼容 Options API，旧的 Vue2 语法大部分可以直接跑，不是必须全部重写。`\n\n**Vue3应用结构**：\n\n```mermaid\ngraph LR\n A[\"createApp(App)\"] --> B[\"应用实例\"]\n B --> C[\"app.use(router)路由<br>实现页面跳转\"]\n B --> D[\"app.use(pinia)状态管理<br>跨组件共享数据\"]\n B --> E[\"app.use(ElementPlus)UI 库<br>(按钮、表单、弹窗)\"]\n B --> F[\"app.mount('#app')挂载<br>应用使用\"]\n```\n\n\n\n## 使用 Vite 创建 Vue 3 项目\n\n> 环境前提：电脑已经安装 Node.js（v18 及以上版本）\n>\n> 校验是否安装成功：打开 PowerShell\n>\n> node -v \n>\n> npm -v\n\n**Node.js** 下载地址：https://nodejs.org/zh-cn/download\n\n\n\n### Vue 3 项目创建\n\n```bat\n# 创建项目（指定 vue 模板）\n# 使用最新的create-vite程序来创建项目，用完自行删除create-vite程序\nnpm create vite@latest crm-frontend -- --template vue\n\n# 进入项目目录\ncd crm-frontend\n\n# 安装基础依赖\nnpm install\n\n# 安装项目所需的核心依赖\n# vue-router@4 路由 / pinia 状态管理 / element-plus UI库 / axios HTTP / echarts 图表\nnpm install vue-router@4 pinia element-plus @element-plus/icons-vue axios echarts\n\n# 安装开发依赖（按需自动导入 Element Plus 组件、sass 预处理器）\nnpm install -D sass unplugin-auto-import unplugin-vue-components\n\n# 启动开发服务器\nnpm run dev\n```\n\n\n\n**生产依赖**：\n\n| 包 | 作用 | 包 | 作用 |\n| ------------ | ----------------------------- | ----------------------- | ------------------------------ |\n| vue‑router@4 | 路由，实现页面跳转 | @element‑plus/icons‑vue | element plus 图标集合 |\n| pinia | 状态管理，跨组件共享数据 | axios | 后端 http 请求，调 Django 接口 |\n| element‑plus | UI 组件库（按钮、表单、弹窗） | echarts | 图表库，做统计图表 |\n\n**开发依赖**\n\n| 包 | 作用 |\n| ----------------------- | ---------------------------------------------------- |\n| sass | css 预处理器，写`.scss`样式文件 |\n| unplugin‑auto‑import | 自动导入 API（ref、reactive、ElMessage 等） |\n| unplugin‑vue‑components | 自动导入 element‑plus 组件，不用每次 import ElButton |\n\n### 项目目录结构\n\n> crm-frontend/\n> ├─ .vscode/ # VSCode编辑器配置，团队共享编辑器设置，一般不用改\n> ├─ node_modules/ # npm下载的第三方依赖包，vue/vite等都在这里，不要手动修改，git不上传\n> ├─ public/ # 原始静态资源，不会经过vite编译。图标、静态字体放这里，访问直接 /文件名\n> ├─ src/ # `项目源代码目录，业务代码全部写这里`\n> │ ├─ assets/ \t# 需要编译处理的资源：图片、样式文件\n> │ ├─ components/ # 可复用vue小组件存放目录,`我们所有课堂案例组件，全部新建在这里！`\n> │ ├─ App.vue # 项目根组件，所有页面的父容器\n> │ └─ main.js \t# js入口文件，创建vue实例，挂载到index.html\n> ├─ .gitignore \t # git忽略规则，配置哪些文件不要提交版本库\n> ├─ index.html \t# vite项目真正的html入口，包含id=\"app\"挂载节点，可修改网页标题\n> ├─ package.json # 项目配置清单：依赖版本、npm脚本命令(dev/build)、项目信息\n> ├─ package-lock.json # 锁定依赖精确版本，保证不同机器安装包版本一致，禁止手动修改\n> ├─ README.md # 项目说明文档，记录启动、部署说明\n> └─ vite.config.js \t# vite核心配置文件：代理、端口、别名、插件、打包配置都写这里\n\n\n\n```tex\n\n\n# 关键区分\npublic：资源不编译，直接原样输出，引用路径 /xxx\nsrc/assets：资源经过vite编译打包，需要import导入使用\n\n# 常用命令\nnpm run dev 启动本地开发调试服务器\nnpm run build 打包生成dist目录，用于部署上线\n```\n\n\n\n## vscode 插件安装\n\n### 手动安装插件\n\n| 插件名称 | ID和安装地址 | 作用 |\n| ----------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |\n| **Vue(Official)** | [Vue.volar](https://marketplace.visualstudio.com/items?itemName=Vue.volar)| Vue 单文件核心支持（语法高亮、补全、跳转、类型，替代旧 Volar 双插件） |\n| ESLint | [dbaeumer.vscode‑eslint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint)| js 代码语法检查、规范校验 |\n| Prettier‑Code formatter | [esbenp.prettier‑vscode](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode)| 代码格式化 |\n| Auto Rename Tag | [formulahendry.auto‑rename‑tag](https://marketplace.visualstudio.com/items?itemName=formulahendry.auto-rename-tag)| html/vue 标签同步改开闭标签 |\n| Path Intellisense | [christian‑kohler.path‑intellisense]()| 文件路径自动提示 |\n\n**配置插件**\n\n打开：`Ctrl+Shift+P` → `Preferences:Open User Settings(JSON)`，全部复制粘贴进去\n\n```json\n{\n \"editor.rename.enablePreview\":false,\n\n // -------------------------- 编辑器基础 --------------------------\n \"editor.tabSize\":2,\n \"editor.formatOnSave\":true,\n \"editor.formatOnPaste\":false,\n \"editor.rulers\":[120],\n\n // -------------------------- Prettier 全局格式化规则（全部写这里，不用项目 .prettierrc） --------------------------\n \"prettier.semi\":true,\n \"prettier.singleQuote\":true,\n \"prettier.tabWidth\":2,\n \"prettier.trailingComma\":\"es5\",\n \"prettier.printWidth\":120,\n \"prettier.arrowParens\":\"avoid\",\n \"prettier.bracketSpacing\":true,\n \"prettier.htmlWhitespaceSensitivity\":\"css\",\n \"prettier.endOfLine\":\"lf\",\n\n // 指定各类文件默认格式化器为 Prettier\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\",\n \"[vue]\":{\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\"\n},\n \"[javascript]\":{\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\"\n},\n \"[typescript]\":{\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\"\n},\n \"[json]\":{\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\"\n},\n \"[jsonc]\":{\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\"\n},\n \"[html]\":{\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\"\n},\n \"[css]\":{\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\"\n},\n \"[scss]\":{\n \"editor.defaultFormatter\":\"esbenp.prettier-vscode\"\n},\n\n // -------------------------- ESLint 全局行为配置 --------------------------\n \"eslint.enable\":false,\n \"eslint.validate\":[\n \"javascript\",\n \"javascriptreact\",\n \"typescript\",\n \"typescriptreact\",\n \"vue\"\n ],\n \"editor.codeActionsOnSave\":{\n \"source.fixAll.eslint\":true\n},\n \"eslint.workingDirectories\":[{\"mode\":\"auto\"}],\n\n // -------------------------- Auto‑Rename‑Tag 自动重命名标签 --------------------------\n \"auto-rename-tag.activationOnLanguage\":[\n \"html\",\n \"vue\",\n \"javascript\",\n \"typescript\",\n \"jsx\",\n \"tsx\"\n ],\n\n // -------------------------- Path‑Intellisense 路径智能提示 --------------------------\n \"path-intellisense.mappings\":{\n \"/\":\"${workspaceFolder}\"\n},\n \"path-intellisense.extensionOnImport\":true,\n\n // -------------------------- Volar(Vue.volar)Vue语言服务全局设置 --------------------------\n \"vue.server.hybridMode\":true,\n \"vue.format.enable\":false,\n \"vue.autoInsert.dotValue\":true,\n\n // -------------------------- 关闭其他格式化插件冲突 --------------------------\n \"javascript.format.enable\":false,\n \"typescript.format.enable\":false\n}\n```\n\n需执行面板重新加载： Ctrl+Shift+P` → ` Reload Window\n\n\n\n**项目安装插件提示**：\n\n打开项目： 执行安装命令\n\n```bat\nnpm install prettier --save-dev\n```\n\n\n\n### 导入配置安装插件\n\n**导入配置**\n\nvscode 左下角设置 --> 配置文件 --> 新建配置文件右边V,--> 导入配置文件 --> 选择 `vue项目配置.code-profile` 文件\n\n**项目安装插件提示**：\n\n\n\n## components/.vue 组件\n\n所有的案例组件都是写在 components 下的.vue 文件上\n\n一个 vue 组件分成 3 部分：\n\n```vue\n<template>\n <!-- 这里写HTML代码，页面显示内容写这里 -->\n</template>\n\n<script setup>\n// 这里写JS业务逻辑，导入API、定义变量、写函数\n</script>\n\n<style scoped>\n/* 写css样式，scoped代表样式只对当前组件生效 */\n</style>\n```\n\n\n\n### 组件创建与配置\n\n1. 在 `src/components/` 新建文件，例如 `01.Demo.vue`\n\n ```vue\n <style scoped>\n /* scoped：样式仅作用于当前组件，不会污染全局页面 */\n .box{\n width:400px;\n padding:30px;\n border:1px solid #eee;\n border-radius:8px;\n}\n </style>\n \n <!-- template区域：编写网页展示的HTML内容 -->\n <template>\n <div class=\"box\">\n <h2>Vue3 Demo入门演示</h2>\n <p>当前计数：{{name}}</p>\n </div>\n </template>\n \n <!-- script setup：编写JS逻辑代码 -->\n <script setup>\n // 从vue框架引入 ref 工具函数\n import{ref}from 'vue';\n \n // 定义响应式名称变量 name，默认值：晓东，ref 可定义任何类型的变量\n const name = ref('晓东');\n </script>\n ```\n \n2. 打开 `src/App.vue`，导入并且使用这个组件\n\n ```vue\n <script setup>\n // 导入写好的组件\n import Demo from './components/01.Demo.vue'\n </script>\n \n <template>\n <!-- 使用组件标签 -->\n <Demo />\n </template>\n ```\n\n\n\n### 浏览器查看和编辑数据\n\n安装vue插件： [devtools_v3_7.7.7.0.crx](https://chromewebstore.google.com/detail/vuejs-devtools/nhdogjmejiglipccpnnnanhbledajbpd?utm_source=ext_sidebar)\n\n打开浏览器 拓展程序 --> 拖入 devtools_v3_7.7.7.0.crx 文件点安装即可\n\n\n\n### vue 项目交互图\n\n```mermaid\nsequenceDiagram\n participant 用户\n participant 浏览器\n participant index.html\n participant main.js\n participant Vue应用\n\n 用户->>浏览器:打开网页\n 浏览器->>index.html:请求并加载页面\n index.html-->>浏览器:返回 HTML（含挂载点 <div id=\"app\">）\n 浏览器->>main.js:加载并执行 main.js\n main.js->>Vue应用:创建 Vue 实例，并挂载到 #app\n Vue应用-->>浏览器:页面内容渲染完成\n 浏览器-->>用户:显示界面\n\n 用户->>Vue应用:点击按钮 / 输入内容\n Vue应用->>Vue应用:更新数据（自动刷新视图）\n Vue应用-->>浏览器:更新页面显示\n 浏览器-->>用户:看到新内容\n```\n\n## 插值表达式{{}}\n\n**作用**：把 JS 数据、运算结果渲染到页面 HTML 文本中。支持变量、运算、三元、函数调用、对象取值；不支持语句和复杂逻辑。\n\n**核心规则**：\n\n1. 只能写**单行简单 JS 表达式**\n\n2. 不能写：语句、流程控制、变量声明、if、for、函数调用定义\n\n3. 最终运算结果会转为字符串展示在页面\n\n4. 响应式数据变化，插值内容自动更新\n\n**常用方式**：\n\n| 用法 | 作用 | 例子 |\n| ------------------ | -------------------------------------------------------- | ------------------------------------------------------------ |\n| 渲染响应式变量 | 直接渲染响应式变量 | `<p>姓名：{{username}}</p>` |\n| 算术运算 | 加减乘除、取模都支持 | `<p>10 + 20 ={{10 + 20}}</p>` |\n| 字符串拼接 | 字符串拼接 | `<p>{{'我的名字：' + username}}</p>` |\n| 三元表达式 | 模板里不能写 if，判断显示文字用三元运算符 | `<p>状态：{{isLogin ? '已登录':'未登录'}}</p>` |\n| 调用函数 | 只能调用 `<script setup>` 中声明完毕的方法，用来返回结果 | `<p>两倍数字：{{doubleCount()}}</p>` |\n| 读取对象、数组属性 | 读取对象、数组属性 | `<p>用户：{{user.name}}</p>` <br> `<p>首个商品：{{goodsList[0].title}}</p>` |\n| 读取计算属性 | 读取计算属性 computed | `<p>合计：{{total}}</p>` |\n\n\n\n## 组合式（Composition）API \n\nVue3 推出 **Composition API（组合式 API）**：\n\n把**同一个业务逻辑的代码收拢写在一起**，自由组合、抽离复用；使用`Composition API 的语法糖<script setup>，日常开发主流写法。`\n\n+ **业务代码聚合在一起，方便阅读**\n\n 同一个功能的变量、函数写相邻位置，不用在文件来回上下翻。\n\n+ **逻辑复用超级方便**\n\n 可以把一整块业务，抽取到单独 js 文件，多个组件直接导入使用。\n\n\n\n`<script setup>` 是其编译期语法糖，是最推荐的写法。\n\n```vue\n<template>\n <button @click=\"count++\">点击{{count}}次</button>\n <p>{{product.productName}}</p>\n</template>\n\n<script setup>\nimport{ref,reactive,computed}from 'vue'\n\n// 声明响应式数据后，模板中可直接使用，无需 return\nconst count = ref(0)\nconst product = reactive({productName:'鼠标'})\n</script>\n```\n\n**要点**：\n\n- `<script setup>` 中顶层的变量、函数、import 自动暴露给模板\n- 组件导入后**无需注册**即可在模板中使用\n- 与 Options API 可共存，但新项目推荐统一用 `<script setup>`\n\n\n\n### Composition API 常用核心工具（必掌握）\n\n全部需要从 vue 中导入使用\n\n```js\nimport{ref,reactive,computed,watch,onMounted}from 'vue'\n```\n\n| 工具 | 作用 | 例子 |\n| ------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |\n| **ref** | 存放**数字、字符串、布尔**简单类型响应式数据<br>JS 内部修改：`.value`;模板直接使用，不加 .value | `const num = ref(10)`<br> `const name = ref(\"张三\")`<br>`num.value = 20` |\n| **reactive** | 存放**对象、数组**复杂数据，无需 .value | `const user = reactive({name:\"小明\"})`<br>`user.name = \"小红\"` |\n| **computed ** | 依赖现有数据自动算出新值，数据变化自动更新 | `const num = ref(2)`<br>// 总价自动计算<br> `const total = computed(()=>{return price.value * num.value})` |\n| **watch** | 监听数据变化，触发自定义逻辑（日志、接口请求） | `const count = ref(0)` <br>`watch(count,`<br>`(newVal,oldVal)=>{console.log(\"数字变了\",newVal)}`<br>`)` |\n| **onMounted** | 生命周期钩子,页面初次加载完成执行（请求接口首选） | `onMounted(()=>{console.log(\"页面渲染完毕\")})` |\n\n**示例**：\n\n```vue\n<template>\n <div>\n <h3>Composition API综合案例</h3>\n <p>数量：{{count}}</p>\n <button @click=\"addNun\">+1</button>\n <p>总价：{{totalPrice}}</p>\n <p>用户名：{{user.name}}</p>\n </div>\n</template>\n\n<script setup>\n// 导入组合式API函数\nimport{ref,reactive,computed,watch,onMounted}from 'vue'\n\n// 1.简单数据 ref\nconst count = ref(1)\n\n// 2.对象数据 reactive\nconst user = reactive({\n name:\"小李\"\n})\n\n// 3.计算属性\nconst price = ref(50)\nconst totalPrice = computed(()=>{\n return count.value * price.value\n})\n\n// 4.方法\nconst addNum =()=>{\n count.value++\n}\n\n// 5.监听数据变化\nwatch(count,(newVal)=>{\n console.log(\"数量更新为：\",newVal)\n})\n\n// 6.页面加载生命周期\nonMounted(()=>{\n console.log(\"页面初次渲染完成，可以请求后端数据\")\n})\n</script>\n```\n\n\n\n## 页面业务处理\n\n### 条件渲染 v-if / v-show\n\n| 指令 | 机制 | 切换成本 | 适用场景 |\n|------|------|---------|---------|\n| `v-if` | 真正销毁/重建 DOM 元素 | 较高 | 条件很少改变 |\n| `v-show` | 切换 CSS `display` | 低 | 频繁切换显示 |\n\n```html\n<template>\n <div class=\"box\">\n <!-- v-if：条件为 false 时元素不存在于 DOM -->\n <div v-if=\"isLoading\">加载中...</div>\n\n <!-- v-show：元素始终在 DOM，仅切换 display -->\n <div v-show=\"isExpanded\">展开内容</div>\n </div>\n</template>\n\n<script setup>\n// 从vue框架引入 ref 工具函数\nimport{ref}from 'vue';\nconst isLoading = ref(true);\nconst isExpanded = ref(true);\n</script>\n\n<style scoped>\n.box{\n width:400px;\n padding:30px;\n border:1px solid #eee;\n border-radius:8px;\n}\n</style>\n```\n\n`v-else` 和 `v-else-if` 必须紧跟在 `v-if` 元素之后。\n\n\n\n### 列表渲染 v-for\n\n```html\n<template>\n <div class=\"box\">\n <!-- 遍历数组，必须绑定唯一 key -->\n <h3>中国四大美女</h3>\n <li v-for=\"(item,index)in list\":key=\"item.id\">{{index}}-{{item.name}}</li>\n\n <!-- 遍历对象 -->\n <div v-for=\"(value,key)in user\":key=\"key\">{{key}}:{{value}}</div>\n </div>\n</template>\n\n<script setup>\n// 从vue框架引入 ref 工具函数\nimport{ref,reactive}from 'vue';\n\n// 1、reactive 定义数组（里面存放对象，包含id唯一标识+姓名）\nconst list = reactive([\n{id:1,name:'西施'},\n{id:2,name:'王昭君'},\n{id:3,name:'貂蝉'},\n{id:4,name:'杨玉环'},\n]);\n\n// 2、reactive 定义普通对象\nconst user = reactive({\n name:'李白',\n age:26,\n gender:'男',\n});\n</script>\n\n<style scoped>\n.box{\n width:400px;\n padding:30px;\n border:1px solid #eee;\n border-radius:8px;\n}\n</style>\n```\n\n**要点**：\n\n- `:key` 必须绑定唯一且稳定的值（推荐用数据 id，不要用 index）\n- `v-for` 和 `v-if` 不建议写在同一元素上（Vue 3 中 v-if 优先级更高，会报错）\n\n\n\n### 事件处理 @click\n\n```html\n<template>\n <div class=\"box\">\n <!-- 内联语句 -->\n <button @click=\"count++\">加1,结果：{{count}}</button>\n\n <!-- 方法调用 -->\n <button @click=\"handleClick\">提交</button>\n\n <!-- 传参 + 事件对象 -->\n <button @click=\"doAction(item,$event)\">操作</button>\n\n <!-- 修饰符 -->\n <!-- 阻止冒泡：避免点击子盒子，触发父盒子的点击事件 -->\n <div @click=\"parentClick\" style=\"padding:20px;background:#eee;\">\n 父容器\n <button @click.stop=\"doSomething1\">阻止冒泡</button>\n <button @click=\"doSomething2\">未阻止冒泡</button>\n </div>\n <input @keyup.enter=\"submit\">回车提交</input>\n <form @submit.prevent=\"onSubmit\">阻止默认提交</form>\n </div>\n</template>\n\n<script setup>\n import{ref}from 'vue';\n\n // 1. 计数器数字\n const count = ref(0)\n\n // 2. 普通点击方法\n const handleClick =()=>{\n alert('点击了提交按钮')\n}\n\n // 3. 准备要传递的参数\n const item ={\n id:1,\n name:'测试数据'\n}\n\n // 接收参数 + 原生事件对象\n const doAction =(data,e)=>{\n console.log('传递的数据：',data)\n console.log('原生事件对象：',e)\n}\n \n\n // 阻止冒泡的事件\n const doSomething1 =()=>{\n console.log('已阻止事件冒泡')\n}\n const doSomething2 =()=>{\n console.log('未阻止事件冒泡')\n}\n const parentClick =()=>{\n console.log('父盒子点击事件');\n \n}\n \n\n // 回车触发\n const submit =()=>{\n alert('按下回车键，提交成功')\n}\n\n // 阻止表单默认刷新提交\n const onSubmit =()=>{\n alert('表单提交，不会刷新页面')\n}\n</script>\n\n<style scoped>\n.box{\n width:400px;\n padding:30px;\n border:1px solid #eee;\n border-radius:8px;\n}\n</style>\n```\n\n\n\n### watch 侦听器进阶\n\n\n\n**监听 reactive 响应式对象中的属性**\n\n> 语法:\n>\n> ```js\n> watch(()=> 对象.属性,\n>(newVal,oldVal)=>{\n> \t\tconsole.log(`数据从 \"${oldVal}\" 变为 \"${newVal}\"`)\n> \t}\n>)\n> ```\n\n**监听多个数据源**\n\n> 语法：\n>\n> ```js\n> watch(\n> [数据源1,数据源2,数据源3...],\n>([新值1,新值2,新值3],[旧值1,旧值2,旧值3])=>{\n> // 任意一个数据源发生变化，都会触发回调\n>}\n>)\n> ```\n\n**示例**：\n\n```vue\n<template>\n <div>\n <!-- 关键词：参与多源监听 -->\n <input v-model=\"keyword\" placeholder=\"输入关键词\" />\n <br /><br />\n\n <!-- 状态：单独监听对象属性 -->\n <select v-model=\"filters.status\">\n <option value=\"\">请选择状态</option>\n <option value=\"lead\">线索</option>\n <option value=\"following\">跟进中</option>\n </select>\n <br /><br />\n\n <!-- 来源：和关键词组合多源监听 -->\n <select v-model=\"filters.source\">\n <option value=\"\">请选择来源</option>\n <option value=\"online\">线上</option>\n <option value=\"offline\">线下</option>\n </select>\n </div>\n</template>\n\n<script setup>\nimport{ref,watch,reactive}from 'vue';\n\n// 普通ref（仅用于多源组合）\nconst keyword = ref('');\n\n// reactive 响应式对象\nconst filters = reactive({\n status:'',\n source:'',\n});\n\n// 1、监听 reactive 对象中的单个属性\nwatch(\n()=> filters.status,\n newVal =>{\n console.log('筛选状态已修改：',newVal);\n}\n);\n\n// 2、一次性监听多个数据：关键词 + 来源\nwatch([keyword,()=> filters.source],([kw,src])=>{\n console.log('关键词 / 来源任意一项发生变化：',kw,src);\n});\n</script>\n\n```\n\n\n\n### 表单双向绑定 v-model\n\n`v-model` 在表单元素上实现双向绑定：输入框输入 → 数据更新，数据更新 → 输入框显示。\n\n```mermaid\nflowchart LR\n A[user.name 响应式数据] -->|:value绑定| B[输入框]\n B -->|输入触发input事件回传新值| A\n```\n\n\n\n#### Element Plus 组件库\n\n[Element Plus](https://element-plus.org/)的原名是 Element UI,是一个Vue3 的组件库，内置写好的样式，只需要引用即可高效的写出漂亮的 vue 3 页面\n\n文档：https://element-plus.org/\n\n**安装依赖**：\n\n```shell\nnpm install element-plus\t\t# 一开始就安装了，这里不用安装\n```\n\n**vue项目完整引入**：\n\n```js\nimport{createApp}from 'vue'\nimport App from './App.vue'\n// 引入 ElementPlus\nimport ElementPlus from 'element-plus'\n// 引入 ElementPlus 样式文件\nimport 'element-plus/dist/index.css'\n\nconst app = createApp(App)\napp.use(ElementPlus)\t\t\t// 全局挂载，所有页面直接使用 el-xxx 组件\napp.mount('#app')\n```\n\n\n\n#### 表单基础案例\n\n```html\n<template>\n <div class=\"box\">\n <!-- 文本输入 -->\n <el-input v-model=\"form.username\" placeholder=\"用户名\" />\n\n <!-- 密码输入 -->\n <el-input v-model=\"form.password\" type=\"password\" show-password />\n\n <!-- 下拉选择 -->\n <el-select v-model=\"form.status\">\n <el-option label=\"线索\" value=\"lead\" />\n <el-option label=\"跟进中\" value=\"following\" />\n </el-select>\n </div>\n</template>\n```\n\n**修饰符**：\n\n- `.lazy`：失焦或回车时才同步（默认 input 即同步）\n- `.number`：自动转为数字\n- `.trim`：自动去除首尾空格\n\n```html\n<!-- 失去焦点/回车才更新 -->\n<input v-model.lazy=\"msg\" placeholder=\"输入文字试试\" />\n```\n\n\n\n**JS**\n\n```js\n\n<script setup>\n // Element Plus 表单统一用 reactive 定义表单对象最合适\n import{reactive}from 'vue'\n\n // 表单整体数据对象，所有表单项都挂载在form里\n const form = reactive({\n username:'',\n password:'',\n status:''\n})\n</script>\n```\n\n\n\n### CRM登录表单\n\n以下为 `LoginView.vue` 的核心简化版，演示 `v-model` 与 `reactive` 配合实现登录表单：\n\nv-model ：双向数据绑定\n\n:model ：单向数据绑定，一般写在 form 表单标签上\n\n```vue\n<template>\n <div class=\"login-container\">\n <div class=\"login-box\">\n <div class=\"login-header\">\n <h2>CRM 客户管理系统</h2>\n <p>请登录您的账号</p>\n </div>\n <!-- el-form 绑定模型与校验规则 -->\n <el-form\n ref=\"loginFormRef\"\n:model=\"loginForm\"\n:rules=\"loginRules\"\n size=\"large\"\n @keyup.enter=\"handleLogin\"\n >\n <!-- v-model 双向绑定用户名 -->\n <el-form-item prop=\"username\">\n <el-input\n v-model=\"loginForm.username\"\n placeholder=\"请输入用户名\"\n:prefix-icon=\"User\"\n clearable\n />\n </el-form-item>\n <!-- v-model 双向绑定密码 -->\n <el-form-item prop=\"password\">\n <el-input\n v-model=\"loginForm.password\"\n type=\"password\"\n placeholder=\"请输入密码\"\n:prefix-icon=\"Lock\"\n show-password\n clearable/>\n </el-form-item>\n <el-form-item>\n <el-button\n type=\"primary\"\n class=\"login-btn\"\n:loading=\"loading\"\n @click=\"handleLogin\">\n 登 录\n </el-button>\n </el-form-item>\n </el-form>\n </div>\n </div>\n</template>\n\n<script setup>\nimport{ref,reactive}from 'vue'\nimport{User,Lock}from '@element-plus/icons-vue'\nimport{ElMessage}from 'element-plus'\n\n// 表单引用，用于调用 validate 方法\nconst loginFormRef = ref(null)\n// 登录按钮 loading 状态\nconst loading = ref(false)\n\n// 登录表单数据（reactive 管理对象）\nconst loginForm = reactive({\n username:'',\n password:''\n})\n\n// 表单校验规则\nconst loginRules ={\n username:[\n{required:true,message:'请输入用户名',trigger:'blur'},\n{min:2,max:30,message:'用户名长度在 2 到 30 个字符',trigger:'blur'}\n ],\n password:[\n{required:true,message:'请输入密码',trigger:'blur'},\n{min:6,max:30,message:'密码长度在 6 到 30 个字符',trigger:'blur'}\n ]\n}\n\n// 登录处理（本节先做静态校验，后续接入 API）\nasync function handleLogin(){\n if(!loginFormRef.value)return\n try{\n // 调用表单校验\n await loginFormRef.value.validate()\n loading.value = true\n // TODO:Day 9 接入后端登录 API\n ElMessage.success('表单校验通过')\n console.log('提交的表单数据：',loginForm)\n}catch(e){\n // 校验失败\n ElMessage.warning('请完善表单信息')\n}finally{\n loading.value = false\n}\n}\n</script>\n\n<style scoped>\n.login-container{\n height:100vh;\n display:flex;\n align-items:center;\n justify-content:center;\n background:linear-gradient(135deg,#667eea 0%,#764ba2 100%);\n}\n.login-box{\n width:400px;\n padding:40px;\n background:#fff;\n border-radius:8px;\n box-shadow:0 8px 30px rgba(0,0,0,0.2);\n}\n.login-header{\n text-align:center;\n margin-bottom:30px;\n}\n.login-btn{\n width:100%;\n}\n</style>\n```\n\n**要点**：\n\n- `reactive` 管理整个表单对象，`v-model` 绑定到对象的各个属性\n- `ref` 用于管理 DOM 引用（`loginFormRef`）来管理表单组件的校验和布尔状态（`loading`）\n- `@keyup.enter` 修饰符实现回车提交\n- `:loading` 绑定按钮加载状态，防止重复提交\n\n\n\n## 其他配置（拓张）\n\n### 配置 vite.config.js\n\n1. 配置 Element Plus 自动导入，防止项目打包时导入一堆没用到的组件和样式\n\n\n\n完整配置\n\n```js\nimport{defineConfig}from 'vite'\nimport vue from '@vitejs/plugin-vue'\nimport{resolve}from 'path'\n// 自动导入依赖\nimport AutoImport from 'unplugin-auto-import/vite'\nimport Components from 'unplugin-vue-components/vite'\nimport{ElementPlusResolver}from 'unplugin-vue-components/resolvers'\n\nexport default defineConfig({\n plugins:[\n vue(),\n AutoImport({\n // 自动导入vue、vue-router相关API：ref/reactive/watch/onMounted/useRouter等\n imports:['vue','vue-router'],\n // 自动引入Element Plus 工具方法：ElMessage、ElMessageBox等\n resolvers:[ElementPlusResolver()],\n // 生成ts类型文件，解决TS找不到ref等变量报错\n dts:'./auto-imports.d.ts'\n}),\n Components({\n // 自动解析template中el-开头UI组件，无需手动引入注册\n resolvers:[ElementPlusResolver()],\n // 组件类型声明文件\n dts:'./components.d.ts'\n})\n ],\n\n resolve:{\n alias:{\n // @ 映射 src 目录\n '@':resolve(__dirname,'src')\n}\n},\n\n server:{\n proxy:{\n '/api':{\n target:'http://127.0.0.1:8000',\n changeOrigin:true,\n // 去除接口前缀 /api\n // rewrite:(path)=> path.replace(/^\\/api/,'')\n}\n}\n}\n})\n```\n\n使用效果\n\n```vue\n<script setup>\n// 无需 import{ref,watch}from 'vue'\nconst count = ref(0)\nwatch(count,(val)=>{\n console.log(val)\n})\n\n// 无需 import{ElMessage}from 'element-plus'\nconst tip =()=> ElMessage.success('操作成功')\n</script>\n\n<template>\n <!-- 无需引入 ElInput 组件 -->\n <el-input v-model=\"count\" />\n <el-button @click=\"tip\">点击</el-button>\n</template>\n```\n\n### 创建.env.development\n\n.env.development\n\n```\n# 开发环境接口基础地址\nVITE_API_BASE_URL=/api\n```\n\n代码中读取环境变量\n\n```\n// src/utils/request.js\nimport axios from 'axios'\n\nconst service = axios.create({\n baseURL:import.meta.env.VITE_API_BASE_URL,\n timeout:10000\n})\n\nexport default service\n```\n\n请求时：\n\n```\n// 最终拼接地址：/api/login，会被vite代理转发到Django后端\nrequest.post('/login',{username,password})\n```\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n创建 CRM 系统前端工程，完成基础配置并编写登录页面静态原型。\n\n### 实现步骤\n1. 使用 `npm create vite@latest` 创建 Vue 3 项目，命名为 `crm-frontend`\n2. 安装核心依赖：`vue-router`、`pinia`、`element-plus`、`@element-plus/icons-vue`、`axios`、`echarts`\n3. 安装开发依赖：`sass`、`unplugin-auto-import`、`unplugin-vue-components`\n4. 配置 `vite.config.js`：设置 `@` 别名指向 `src`、配置 `/api` 代理到 Django 后端 `http://127.0.0.1:8000`、配置 Element Plus 自动导入\n5. 创建 `.env.development`，配置 `VITE_API_BASE_URL=/api`\n6. 编写 `src/main.js`，注册 Element Plus\n8. 创建 `LoginView.vue`，使用 el-form + v-model 完成登录表单静态原型\n9. 运行 `npm run dev` 启动开发服务器，浏览器访问 `http://localhost:5173` 验证\n\n## 六、AI 辅助编程实践\n\n### 本节 AI 实践主题\n用 AI 快速生成 Vue 组件骨架，学习组件级 Prompt 编写。\n\n### 工具\nTrae / ChatGPT / Claude 等支持代码生成的 AI 工具。\n\n### 场景\n创建登录页面时，需要编写模板结构、响应式数据、校验规则和事件处理。可以让 AI 先生成骨架代码，再在其基础上修改。\n\n### Prompt 示例\n\n```\n你是一位资深 Vue 3 前端工程师。请帮我生成一个登录页面组件\nLoginView.vue，技术要求如下：\n\n1. 使用 Vue 3 <script setup> 语法\n2. UI 库使用 Element Plus（el-form / el-input / el-button）\n3. 表单字段：username（用户名）、password（密码）\n4. 使用 reactive 管理表单数据，ref 管理 loading 状态\n5. 包含表单校验规则（必填 + 长度限制）\n6. 用户名输入框前缀图标用 User，密码框用 Lock，密码框 show-password\n7. 登录按钮绑定 click 事件，回车键也能提交\n8. 样式：全屏渐变背景，居中卡片式登录框\n9. 中文注释，代码可运行\n\n请输出完整的 .vue 文件代码。\n```\n\n### 实践要点\n- Prompt 中明确技术栈版本（Vue 3 + script setup + Element Plus），避免生成 Vue 2 或 Options API 代码\n- 指明数据管理方式（reactive 还是 ref），让 AI 输出风格统一\n- 生成后需人工检查：图标是否从 `@element-plus/icons-vue` 正确导入、校验规则是否符合业务需求\n- 可让 AI 同时生成 `<style scoped>` 样式，再根据设计稿微调\n\n## 课堂练习\n\n**练习 1**（基础）：编写一个计数器组件 `Counter.vue`，使用 `ref` 管理计数值，`computed` 计算计数的平方，包含\"加1\"\"减1\"\"重置\"三个按钮。当计数为 0 时用 `v-show` 隐藏\"减1\"按钮。\n\n思路提示：`ref(0)` 声明计数，`computed(()=> count.value * count.value)` 计算平方，`v-show=\"count > 0\"` 控制按钮显隐。\n\n**练习 2**（中等）：编写一个客户筛选组件，包含一个搜索输入框（v-model 绑定 keyword）和一个状态下拉选择（v-model 绑定 status），使用 `watch` 监听这两个值的变化并打印日志，用 `v-for` 渲染一个静态客户列表（5 条数据），并用 `v-if` 根据搜索词过滤显示。\n\n思路提示：\n- 用 `reactive` 管理 `{keyword:'',status:''}`\n- 静态数据用 `ref([{id,name,status},...])`\n- 计算属性 `filteredList` 根据筛选条件返回过滤后的列表\n\n## 课后作业\n\n### 必做\n1. 完成 Vite + Vue 3 + Element Plus 项目创建，确保 `npm run dev` 能正常启动\n2. 完成 `LoginView.vue` 登录页面静态原型，表单校验功能可用\n3. 编写一个 `Counter.vue` 计数器组件，演示 ref/reactive/computed/watch 的用法\n4. 配置好 `vite.config.js` 的 `@` 别名和 `/api` 代理\n\n### 选做（进阶）\n1. 给登录页添加\"记住密码\"复选框（v-model 绑定 boolean），勾选时将用户名存入 localStorage\n2. 用 AI 生成一个注册页面组件，对比 AI 输出与手写代码的差异\n\n### 提交方式\n将 `frontend/` 项目目录打包提交，附上登录页面运行截图。\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[Vue 3 基础] --> B[项目搭建]\n A --> C[Composition API]\n A --> D[模板指令]\n B --> B1[Vite 创建]\n B --> B2[main.js 入口]\n B --> B3[Element Plus]\n C --> C1[script setup]\n C --> C2[ref 基本类型]\n C --> C3[reactive 对象]\n C --> C4[computed 计算]\n C --> C5[watch 侦听]\n D --> D1[v-if / v-show]\n D --> D2[v-for 列表]\n D --> D3[@click 事件]\n D --> D4[v-model 表单]\n```\n\n## 下节预告\n\n- 下一节主题：Vue 组件与路由\n- 需要预习的内容：\n - 复习本节的 `<script setup>` 与响应式 API\n - 思考：什么是组件化？父子组件之间如何传递数据？\n - 了解单页面应用（SPA）的概念，思考前端路由是如何工作的\n","order":7},{"chapterName":"08-Vue组件与路由","content":"# 学习目标\n\n1. **知识目标**：理解组件化开发思想，掌握 props/emit/slot 三种组件通信方式，理解 Vue Router 4 的工作原理与嵌套路由机制\n2. **技能目标**：能编写父子组件并通过 props/emit 通信，能配置路由表、嵌套路由、动态路由参数，能编写路由守卫实现登录拦截与权限控制\n3. **项目目标**：搭建 `DefaultLayout` 布局（侧边栏 + 顶部栏），完成客户列表/详情页面骨架，配置路由表和全局路由守卫\n4. **AI 实践目标**：用 AI 生成路由配置和布局组件，学习结构化 Prompt 模板\n\n\n\n# 理论知识铺垫\n\n## 组件化思想\n\n组件化是将界面拆分为**独立、可复用**的组件，每个组件负责自己的模板、逻辑和样式。\n\n```\n+----------------------------------------------------------------------+\n| CRM Application |\n+--------+-------------------------------------------------------------+\n| | [Collapse] [Breadcrumb] [User Avatar] |\n| Sidebar| --------------------------------------------------------- |\n|(Menu)| |\n| | <router-view /> Main Content Area |\n| Item1 | |\n| Item2 | +-------------------+ +-------------------+ |\n| Item3 | | CustomerList | | DashboardView | |\n| ... | |(Customer List)| |(Dashboard)| |\n| | +-------------------+ +-------------------+ |\n| | | CustomerDetail | | ... | |\n| | |(Customer Detail)| | | |\n| | +-------------------+ +-------------------+ |\n+--------+-------------------------------------------------------------+\n```\n\n```mermaid\ngraph TD\n A[CRM 整个应用根项目] --> B[DefaultLayout 全局布局容器组件<br/>（页面外壳，常驻页面）]\n \n %% DefaultLayout内部两大板块：顶部导航 + 侧边栏 + 主体内容区\n B --> C[左侧 SideBar 侧边栏组件]\n B --> D[顶部 NavBar 导航栏组件]\n B --> E[<router-view/> 路由挂载主内容区域]\n\n %% 侧边栏内容\n C --> C1[循环渲染菜单项<br/>基于路由配置生成菜单]\n \n %% 顶部导航栏内容\n D --> D1[侧边栏折叠按钮]\n D --> D2[面包屑导航]\n D --> D3[用户信息/头像下拉]\n\n %% 路由视图渲染的页面组件（路由匹配后动态载入）\n E --> E1[DashboardView 仪表盘首页]\n E --> E2[CustomerList 客户列表页]\n E --> E3[CustomerDetail 客户详情页]\n E --> E4[其他业务页面...]\n```\n\n**组件化的好处**：\n\n- **复用**：同一组件可在多处使用（如 `StatusTag` 在列表、详情中复用）\n- **维护**：每个组件独立关注一个功能，便于定位问题\n- **协作**：不同开发者负责不同组件，互不干扰\n\n\n\n### 组件化对比\n\n**不拆分组件**：所有代码堆在一页，臃肿难维护\n\n```vue\n<!-- 原始写法：没有组件化，全部写在一个文件里 -->\n<template>\n <div>\n <div class=\"tag\">线索</div>\n <div class=\"tag\">跟进中</div>\n </div>\n</template>\n<style>\n.tag{padding:4px 8px;background:#909399;color:#fff;border-radius:4px;margin:4px}\n</style>\n```\n\n**组件化拆分后**：把标签抽取成独立组件，反复调用\n\n```vue\n<!-- 公共子组件 Tag.vue -->\n<template>\n <div class=\"tag\">{{text}}</div>\n</template>\n<style scoped>\n\t.tag{padding:4px 8px;background:#909399;color:#fff;border-radius:4px;margin:4px}\n</style>\n```\n\n```vue\n<!-- 父页面直接复用 -->\n<template>\n <div>\n <Tag text=\"线索\" />\n <Tag text=\"跟进中\" />\n <Tag text=\"已成交\" />\n </div>\n</template>\n<script setup>\n\timport Tag from './Tag.vue'\n</script>\n```\n\n`代码精简、统一样式、修改只需要改一处`\n\n\n\n**组件层级流转**\n\n顶层页面（父组件）→ 业务子组件 → 全局公共基础组件\n\n数据默认自上而下流动。\n\n\n\n## 组件通信四大核心\n\n### props：父向子传递数据\n\n父组件通过`属性`向子组件`传递数据`，子组件用 `defineProps` 声明接收。\n\n+ 作用：父页面把变量、文本、状态传给子组件渲染使用\n\n+ 规则：数据只能父传给子，子组件不允许直接修改 props 接收的值\n\n+ 附加能力：支持数据类型校验、默认值、必填项设置\n\n+ 适用场景：客户状态传给标签组件、列表数据传给表格组件\n\n```\n 父组件 子组件\n┌──────────┐ props ┌──────────────┐\n│ msg=\"\" | ───────→ │ defineProps |\n│ 或:msg=\"\" | | 读取 msg |\n└──────────┘ └──────────────┘\n```\n\n\n\n**常用状态标签抽取**\n\nhttps://element-plus.org/zh-CN/component/tag\n\n```vue\n<!-- common/StatusTag.vue--->\n<!-- 子组件定义标状态签选项 -->\n<script setup>\nimport{computed}from 'vue'\n\n// 接受父组件属性传递过来的数据\nconst props = defineProps({\n status:{\n type:String,\n default:'lead' // 默认值\n},\n effect:{\n type:String,\n default:'light' // 默认 Light 主题，可在父组件中修改主题\n}\n})\n\n// 统一配置中心\nconst STATUS_MAP ={\n lead:{text:'线索',type:'info'},\n contacted:{text:'已联系',type:'primary'},\n qualified:{text:'有意向',type:'warning'},\n won:{text:'已成交',type:'success'},\n lost:{text:'已流失',type:'danger'}\n}\n\nconst statusText = computed(()=> STATUS_MAP[props.status]?.text || '未知状态')\nconst tagType = computed(()=> STATUS_MAP[props.status]?.type || 'info')\n</script>\n\n<template>\n <el-tag:type=\"tagType\":effect=\"effect\" size=\"small\">\n{{statusText}}\n </el-tag>\n</template>\n```\n\n```vue\n<!-- CustomerListView01.vue--->\n<!-- 父组件中使用 -->\n<template>\n <!-- 带:动态绑定数据，无：直接给属性赋值 -->\n <StatusTag:status=\"row.status\"/><br>\n <StatusTag status=\"qualified\" effect=\"dark\"/>\n</template>\n\n<script setup>\nimport StatusTag from '../common/StatusTag.vue'\nimport{reactive}from 'vue'\nconst row = reactive({\n status:'won'\n})\n</script>\n```\n\n要点：\n- `defineProps` 是编译宏，无需 import\n- props 是**单向数据流**，子组件不应直接修改 props\n- 可通过 `type`/`default`/`required`/`validator` 进行校验\n\n\n\n### emit：子向父传递事件\n\n子组件通过 `defineEmits` 声明事件，用 `emit()` 触发，父组件用 `@事件名` 监听。\n\n+ 作用：子组件无法直接修改父数据，通过自定义事件通知父组件执行逻辑\n\n+ 流程：子组件定义事件 → 触发 emit → 父组件监听事件并执行回调函数\n\n+ 适用场景：子组件点击刷新按钮，通知父组件重新请求客户列表\n\n```\n 父组件 子组件\n┌──────────┐ emit ┌──────────────┐\n│ @refresh\t\t | ←─────── │ defineEmits \t\t |\n│ \t\t | │ emit('refresh')\t |\n└──────────┘ └──────────────┘\n```\n\n**示例**：\n\n客户列表抽取为列表项组件，然后父容器包含子容器，列表被删除时触发父容器更新\n\n```vue\n<!-- common/BtnDemo.vue -->\n<template>\n <el-button @click=\"handleClick\">删除客户刷新列表</el-button>\n</template>\n\n<script setup>\n// 声明要派发的自定义事件\nconst emit = defineEmits(['refresh'])\n\nconst handleClick =()=>{\n console.log(\"子组件删除列表项\")\n \t// 触发事件，还可以传递参数\n \temit('refresh','需要重新加载客户数据')\n}\n</script>\n```\n\n```vue\n<template>\n <div>\n <p>客户列表数据</p>\n <!-- @自定义事件名 接收子组件派发的事件 -->\n <BtnDemo @refresh=\"refreshTable\" />\n </div>\n</template>\n\n<script setup>\nimport BtnDemo from '../common/BtnDemo.vue'\n\nconst refreshTable =(msg)=>{\n console.log('父组件收到通知：',msg)\n // 此处写刷新列表逻辑\n console.log('发起请求，属性列表')\n}\n</script>\n```\n\n\n\n**CRM使用场景介绍**：\n\n```vue\n<!-- 子组件 CommentItem.vue（简化） -->\n<script setup>\nconst props = defineProps({\n comment:{type:Object,required:true}\n})\n\n// 声明会触发的事件\nconst emit = defineEmits(['reply','refresh'])\n\n// 删除评论后通知父组件刷新\nasync function handleDelete(){\n await deleteComment(props.comment.id)\n emit('refresh')// 触发 refresh 事件\n}\n</script>\n```\n\n```vue\n<!-- 父组件 CommentSection.vue 监听事件 -->\n<template>\n <CommentItem\n v-for=\"item in commentTree\"\n:key=\"item.id\"\n:comment=\"item\"\n @refresh=\"fetchComments\" <!-- 监听子组件的 refresh 事件 -->\n />\n</template>\n```\n\n\n\n### v-model 在组件上的用法（双向绑定组件）\n\n父组件用 `v-model` 绑定子组件，子组件通过 `modelValue` props 接收、`update:modelValue` 事件回传，实现父子双向同步。这是封装弹窗、输入组件的常用模式。\n\n**规则**：\n\n1. 子组件通过 `props` 接收固定字段 `modelValue`\n2. 子组件关闭弹窗时，派发 `update:modelValue` 事件修改父组件变量\n3. 最常用场景：弹窗显隐、自定义输入框、表单组件\n\n\n\n```\n 父组件 \t\t\t\t\t子组件\n┌───────────────┐ \t┌──────────────────────┐\n│ v-model=\"show\" │ ── modelValue ─→ │ props:modelValue │\n│ │ ←─ update:──── │ emit('update:modelValue')│\n└───────────────┘ modelValue \t└──────────────────────┘\n```\n\n**示例**：\n\n```vue\n<!-- common/ModalDemo.vue -->\n<template>\n <div v-if=\"modelValue\" class=\"mask\">\n <div class=\"modal-box\">\n <p>简易弹窗内容</p>\n <el-button @click=\"closeModal\">关闭弹窗</el-button>\n </div>\n </div>\n</template>\n\n<script setup>\nconst props = defineProps(['modelValue'])\nconst emit = defineEmits(['update:modelValue'])\n\n// 关闭弹窗，通知父组件修改值为false\nconst closeModal =()=>{\n emit('update:modelValue',false)\n}\n</script>\n\n<style scoped>\n.mask{\n position:fixed;\n inset:0;\n background:rgba(0,0,0,0.5);\n display:flex;\n align-items:center;\n justify-content:center;\n}\n.modal-box{\n background:#fff;\n padding:20px;\n border-radius:6px;\n}\n</style>\n```\n\n```vue\n<!-- CustomerListView03.vue -->\n<template>\n <div>\n <el-button @click=\"visible = true\">打开弹窗</el-button>\n <ModalDemo v-model=\"visible\" />\n </div>\n</template>\n\n<script setup>\nimport{ref}from 'vue'\nimport ModalDemo from './ModalDemo.vue'\nconst visible = ref(false)\n</script>\n```\n\n\n\n**CRM 使用场景**\n\n```vue\n<!-- 子组件 CustomerForm.vue（弹窗组件，简化） -->\n<script setup>\nimport{ref,watch}from 'vue'\n\nconst props = defineProps({\n modelValue:{type:Boolean,default:false},// 接收显示状态\n title:{type:String,default:'新增客户'}\n})\nconst emit = defineEmits(['update:modelValue','submit'])\n\n// 内部显示状态\nconst visible = ref(props.modelValue)\n// 监听父组件变化\nwatch(()=> props.modelValue,(val)=>{visible.value = val})\n// 内部变化时通知父组件\nwatch(visible,(val)=>{emit('update:modelValue',val)})\n</script>\n\n<template>\n <el-dialog v-model=\"visible\":title=\"title\">\n <!-- 表单内容 -->\n </el-dialog>\n</template>\n```\n\n```vue\n<!-- 父组件使用 v-model 控制弹窗 -->\n<template>\n <CustomerForm v-model=\"formVisible\":title=\"formTitle\" @submit=\"handleSubmit\" />\n</template>\n```\n\n### 2.5 slot 插槽\n\n插槽用于让父组件向子组件**插入内容**，实现内容分发。\n\n+ 作用：子组件预留空白占位区域，父组件可以自由传入任意文字、HTML、组件，实现通用容器自定义内容\n\n+ 基础：默认插槽\n\n+ 适用场景：通用卡片容器、弹窗布局\n\n\n\n**封装通用卡片容器 `CardDemo.vue`**\n\n```vue\n<!-- 子组件定义插槽 -->\n<template>\n <div class=\"card\">\n <h4>通用卡片标题</h4>\n <!-- 插槽坑位：父组件自定义填充内容 -->\n <slot>默认内容（父组件不传时显示）</slot>\n </div>\n</template>\n\n<style scoped>\n.card{\n border:1px solid #eee;\n padding:16px;\n border-radius:6px;\n width:300px;\n}\n</style>\n```\n\n**父组件自由填充内容 `CardListDemo.vue`**\n\n```vue\n<!-- 父组件填充插槽 -->\n<template>\n <div>\n <CardDemo>\n <p>第一条：客户信息资料</p>\n </CardDemo>\n\n <CardDemo>\n <ul>\n <li>待跟进客户</li>\n <li>已成交客户</li>\n </ul>\n </CardDemo>\n </div>\n</template>\n\n<script setup>\nimport CardDemo from './CardDemo.vue'\n</script>\n```\n\n**插槽分类**：\n\n| 类型 | 语法 | 用途 |\n|------|------|------|\n| 默认插槽 | `<slot />` | 单个内容插入点 |\n| 具名插槽 | `<slot name=\"header\" />` | 多个内容插入点 |\n| 作用域插槽 | `<slot:data=\"row\" />` | 子组件向父组件回传数据 |\n\n\n\n## Vue Router 4 基础\n\nVue Router 是 Vue.js 官方路由管理器，用于构建单页面应用（SPA）。核心概念：URL 地址和页面组件一一映射，切换地址页面无刷新跳转。\n\n\n\n**简易结构示意**：\n\n```\n/login → 登录页面组件\n/dashboard → 仪表盘页面组件\n```\n\n\n\n### 嵌套路由（后台管理系统必备架构）\n\n+ 后台页面统一结构：\n\n+ 外层固定布局外壳（侧边栏 + 顶部导航）为父路由\n\n+ 仪表盘、客户列表、用户管理等业务页面全部嵌套在布局内部\n\n依靠`children`配置嵌套子路由，布局内部`<router-view>`承载子页面。\n\n路由可以有 `children`，子路由渲染在父路由组件的 `<router-view />` 中。这是布局组件的核心机制。\n\n```\n/ 根路径\n└── 外层Layout布局组件\n ├─ /dashboard（子页面）\n └─ /customers（子页面）\n```\n\n```js\n{\n path:'/',\n component:Layout,// 父路由渲染布局组件\n redirect:'/dashboard',\n children:[ // 子路由渲染在布局组件的 <router-view /> 中\n{path:'dashboard',component:Dashboard},\n{path:'customers',component:CustomerList},\n{path:'customers/:id',component:CustomerDetail}\n ]\n}\n```\n\n### 动态路由参数\n\n路径中以 `:参数名` 形式定义动态段，组件内通过 `route.params.参数名` 获取。\n\n写法：`/customer/:id`\n\n场景：客户详情页共用一套页面模板，依靠 URL 携带客户唯一 ID，区分不同客户数据\n\n**示例**：\n\n```js\n// 路由定义\n{path:'customers/:id',name:'CustomerDetail',component:CustomerDetail}\n```\n\n```vue\n<!-- CustomerDetail.vue 中获取参数 -->\n<script setup>\nimport{useRoute}from 'vue-router'\nconst route = useRoute()\nconst customerId = computed(()=> route.params.id)// 获取:id\n</script>\n```\n\n编程式导航传参：\n```js\nrouter.push(`/customers/${row.id}`)// 路径方式\nrouter.push({name:'CustomerDetail',params:{id:row.id}})// 命名路由方式\n```\n\n### 路由守卫\n\n路由守卫在导航发生时拦截，常用于**登录验证**和**权限控制**。\n\n```\n导航触发\n │\n ▼\n┌─────────────────────────────────────┐\n│ beforeEach（全局前置守卫） \t\t\t\t\t\t │\n│ 1. 设置页面标题 \t\t\t\t\t\t │\n│ 2. 检查是否有 Token \t\t\t\t\t\t │\n│ ├─ 无 Token + 非白名单 → 跳登录 \t\t\t\t\t\t │\n│ ├─ 有 Token + 访问 login → 跳首页\t\t\t\t\t\t│\n│ └─ 有 Token → 检查角色权限 \t\t\t\t\t\t│\n└─────────────────────────────────────┘\n │\n ▼\n next()放行\n```\n\n**全局守卫类型**：\n\n| 守卫 | 时机 | 常见用途 |\n|------|------|---------|\n| `beforeEach` | 导航触发前 | 登录验证、权限检查 |\n| `afterEach` | 导航完成后 | 页面统计、结束加载条 |\n| `beforeResolve` | 组件内守卫之后 | 获取数据 |\n\n简易代码预览（先看结构，后续完整编写）\n\n```js\nrouter.beforeEach((to,from,next)=>{\n const token = localStorage.getItem('token')\n if(!token && to.path !== '/login'){\n next('/login')\n}else{\n next()\n}\n})\n```\n\n\n\n# CRM 项目实操\n\n## 项目初始化与目录规范\n\n### 项目目录结构（严格对照创建）\n\n```\nsrc\n├── components\n│ ├── common # 全局通用公共组件\n│ │ └── StatusTag.vue # 客户状态标签组件（props正式实操）\n│ └── business # 业务弹窗组件\n│ └── CustomerForm.vue # 客户新增弹窗（v-model双向绑定实操）\n├── layouts # 全局后台布局组件\n│ ├── DefaultLayout.vue # 布局总外壳\n│ ├── SideBar.vue # 左侧侧边菜单栏\n│ └── NavBar.vue # 顶部导航栏\n├── router\n│ ├── routes.js # 所有路由规则配置\n│ └── index.js # 路由实例创建 + 全局路由守卫\n├── stores\n│ └── app.js # Pinia全局状态：侧边栏折叠状态\n├── views # 所有业务页面\n ├── Login.vue # 登录页面（独立路由）\n ├── Dashboard.vue # 仪表盘首页\n └── customer\n ├── CustomerList.vue # 客户列表页面（调用新增弹窗）\n └── CustomerDetail.vue # 客户详情动态页面\n```\n\n### 依赖提前安装\n\n```bat\n# 安装路由、状态管理、UI组件库\nnpm install vue-router@4 pinia element-plus\n```\n\n\n\n### vite.config.js（配置 @路径别名，必加）\n\n```js\nimport{defineConfig}from 'vite'\nimport vue from '@vitejs/plugin-vue'\nimport{resolve}from 'path'\n\nexport default defineConfig({\n plugins:[vue()],\n resolve:{\n alias:{\n '@':resolve(__dirname,'src')\n}\n}\n})\n```\n\n\n\n### src/stores/app.js 侧边栏状态\n\n```js\nimport{defineStore}from 'pinia'\n\nexport const useAppStore = defineStore('app',{\n state:()=>({\n sidebarCollapsed:false\n}),\n actions:{\n toggleSidebar(){\n this.sidebarCollapsed = !this.sidebarCollapsed\n}\n}\n})\n```\n\n### src/router/routes.js 极简路由\n\n```js\n// 布局容器\nconst Layout =()=> import('@/layouts/DefaultLayout.vue')\n\nconst routes = [\n{\n path:'/',\n component:Layout,\n redirect:'/dashboard',\n children:[\n // 仪表盘首页（简单图表页面）\n{\n path:'dashboard',\n name:'Dashboard',\n component:()=> import('@/views/Dashboard.vue'),\n meta:{\n title:'首页仪表盘',\n icon:'Odometer'\n}\n},\n // 客户页面\n{\n path:'customer',\n name:'Customer',\n component:()=> import('@/views/Customer.vue'),\n meta:{\n title:'客户管理',\n icon:'User'\n}\n}\n ]\n},\n // 404兜底\n{\n path:'/:pathMatch(.*)*',\n component:()=> import('@/views/404.vue')\n}\n]\n\nexport default routes\n```\n\n### src/router/index.js 路由实例\n\n```js\nimport{createRouter,createWebHistory}from 'vue-router'\nimport routes from './routes'\n\nconst router = createRouter({\n history:createWebHistory(),\n routes,\n scrollBehavior:()=>({top:0})\n})\n\n// 简单设置网页标题\nrouter.beforeEach((to,from,next)=>{\n document.title = to.meta.title || 'CRM后台系统'\n next()\n})\n\nexport default router\n```\n\n## 组件设计 \n\n### src/layouts/DefaultLayout.vue 总布局容器\n\n```vue\n<template>\n <el-container style=\"height:100vh;\">\n <!-- 左侧侧边栏 -->\n <el-aside:width=\"sidebarWidth\" style=\"background:#304156\">\n <SideBar />\n </el-aside>\n\n <!-- 右侧区域：顶部栏 + 内容区 -->\n <el-container direction=\"vertical\">\n <el-header style=\"height:60px;background:#fff;box-shadow:0 1px 3px #eee;\">\n <NavBar />\n </el-header>\n <el-main style=\"background:#f5f7fa;padding:20px;\">\n <router-view />\n </el-main>\n </el-container>\n </el-container>\n</template>\n\n<script setup>\nimport{computed}from 'vue'\nimport SideBar from './SideBar.vue'\nimport NavBar from './NavBar.vue'\nimport{useAppStore}from '@/stores/app'\n\nconst appStore = useAppStore()\nconst sidebarWidth = computed(()=> appStore.sidebarCollapsed ? '64px':'220px')\n</script>\n```\n\n### src/layouts/SideBar.vue 左侧菜单\n\n```vue\n<template>\n <div>\n <!-- Logo -->\n <div style=\"height:60px;line-height:60px;text-align:center;color:#fff;background:#263445\">\n{{collapsed ? 'CRM':'CRM管理系统'}}\n </div>\n\n <el-menu\n:collapse=\"collapsed\"\n background-color=\"#304156\"\n text-color=\"#bfcbd9\"\n active-text-color=\"#409EFF\"\n router\n >\n <el-menu-item v-for=\"item in menuList\":key=\"item.path\":index=\"item.path\">\n <el-icon>\n <component:is=\"item.meta.icon\" />\n </el-icon>\n <template #title>{{item.meta.title}}</template>\n </el-menu-item>\n </el-menu>\n </div>\n</template>\n\n<script setup>\nimport{computed}from 'vue'\nimport{useRoute}from 'vue-router'\nimport{useAppStore}from '@/stores/app'\nimport routes from '@/router/routes'\n\nconst route = useRoute()\nconst appStore = useAppStore()\nconst collapsed = computed(()=> appStore.sidebarCollapsed)\n\n// 提取侧边栏菜单\nconst menuList = computed(()=>{\n const root = routes.find(r => r.path === '/')\n return root?.children || []\n})\n</script>\n```\n\n### src/layouts/NavBar.vue 顶部导航栏\n\n```vue\n<template>\n <div style=\"height:100%;display:flex;align-items:center;padding:0 20px;justify-content:space-between\">\n <!-- 折叠按钮 -->\n <el-button text @click=\"appStore.toggleSidebar()\">\n <el-icon size=\"20\"><Menu /></el-icon>\n </el-button>\n\n <!-- 面包屑 -->\n <el-breadcrumb separator=\"/\">\n <el-breadcrumb-item v-for=\"item in route.matched\":key=\"item.path\">\n{{item.meta.title}}\n </el-breadcrumb-item>\n </el-breadcrumb>\n\n <!-- 用户 -->\n <span style=\"display:flex;align-items:center;gap:6px\">\n <el-icon><User /></el-icon>\n 管理员\n </span>\n </div>\n</template>\n\n<script setup>\nimport{useRoute}from 'vue-router'\nimport{useAppStore}from '@/stores/app'\n\nconst route = useRoute()\nconst appStore = useAppStore()\n</script>\n```\n\n\n\n### src/views/Dashboard.vue 首页（简单图表展示）\n\n```vue\n<template>\n <div>\n <el-row:gutter=\"20\">\n <el-col:span=\"6\">\n <el-card shadow=\"hover\">\n <h3>总客户数</h3>\n <p style=\"font-size:26px;color:#409eff\">1258</p>\n </el-card>\n </el-col>\n <el-col:span=\"6\">\n <el-card shadow=\"hover\">\n <h3>今日新增</h3>\n <p style=\"font-size:26px;color:#67c23a\">36</p>\n </el-card>\n </el-col>\n <el-col:span=\"6\">\n <el-card shadow=\"hover\">\n <h3>待跟进</h3>\n <p style=\"font-size:26px;color:#e6a23c\">89</p>\n </el-card>\n </el-col>\n <el-col:span=\"6\">\n <el-card shadow=\"hover\">\n <h3>已流失</h3>\n <p style=\"font-size:26px;color:#f56c6c\">42</p>\n </el-card>\n </el-col>\n </el-row>\n\n <el-card style=\"margin-top:20px\" shadow=\"hover\">\n <h4>客户增长趋势图表区域</h4>\n <div style=\"height:300px;display:flex;align-items:center;justify-content:center;color:#999\">\n 此处可接入ECharts折线图\n </div>\n </el-card>\n </div>\n</template>\n```\n\n### src/views/Customer.vue 客户页面\n\n```vue\n<template>\n <el-card>\n <h3>客户管理页面</h3>\n <p>后续可添加表格、筛选、分页</p>\n </el-card>\n</template>\n```\n\n## 公共配置\n\n### src/views/404.vue\n\n```vue\n<template>\n <div style=\"text-align:center;padding-top:120px\">\n <h1>404 页面不存在</h1>\n <el-button type=\"primary\" @click=\"$router.push('/')\">返回首页</el-button>\n </div>\n</template>\n```\n\n### src/App.vue\n\n```vue\n<template>\n <router-view />\n</template>\n```\n\n### src/main.js\n\n```js\nimport{createApp}from 'vue'\nimport App from './App.vue'\nimport router from './router'\nimport{createPinia}from 'pinia'\nimport ElementPlus from 'element-plus'\nimport 'element-plus/dist/index.css'\nimport * as ElementPlusIconsVue from '@element-plus/icons-vue'\n\nconst app = createApp(App)\n\n// 全局注册图标\nfor(const key in ElementPlusIconsVue){\n app.component(key,ElementPlusIconsVue[key])\n}\n\napp.use(createPinia())\napp.use(ElementPlus)\napp.use(router)\n\napp.mount('#app')\n```\n\n\n\n## 目录结构对照\n\n```\nsrc\n├── layouts\n│ ├─ DefaultLayout.vue\n│ ├─ SideBar.vue\n│ └─ NavBar.vue\n├── router\n│ ├─ index.js\n│ └─ routes.js\n├── stores\n│ └─ app.js\n├── views\n│ ├─ Dashboard.vue\n│ ├─ Customer.vue\n│ └─ 404.vue\n├── App.vue\n└── main.js\n```\n\n\n\n# 本节项目目标\n搭建后台管理布局，配置完整路由体系，完成客户列表与详情页面骨架。\n\n## 实现步骤\n1. 创建 `src/components/common/StatusTag.vue` 公共组件，根据客户状态显示不同颜色标签\n2. 创建 `src/layouts/DefaultLayout.vue`，使用 el-container 搭建侧边栏 + 顶部栏 + 主内容区布局\n3. 创建 `src/layouts/SideBar.vue`，从路由表读取菜单数据，支持折叠与角色过滤\n4. 创建 `src/layouts/NavBar.vue`，包含折叠按钮、面包屑、用户下拉菜单\n5. 创建 `src/router/routes.js`，定义登录页、布局嵌套路由（仪表盘/客户列表/客户详情/跟进/用户管理）、404 页面\n6. 创建 `src/router/index.js`，创建路由实例并编写 `beforeEach` 全局守卫（登录拦截 + 权限校验）\n7. 创建 `src/stores/app.js`，管理侧边栏折叠状态\n8. 创建 `src/views/customer/CustomerList.vue` 页面骨架（表格 + 筛选栏 + 分页结构）\n9. 创建 `src/views/customer/CustomerDetail.vue` 页面骨架，通过 `route.params.id` 获取客户ID\n10. 测试：未登录访问 `/customers` 应跳转登录页，登录后菜单与路由正常切换\n\n# AI 辅助编程实践\n\n## 本节 AI 实践主题\n用 AI 生成路由配置和布局组件，学习结构化 Prompt 模板。\n\n## 工具\nTrae / ChatGPT / Claude 等代码生成 AI 工具。\n\n## 场景\n后台系统的路由表结构相似（每个页面都是一条路由记录），布局组件结构固定。可以通过结构化 Prompt 让 AI 一次性生成完整路由配置。\n\n## Prompt 示例\n\n```\n你是一位资深 Vue 3 前端工程师。请帮我生成路由配置文件 router/routes.js，\n技术要求如下：\n\n1. 使用 Vue Router 4，路由懒加载（()=> import 方式）\n2. 布局组件路径：@/layouts/DefaultLayout.vue\n3. 需要配置以下路由：\n - /login 登录页（@/views/login/LoginView.vue），无需鉴权\n - / 布局路由，重定向到 /dashboard，子路由包括：\n * dashboard 仪表盘（icon:Odometer）\n * customers 客户列表（icon:User）\n * customers/:id 客户详情（hidden:true,activeMenu:/customers）\n * follow-ups 跟进记录（icon:ChatLineRound）\n * users 用户管理（icon:UserFilled,roles:['admin']）\n - /:pathMatch(.*)* 404 页面\n4. 每条路由的 meta 包含 title、icon、requiresAuth、roles 等字段\n5. 导出 routes 数组，同时 default 导出\n\n请输出完整代码，中文注释。\n```\n\n## 实践要点\n- Prompt 中列出所有路由的路径、组件、meta 字段，避免 AI 遗漏\n- 明确懒加载写法（`()=> import(...)`），避免 AI 生成同步导入导致首屏过大\n- 生成后检查：路径是否正确、meta 字段是否完整、动态路由参数格式是否正确\n- 布局组件可让 AI 先生成结构骨架（template + script 框架），再补充业务逻辑\n\n\n\n# 课堂练习\n\n**练习 1**（基础）：编写一个 `StatusTag.vue` 组件，接收 `status` props，根据状态值（lead/following/contracted/lost）显示不同颜色的 `el-tag`。在父组件中用 `v-for` 渲染一个客户列表，每行使用该组件显示状态。\n\n思路提示：`defineProps({status:String})`，用 `computed` 根据 status 映射 tag 的 type（info/warning/success/danger）。\n\n**练习 2**（中等）：配置一个包含动态路由参数的路由 `users/:id`，创建 `UserDetail.vue` 页面，通过 `useRoute()` 获取 `id` 参数并显示在页面上。再编写一个列表页，点击列表项用 `router.push()` 跳转到详情页。\n\n思路提示：\n- 路由定义：`{path:'users/:id',name:'UserDetail',component:()=> import(...)}`\n- 详情页：`const route = useRoute();const userId = computed(()=> route.params.id)`\n- 列表页跳转：`router.push('/users/' + row.id)` 或 `router.push({name:'UserDetail',params:{id:row.id}})`\n\n\n\n# 课后作业\n\n## 必做\n1. 完成 `DefaultLayout.vue`、`SideBar.vue`、`NavBar.vue` 三个布局组件\n2. 完成 `router/routes.js` 和 `router/index.js`，确保路由守卫能拦截未登录访问\n3. 完成 `StatusTag.vue` 公共组件并在客户列表中使用\n4. 创建 `CustomerList.vue` 和 `CustomerDetail.vue` 页面骨架，确保路由跳转正常\n\n## 选做（进阶）\n1. 在 `DefaultLayout.vue` 中添加 `<keep-alive>` 缓存客户列表页，观察切换详情再返回时筛选条件是否保留\n2. 用 AI 生成 `NavBar.vue` 的完整代码（含面包屑、用户下拉菜单、退出登录功能）\n\n## 提交方式\n附上路由切换截图（登录页 → 仪表盘 → 客户列表 → 客户详情）、未登录被拦截跳转登录页的截图。\n\n# 本节知识图谱\n\n```mermaid\ngraph LR\n A[Vue 组件与路由] --> B[组件化]\n A --> C[组件通信]\n A --> D[Vue Router 4]\n B --> B1[组件拆分]\n B --> B2[递归组件]\n C --> C1[props 父→子]\n C --> C2[emit 子→父]\n C --> C3[v-model 组件]\n C --> C4[slot 插槽]\n D --> D1[路由表 routes]\n D --> D2[嵌套路由 children]\n D --> D3[动态路由:id]\n D --> D4[路由守卫 beforeEach]\n D --> D5[权限控制 meta.roles]\n```\n\n## 十、下节预告\n\n- 下一节主题：Axios 与 API 交互\n- 需要预习的内容：\n - 复习 HTTP 请求方法（GET/POST/PUT/DELETE）与状态码（200/401/403/500）\n - 思考：前端如何携带 JWT Token 发送请求？Token 过期后如何自动刷新？\n - 预览项目中 `utils/request.js` 和 `api/customer.js` 的结构\n - 了解 Promise / async-await 异步编程基础\n","order":8},{"chapterName":"09-Axios与API交互","content":"# Day 9 - Axios 与 API 交互\n\n## 学习目标\n\n1. **知识目标**：理解 Axios 的工作原理与核心配置，掌握请求/响应拦截器机制，理解 JWT Token 自动注入与无感刷新原理\n2. **技能目标**：能封装 Axios 实例并配置拦截器，能统一处理业务错误码与 HTTP 状态码，能按模块封装 API 层并在组件中调用\n3. **项目目标**：封装 `utils/request.js`（拦截器 + Token 注入 + 401 自动刷新），创建 `api/customer.js`，在 `CustomerList.vue` 中调用后端 API 展示客户列表\n4. **AI 实践目标**：用 AI 生成 Axios 拦截器代码，学习 API 封装 Prompt 模板\n\n## 知识点铺垫\n\n### Axios 基础\n\nAxios 是一个基于 Promise 的 HTTP 客户端，支持浏览器和 Node.js。相比原生 `fetch`，它自带请求/响应拦截器、自动 JSON 转换、超时控制、取消请求等能力。\n\n```\n┌─────── Axios 请求流程 ────────────┐\n│ │\n│ 组件调用 api.xxx()│\n│ │ │\n│ ▼ │\n│ 请求拦截器（注入 Token、设置 header） \t│\n│ │ │\n│ ▼ │\n│ 发送 HTTP 请求 → Django 后端 │\n│ │ │\n│ ▼ │\n│ 响应拦截器（处理 code、错误提示、401 刷新） \t │\n│ │ │\n│ ▼ │\n│ 返回数据给组件 │\n│ │\n└─────────────────────────────┘\n```\n\n核心配置项：\n\n| 配置 | 说明 | 项目取值 |\n|------|------|---------|\n| `baseURL` | 请求基础地址 | `/api`（由环境变量 `VITE_API_BASE_URL` 提供） |\n| `timeout` | 超时时间（ms） | `15000`（15 秒） |\n| `headers` | 请求头 | 拦截器中动态注入 `Authorization` |\n\n**示例**:\n\n```js\n// src/utils/request.js\n// Axios 请求实例，含请求/响应拦截器\nimport axios from 'axios'\n\n// 需提前创建.env.development 内容如下：VITE_API_BASE_URL=/api\n// 基础地址，从环境变量读取 \n\nconst baseURL = import.meta.env.VITE_API_BASE_URL || '/api'\n\n// 创建 axios 实例\nconst service = axios.create({\n baseURL,\n timeout:15000\n})\n```\n\n\n\n### 请求拦截器\n\n请求拦截器在**发送请求前**执行，常用于自动注入 Token、统一设置请求头、添加 loading 等。\n\n```\n请求拦截器\n │\n ├── 从 localStorage 读取 Token\n ├── Token 存在 → headers['Authorization'] = 'Bearer xxx'\n └── 返回 config，继续发送请求\n```\n\n```js\nservice.interceptors.request.use(\n(config)=>{\n // getToken()需要自行编写，从 localStorage 读取 Token\n const token = getToken()\n if(token){\n config.headers['Authorization'] = `Bearer ${token}`\n}\n return config\n},\n(error)=> Promise.reject(error)\n)\n```\n\n要点：\n- JWT 的标准格式是 `Bearer <token>`，前缀 `Bearer` 表示持有者令牌\n- Token 从 `localStorage` 读取（见 `utils/auth.js`），刷新页面后不丢失\n- 即使请求不需要 Token，注入也无害（后端会忽略）\n\n### 响应拦截器\n\n响应拦截器在**收到响应后**执行，用于统一处理业务状态码、错误提示、Token 过期刷新。\n\n```\n响应拦截器\n │\n ├── 成功响应（HTTP 2xx）\n │ ├── 判断后端业务码 res.code\n │ │ ├── code === 200 → 返回 res（正常数据）\n │ │ └── code !== 200 → ElMessage.error 提示，reject\n │ └── 文件流（blob）→ 直接返回 response\n │\n └── 失败响应（HTTP 4xx/5xx）\n ├── 401 → 尝试用 refreshToken 刷新 Token\n │ ├── 刷新成功 → 重放原请求\n │ └── 刷新失败 → 清除 Token，跳转登录\n ├── 403 → 提示\"没有权限\"\n └── 其他 → ElMessage.error 提示错误信息\n```\n\n后端统一响应格式：\n```json\n{\n \"code\":200,\n \"msg\":\"success\",\n \"data\":{...}\n}\n```\n\n| 业务码 | 含义 | 处理方式 |\n|--------|------|---------|\n| 200 | 成功 | 返回 `res` 给组件 |\n| 其他 | 业务错误 | `ElMessage.error(res.msg)` 提示并 reject |\n\n**示例**：\n\n```js\nservice.interceptors.response.use(\n(response)=>{\n const res = response.data\n // 处理文件流等非 JSON 响应\n if(response.config.responseType === 'blob'){\n return response\n}\n // 后端统一返回{code,msg,data}\n if(res.code === 200){\n return res // 业务成功，返回完整响应体\n}\n // 业务错误：弹出提示并 reject\n ElMessage.error(res.msg || '请求失败')\n return Promise.reject(new Error(res.msg || 'Error'))\n},\n(error)=> Promise.reject(error)\n)\n```\n\n\n\n### Token 自动注入与无感刷新\n\n```\n┌─────────── Token 无感刷新流程 ───────────┐\n│ \t\t\t │\n│ 请求A → 401（access token 过期） \t\t\t │\n│ │ \t\t\t │\n│ ├── 有 refreshToken？ \t\t │\n│ │ ├── 是 → 用 refreshToken 换新 \t\t\t │\n│ │ │ access token \t\t\t │\n│ │ │ ├── 成功 → 更新本地存储 \t\t │\n│ │ │ │ 重放请求A \t\t\t │\n│ │ │ └── 失败 → 跳转登录 \t\t │\n│ │ └── 否 → 跳转登录 \t\t\t│\n│ │ \t\t\t │\n│ 请求B 同时到达 401（刷新进行中） \t\t\t │\n│ └── 加入队列等待，刷新后统一重放 \t\t\t│\n│ \t\t │\n└─────────────────────────────────┘\n```\n\n**双 Token 机制**：\n\n- **access token**：短期有效（如 15 分钟），用于业务请求鉴权\n- **refresh token**：长期有效（如 7 天），仅用于换取新的 access token\n\n**无感刷新的关键点**：\n\n- 用 `isRefreshing` 标记防止并发重复刷新\n- 刷新期间的请求加入 `requestsQueue` 队列，刷新成功后统一重放\n\n**示例**：\n\n```js\nservice.interceptors.response.use(\n(response)=>{\n const res = response.data\n ....\n},\n async(error)=>{\n const{response,config}= error\n // 401 未授权：尝试刷新 token\n if(response && response.status === 401 && !config._retry){\n // 刷新token，需要自行编写从\n const refreshToken = getRefreshToken()\n if(refreshToken && !isRefreshing){\n isRefreshing = true\n config._retry = true\n try{\n // 使用原始 axios 发起刷新请求，避免循环\n const{data}= await axios.post(`${baseURL}/token/refresh/`,{\n refresh:refreshToken\n})\n const newAccess = data.access || data.data?.access\n const newRefresh = data.refresh || refreshToken\n \n // 更新本地存储的 token，自行编写\n setToken(newAccess,newRefresh)\n // 重放队列中的请求\n requestsQueue.forEach((cb)=> cb(newAccess))\n requestsQueue = []\n // 重试原请求\n config.headers['Authorization'] = `Bearer ${newAccess}`\n return service(config)\n}catch(refreshError){\n // 刷新失败，清理并跳转登录\n removeToken()\n requestsQueue = []\n ElMessageBox.confirm('登录状态已过期，请重新登录','提示',{\n confirmButtonText:'重新登录',\n cancelButtonText:'取消',\n type:'warning'\n}).then(()=>{\n window.location.href = '/login'\n})\n return Promise.reject(refreshError)\n}finally{\n isRefreshing = false\n}\n}else if(isRefreshing){\n // 正在刷新，将请求加入队列等待\n return new Promise((resolve)=>{\n requestsQueue.push((token)=>{\n config.headers['Authorization'] = `Bearer ${token}`\n resolve(service(config))\n})\n})\n}\n}\n\n // 403 无权限\n if(response && response.status === 403){\n ElMessage.error('没有权限执行此操作')\n return Promise.reject(error)\n}\n\n // 其他错误\n const message =\n response?.data?.msg || response?.data?.detail || error.message || '网络异常'\n ElMessage.error(message)\n return Promise.reject(error)\n}\n)\n```\n\n\n\n### 统一错误处理\n\n| HTTP 状态码 | 含义 | 前端处理 |\n|------------|------|---------|\n| 200 | 成功 | 检查业务 code |\n| 401 | 未授权 | 尝试刷新 Token，失败则跳登录 |\n| 403 | 禁止访问 | 提示\"没有权限执行此操作\" |\n| 404 | 未找到 | 提示错误信息 |\n| 500 | 服务器错误 | 提示\"网络异常\" |\n\n统一处理的好处：组件中无需重复写 `try/catch` 错误提示逻辑，拦截器统一兜底。\n\n\n\n### API 层封装\n\n将每个后端接口封装为独立函数，按业务模块组织。组件不直接调用 `request`，而是调用 API 模块函数。\n\n```\nsrc/\n├── api/\n│ ├── index.js # 统一出口\n│ ├── customer.js # 客户模块\n│ ├── user.js # 用户模块\n│ ├── followUp.js # 跟进模块\n│ ├── comment.js # 评论模块\n│ └── statistics.js # 统计模块\n└── utils/\n └── request.js # Axios 实例\n```\n\n```js\n// api/customer.js\nimport request from '@/utils/request'\n\nexport function getCustomerList(params){\n return request({url:'/customers/',method:'get',params})\n}\n```\n\n```js\n// 组件中调用\nimport{getCustomerList}from '@/api/customer'\nconst res = await getCustomerList({page:1,page_size:10})\n```\n\n要点：\n- API 函数返回的是拦截器处理后的 `res`（即 `{code,msg,data}`）\n- 组件中通过 `res.data` 获取实际业务数据\n- 按模块拆分便于维护和查找\n\n### loading 状态管理\n\n在数据请求期间显示加载状态，提升用户体验。\n\n```vue\n<template>\n <!-- v-loading 绑定 loading 变量 -->\n <el-table v-loading=\"loading\":data=\"list\">\n ...\n </el-table>\n</template>\n\n<script setup>\nconst loading = ref(false)\n\nasync function fetchList(){\n loading.value = true // 请求前开启\n try{\n const res = await getCustomerList(params)\n list.value = res.data.results\n}finally{\n loading.value = false // 无论成功失败都关闭\n}\n}\n</script>\n```\n\n\n\n## 核心代码示例\n\n### utils/auth.js Token 存取工具\n\n以下为项目实际的 `src/utils/auth.js`，基于 `localStorage` 实现 Token 的存取：\n\n```js\n// src/utils/auth.js\n// Token 存取工具，基于 localStorage\n\nconst ACCESS_TOKEN_KEY = 'crm_access_token'\nconst REFRESH_TOKEN_KEY = 'crm_refresh_token'\n\n// 获取 access token\nexport function getToken(){\n return localStorage.getItem(ACCESS_TOKEN_KEY)\n}\n\n// 获取 refresh token\nexport function getRefreshToken(){\n return localStorage.getItem(REFRESH_TOKEN_KEY)\n}\n\n// 同时保存 access 和 refresh token\nexport function setToken(accessToken,refreshToken = ''){\n if(accessToken){\n localStorage.setItem(ACCESS_TOKEN_KEY,accessToken)\n}\n if(refreshToken){\n localStorage.setItem(REFRESH_TOKEN_KEY,refreshToken)\n}\n}\n\n// 清除所有 token\nexport function removeToken(){\n localStorage.removeItem(ACCESS_TOKEN_KEY)\n localStorage.removeItem(REFRESH_TOKEN_KEY)\n}\n```\n\n要点：\n- 使用 `localStorage` 而非 `sessionStorage`，关闭浏览器后 Token 仍保留\n- access token 和 refresh token 分别存储，键名用常量管理避免拼写错误\n\n\n\n### utils/request.js 完整封装\n\n以下为项目实际的 `src/utils/request.js`，包含实例创建、请求拦截器（注入 Bearer Token）、响应拦截器（处理 code != 200 和 401 跳登录/刷新）：\n\n```js\n// src/utils/request.js\n// Axios 请求实例，含请求/响应拦截器\nimport axios from 'axios'\nimport{ElMessage,ElMessageBox}from 'element-plus'\nimport{getToken,getRefreshToken,removeToken,setToken}from './auth'\n\n// 基础地址，从环境变量读取\nconst baseURL = import.meta.env.VITE_API_BASE_URL || '/api'\n\n// 创建 axios 实例\nconst service = axios.create({\n baseURL,\n timeout:15000\n})\n\n// 请求拦截器：自动注入 JWT Token\nservice.interceptors.request.use(\n(config)=>{\n const token = getToken()\n if(token){\n // 将 Token 注入到请求头，格式为 Bearer <token>\n config.headers['Authorization'] = `Bearer ${token}`\n}\n return config\n},\n(error)=>{\n return Promise.reject(error)\n}\n)\n\n// 标记是否正在刷新 token，避免重复刷新\nlet isRefreshing = false\n// 等待 token 刷新的请求队列\nlet requestsQueue = []\n\n// 响应拦截器：统一处理业务状态码与 401\nservice.interceptors.response.use(\n(response)=>{\n const res = response.data\n // 处理文件流等非 JSON 响应\n if(response.config.responseType === 'blob'){\n return response\n}\n // 后端统一返回{code,msg,data}\n if(res.code === 200){\n return res // 业务成功，返回完整响应体\n}\n // 业务错误：弹出提示并 reject\n ElMessage.error(res.msg || '请求失败')\n return Promise.reject(new Error(res.msg || 'Error'))\n},\n async(error)=>{\n const{response,config}= error\n // 401 未授权：尝试刷新 token\n if(response && response.status === 401 && !config._retry){\n const refreshToken = getRefreshToken()\n if(refreshToken && !isRefreshing){\n isRefreshing = true\n config._retry = true\n try{\n // 使用原始 axios 发起刷新请求，避免循环\n const{data}= await axios.post(`${baseURL}/token/refresh/`,{\n refresh:refreshToken\n})\n const newAccess = data.access || data.data?.access\n const newRefresh = data.refresh || refreshToken\n // 更新本地存储的 token\n setToken(newAccess,newRefresh)\n // 重放队列中的请求\n requestsQueue.forEach((cb)=> cb(newAccess))\n requestsQueue = []\n // 重试原请求\n config.headers['Authorization'] = `Bearer ${newAccess}`\n return service(config)\n}catch(refreshError){\n // 刷新失败，清理并跳转登录\n removeToken()\n requestsQueue = []\n ElMessageBox.confirm('登录状态已过期，请重新登录','提示',{\n confirmButtonText:'重新登录',\n cancelButtonText:'取消',\n type:'warning'\n}).then(()=>{\n window.location.href = '/login'\n})\n return Promise.reject(refreshError)\n}finally{\n isRefreshing = false\n}\n}else if(isRefreshing){\n // 正在刷新，将请求加入队列等待\n return new Promise((resolve)=>{\n requestsQueue.push((token)=>{\n config.headers['Authorization'] = `Bearer ${token}`\n resolve(service(config))\n})\n})\n}\n}\n\n // 403 无权限\n if(response && response.status === 403){\n ElMessage.error('没有权限执行此操作')\n return Promise.reject(error)\n}\n\n // 其他错误\n const message =\n response?.data?.msg || response?.data?.detail || error.message || '网络异常'\n ElMessage.error(message)\n return Promise.reject(error)\n}\n)\n\nexport default service\n```\n\n要点：\n- **请求拦截器**：从 `localStorage` 读取 access token，注入到 `Authorization` 请求头\n- **响应拦截器-成功**：判断后端 `code === 200` 才算成功，否则视为业务错误并提示\n- **响应拦截器-401**：用 refresh token 换取新的 access token，成功后重放原请求，实现无感刷新\n- **并发控制**：`isRefreshing` 标记 + `requestsQueue` 队列，确保刷新期间其他 401 请求排队等待而非各自刷新\n- **刷新失败**：清除 Token，弹窗提示用户重新登录\n- 刷新请求用**原始 `axios`** 而非 `service` 实例，避免触发自身拦截器造成死循环\n\n\n\n### api/customer.js 客户模块封装\n\n以下为项目实际的 `src/api/customer.js`，封装客户的增删改查接口：\n\n```js\n// src/api/customer.js\n// 客户相关 API\nimport request from '@/utils/request'\n\n// 获取客户列表（支持 search/status/source/owner/page/page_size 筛选）\nexport function getCustomerList(params){\n return request({\n url:'/customers/',\n method:'get',\n params // GET 请求参数，拼接在 URL 上\n})\n}\n\n// 获取客户详情\nexport function getCustomerDetail(id){\n return request({\n url:`/customers/${id}/`,\n method:'get'\n})\n}\n\n// 新增客户\nexport function createCustomer(data){\n return request({\n url:'/customers/',\n method:'post',\n data // POST 请求体\n})\n}\n\n// 更新客户（整体更新）\nexport function updateCustomer(id,data){\n return request({\n url:`/customers/${id}/`,\n method:'put',\n data\n})\n}\n\n// 部分更新客户\nexport function patchCustomer(id,data){\n return request({\n url:`/customers/${id}/`,\n method:'patch',\n data\n})\n}\n\n// 删除客户\nexport function deleteCustomer(id){\n return request({\n url:`/customers/${id}/`,\n method:'delete'\n})\n}\n```\n\n要点：\n- GET 请求用 `params`（拼接到 URL query string），POST/PUT/PATCH 用 `data`（请求体）\n- RESTful 风格：列表 `/customers/`，详情/更新/删除 `/customers/:id/`\n- 每个函数对应一个后端接口，命名语义化（`getCustomerList` 而非 `getList`）\n\n### API 统一出口\n\n```js\n// src/api/index.js\n// API 模块统一出口\nexport * from './user'\nexport * from './customer'\nexport * from './followUp'\nexport * from './comment'\nexport * from './statistics'\n```\n\n使用时可直接从 `@/api` 统一导入，也可从具体模块导入：\n```js\n// 方式一：从统一出口导入\nimport{getCustomerList,login}from '@/api'\n\n// 方式二：从具体模块导入（推荐，更清晰）\nimport{getCustomerList}from '@/api/customer'\n```\n\n\n\n### usePagination 分页组合式函数\n\n以下为项目实际的 `src/composables/usePagination.js`，封装分页加载逻辑，供多个列表页复用：\n\n```js\n// src/composables/usePagination.js\n// 分页逻辑复用 composable\nimport{ref,reactive}from 'vue'\n\n/**\n * 分页逻辑复用\n * @param{Function}fetchFn 获取数据的函数，接收 params 参数\n * @param{Object}options 初始配置\n */\nexport function usePagination(fetchFn,options ={}){\n // 分页参数\n const pagination = reactive({\n page:1,\n page_size:options.pageSize || 10,\n total:0\n})\n\n // 数据列表\n const list = ref([])\n // 加载状态\n const loading = ref(false)\n\n // 加载数据\n async function loadData(extraParams ={}){\n loading.value = true\n try{\n const params ={\n page:pagination.page,\n page_size:pagination.page_size,\n ...extraParams\n}\n const res = await fetchFn(params)\n // 兼容后端分页格式{count,next,previous,results}\n if(res.data && res.data.results){\n list.value = res.data.results\n pagination.total = res.data.count || 0\n}else if(Array.isArray(res.data)){\n list.value = res.data\n pagination.total = res.data.length\n}else{\n list.value = res.data?.list || []\n pagination.total = res.data?.total || 0\n}\n return res\n}finally{\n loading.value = false\n}\n}\n\n // 页码改变\n function handleCurrentChange(page){\n pagination.page = page\n}\n\n // 每页条数改变\n function handleSizeChange(size){\n pagination.page_size = size\n pagination.page = 1\n}\n\n // 重置到第一页\n function resetPage(){\n pagination.page = 1\n}\n\n return{\n pagination,\n list,\n loading,\n loadData,\n handleCurrentChange,\n handleSizeChange,\n resetPage\n}\n}\n```\n\n**要点**：\n\n- `usePagination` 接收一个 API 函数（如 `getCustomerList`），返回分页所需的状态和方法\n- 兼容多种后端分页响应格式（`results`/数组/`list`）\n- `loading` 在 `try...finally` 中管理，确保异常时也能关闭加载状态\n- 多个列表页（客户、跟进、用户）复用同一逻辑，减少重复代码\n\n\n\n### CustomerList.vue 中调用 API\n\n以下为项目 `CustomerList.vue` 中调用后端 API 展示客户列表的核心代码，配合 `usePagination` 组合式函数实现分页加载：\n\n```vue\n<template>\n <div class=\"customer-list page-container\">\n <!-- 搜索筛选栏 -->\n <!-- 列表 -->\n <div class=\"list-card\">\n <el-table v-loading=\"loading\" border:data=\"list\" style=\"width:100%\">\n <el-table-column type=\"index\" label=\"编号\" width=\"55\" align=\"center\" />\n <el-table-column prop=\"name\" label=\"客户名称\" min-width=\"120\" align=\"center\" />\n <<el-table-column prop=\"phone\" label=\"联系电话\" min-width=\"120\" align=\"center\" />\n <el-table-column prop=\"company\" label=\"公司\" min-width=\"120\" align=\"center\" />\n <el-table-column prop=\"status\" label=\"状态\" min-width=\"120\" align=\"center\" />\n <el-table-column label=\"操作\" min-width=\"120\" fixed=\"right\" align=\"center\">\n <!-- 从scope中解构出每行对象 -->\n <template #default=\"{row}\">\n <el-button type=\"primary\" link size=\"small\" @click=\"goDetail(row)\">详情</el-button>\n <el-button type=\"warning\" link size=\"small\" @click=\"handleEdit(row)\">编辑</el-button>\n <el-button type=\"danger\" link size=\"small\" @click=\"handleDelete(row)\">删除</el-button>\n </template>\n </el-table-column>\n </el-table>\n </div>\n <!-- 分页 -->\n <div class=\"pagination-wrap\">\n <el-pagination\n v-model:current-page=\"pagination.page\"\n v-model:page-size=\"pagination.page_size\"\n:total=\"pagination.total\"\n:page-sizes=\"[3,5,10,20]\"\n layout=\"total,sizes,prev,pager,next,jumper\"\n background\n @size-change=\"onSizeChange\"\n @current-change=\"onCurrentChange\"\n />\n </div>\n </div>\n</template>\n<script setup>\n import{ref,reactive,onMounted}from 'vue';\n // 导入客户 API\n import{getCustomerList,createCustomer,updateCustomer,deleteCustomer}from '@/api/customer';\n\n // 数据列表\n // const list = ref([])\n // 加载数据： 到后端请求数据并封装结果\n async function fetchList(){\n // const res = await getCustomerList()\n // list.value = res.data.results\n await loadData({\n // 筛选条件\n search:filters.search,\n status:filters.status,\n source:filters.source,\n});\n}\n\n // 页面加载时获取数据\n onMounted(()=>{\n fetchList();\n});\n\n // 分页查询，带分页封装\n // 封装请求函数，当调用loadData 时，获取结果封装为分页与数据\n import{usePagination}from '@/composables/usePagination';\n const{pagination,loading,loadData,handleCurrentChange,handleSizeChange,resetPage}=\n usePagination(getCustomerList);\n\n // === 分页条功能：监听 page,page_size 的变化来发起新的分页请求\n watch(\n()=> [pagination.page,pagination.page.page_size],\n()=>{\n fetchList();\n}\n);\n</script>\n\n<style scoped>\n .page-container{\n padding:16px 20px;\n}\n .pagination-wrap{\n display:flex;\n justify-content:flex-end;\n margin-top:16px;\n}\n</style>\n```\n\n\n\n### vite.config.js 开发代理配置\n\n以下为项目实际的 `vite.config.js` 中代理部分，将 `/api` 请求转发到 Django 后端，解决跨域问题：\n\n```js\n// vite.config.js（代理部分）\nimport{defineConfig}from 'vite';\nimport vue from '@vitejs/plugin-vue';\nimport{resolve}from 'path';\n\nexport default defineConfig({\n plugins:[vue()],\n resolve:{\n alias:{\n '@':resolve(__dirname,'src'),\n},\n},\n server:{\n host:'0.0.0.0',\n port:5173,\n open:false,\n proxy:{\n // 将 /api 请求代理到 Django 后端\n '/api':{\n target:'http://127.0.0.1:8000',// Django 开发服务器地址\n changeOrigin:true\n}\n}\n}\n});\n```\n\n```\n开发环境请求链路：\n\n 浏览器 http://localhost:5173\n │\n │ 请求 /api/customers/\n ▼\n Vite 开发服务器(5173)\n │\n │ proxy 转发\n ▼\n Django 后端(8000)\n │\n │ 返回 JSON\n ▼\n Vite → 浏览器\n```\n\n要点：\n- 前端请求 `/api/customers/`，Vite 代理转发到 `http://127.0.0.1:8000/api/customers/`\n- `changeOrigin:true` 修改请求头中的 Host，避免后端拒绝\n- 开发环境用代理避免跨域，生产环境通过 Nginx 反向代理解决\n\n## CRM 项目实战\n\n### 本节项目目标\n封装 Axios 请求层，实现 Token 自动注入与无感刷新，完成客户列表页与后端 API 的数据交互。\n\n### 实现步骤\n1. 创建 `src/utils/auth.js`，封装 access token 和 refresh token 的存取（基于 localStorage）\n2. 创建 `src/utils/request.js`，创建 Axios 实例（baseURL 从环境变量读取、timeout 15s）\n3. 编写**请求拦截器**：从 localStorage 读取 token，注入 `Authorization:Bearer xxx` 请求头\n4. 编写**响应拦截器**：\n - 成功响应判断 `res.code === 200`，否则 `ElMessage.error` 提示业务错误\n - 401 响应尝试用 refresh token 刷新，成功后重放原请求；失败则清除 token 跳转登录\n - 403 提示\"没有权限\"，其他错误统一 `ElMessage.error` 提示\n5. 创建 `src/api/customer.js`，封装 `getCustomerList`/`getCustomerDetail`/`createCustomer`/`updateCustomer`/`deleteCustomer`\n6. 创建 `src/api/index.js` 统一出口\n7. 创建 `src/composables/usePagination.js`，封装分页加载逻辑\n8. 在 `CustomerList.vue` 中导入 API 和 usePagination，实现 `onMounted` 加载、筛选查询、分页切换\n9. 配置 `vite.config.js` 的 `/api` 代理到 `http://127.0.0.1:8000`\n10. 启动 Django 后端和 Vite 前端，验证客户列表数据正常加载、筛选和分页\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n用 AI 生成 Axios 拦截器代码，学习 API 封装 Prompt 模板。\n\n### 工具\nTrae / ChatGPT / Claude 等代码生成 AI 工具。\n\n### 场景\n拦截器逻辑较复杂（含 Token 刷新、队列管理、错误处理），可让 AI 生成基础框架后再补充细节。API 模块结构高度相似，适合用模板化 Prompt 批量生成。\n\n### Prompt 示例（拦截器生成）\n\n```\n你是一位资深前端工程师。请帮我用 Axios 封装一个 HTTP 请求实例\nutils/request.js，要求如下：\n\n1. 基于 Axios 创建实例，baseURL 从环境变量 VITE_API_BASE_URL 读取，\n 默认 /api，timeout 15000ms\n2. 请求拦截器：从 localStorage 读取 access token（key 为 crm_access_token），\n 存在则注入请求头 Authorization:Bearer <token>\n3. 响应拦截器：\n - 成功响应：后端返回{code,msg,data}，code===200 返回 res，\n 否则 ElMessage.error 提示 msg 并 reject\n - 文件流（responseType 为 blob）直接返回\n - 401 错误：用 refresh token（key 为 crm_refresh_token）请求\n /token/refresh/ 换取新 token，成功后重放原请求\n - 刷新期间其他 401 请求加入队列等待，刷新成功后统一重放\n - 刷新失败：清除 token，弹窗提示重新登录并跳转 /login\n - 403：提示\"没有权限执行此操作\"\n - 其他错误：统一 ElMessage.error 提示\n4. 使用 Element Plus 的 ElMessage / ElMessageBox\n5. 中文注释，代码完整可运行\n```\n\n### Prompt 示例（API 模块生成）\n\n```\n你是一位资深前端工程师。请根据以下后端接口列表，帮我生成\napi/customer.js 封装文件。\n\n后端接口（RESTful 风格，统一前缀 /api）：\n- GET /customers/ 获取客户列表（支持 search/status/source/page/page_size 参数）\n- GET /customers/:id/ 获取客户详情\n- POST /customers/ 新增客户\n- PUT /customers/:id/ 更新客户\n- PATCH /customers/:id/ 部分更新客户\n- DELETE /customers/:id/ 删除客户\n\n要求：\n1. 导入 request 实例：import request from '@/utils/request'\n2. 每个接口封装为独立 export function，命名语义化（getCustomerList 等）\n3. GET 请求用 params，POST/PUT/PATCH 用 data\n4. 中文注释说明每个函数的用途和参数\n```\n\n### 实践要点\n- 生成拦截器后重点检查：Token 刷新的死循环防护（`config._retry` 标记）、队列重放逻辑\n- 检查 `baseURL` 是否从环境变量读取而非硬编码\n- API 模块生成后对照接口文档核对 URL 路径和 HTTP 方法\n- `import.meta.env.VITE_xxx` 是 Vite 读取环境变量的方式，提醒 AI 不要用 `process.env`\n\n## 课堂练习\n\n**练习 1**（基础）：参考 `api/customer.js` 的写法，封装 `api/comment.js`，包含 `getCommentList(params)`、`createComment(data)`、`deleteComment(id)` 三个函数。后端接口为 `GET /comments/`、`POST /comments/`、`DELETE /comments/:id/`。\n\n思路提示：照搬 customer.js 的结构，替换 URL 和方法名即可。GET 用 `params`，POST 用 `data`，DELETE 无请求体。\n\n**练习 2**（中等）：在 `CommentSection.vue` 中调用 `getCommentList` 获取评论数据，用 `v-loading` 显示加载状态。要求：`onMounted` 时加载，传入 `{customer:customerId,page_size:200}` 参数，成功后将 `res.data.results` 赋值给 `commentList`。\n\n思路提示：\n- `import{getCommentList}from '@/api/comment'`\n- `const loading = ref(false)`，请求前置 true，`finally` 置 false\n- `const res = await getCommentList({customer:props.customerId,page_size:200})`\n- `commentList.value = res.data?.results || res.data || []`\n\n## 课后作业\n\n### 必做\n1. 完成 `utils/request.js` 封装，确保请求拦截器能正确注入 Token，响应拦截器能处理 code 和 401\n2. 完成 `api/customer.js` 和 `api/index.js` 封装\n3. 在 `CustomerList.vue` 中接入 `getCustomerList` API，实现列表数据加载、筛选查询、分页切换\n4. 配置 `vite.config.js` 代理，确保前后端联调正常（Django 后端需同时运行）\n\n### 选做（进阶）\n1. 封装 `api/user.js`（login/register/getUserInfo/logout），并在 `LoginView.vue` 中接入登录 API\n2. 用 AI 生成 `useCRUD.js` 组合式函数（封装增删改查弹窗逻辑），对比 AI 输出与项目实际代码\n\n### 提交方式\n附上客户列表页运行截图（数据正常加载、筛选功能、分页功能），以及浏览器 Network 面板中请求头携带 Authorization 的截图。\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[Axios 与 API 交互] --> B[Axios 实例]\n A --> C[拦截器]\n A --> D[API 封装]\n A --> E[组件调用]\n B --> B1[baseURL 环境变量]\n B --> B2[timeout 超时]\n C --> C1[请求拦截器 注入Token]\n C --> C2[响应拦截器 code判断]\n C --> C3[401 无感刷新]\n C --> C4[403 权限提示]\n C --> C5[统一错误处理]\n D --> D1[按模块拆分]\n D --> D2[RESTful 风格]\n D --> D3[统一出口 index]\n E --> E1[usePagination]\n E --> E2[v-loading]\n E --> E3[onMounted 加载]\n```\n\n## 下节预告\n\n- 下一节主题：Pinia 状态管理与用户认证\n- 需要预习的内容：\n - 复习本节的 Token 存取与拦截器机制\n - 思考：用户登录后，用户信息（角色、用户名）应该存放在哪里？多个组件如何共享？\n - 了解 Pinia 的概念（state/getters/actions），思考与 Vuex 的区别\n - 预览项目中 `stores/user.js` 和 `stores/app.js` 的结构\n","order":9},{"chapterName":"10-前后端联调","content":"# Day 10 - 前后端联调\n\n## 学习目标\n\n1. **知识目标**：理解浏览器同源策略与 CORS 跨域原理，掌握 django-cors-headers 的配置方法，理解 Vite 开发代理的工作机制，掌握前后端统一响应格式 `{code,msg,data}` 的约定\n2. **技能目标**：能独立配置 Django CORS 和 Vite 代理解决跨域问题，能使用 Element Plus 完成表单验证与错误提示（ElMessage），能完成前后端完整 CRUD 联调（列表/搜索/新增/编辑/删除/状态切换），能实现登录联调并获取 JWT Token\n3. **项目目标**：完成 CRM 系统客户管理模块的前后端联调，包括 CORS 配置、Vite 代理、客户列表/搜索/新增/编辑/删除/状态切换全流程，以及登录页面与后端 Token 获取的联调\n4. **AI 实践目标**：学习使用 AI 辅助调试跨域与联调问题，掌握\"调试型 Prompt\"的编写方法（错误信息 + 上下文 + 期望行为三要素）\n\n## 知识点讲解\n\n### 浏览器同源策略与 CORS\n\n**同源策略**是浏览器的安全机制：如果两个 URL 的协议（protocol）、域名（host）、端口（port）完全一致，则为\"同源\"，否则为\"跨域\"。浏览器默认阻止跨域的 AJAX 请求读取响应数据。\n\n在前后端分离架构中，前端运行在 `http://127.0.0.1:5173`（Vite 默认端口），后端运行在 `http://127.0.0.1:8000`（Django 默认端口），二者端口不同，属于跨域。\n\n```mermaid\nsequenceDiagram\n participant Browser as 浏览器(localhost:5173)\n participant Django as Django后端(127.0.0.1:8000)\n Note over Browser,Django:端口不一致，触发浏览器同源策略校验\n\n %% 无任何跨域处理 原生请求时序\n Browser->>Django:AJAX 请求接口\n Note right of Browser:浏览器检测到跨域，预检/校验响应头\n Django--x Browser:后端未配置CORS响应头\n Note over Browser:浏览器直接拦截返回结果\n Browser->>Browser:控制台抛出CORS跨域报错\n```\n\n**解决方案**：\n\n+ CORS（后端发送允许跨域响应头）\n\n+ Vite 代理（前端开发服务器转发请求）\n\n\n\n### CORS\n\n**CORS（Cross-Origin Resource Sharing）**跨域资源共享：是 W3C 标准，由后端在响应头中声明允许哪些来源跨域访问\n\n| 响应头 | 作用 |\n|--------|------|\n| `Access-Control-Allow-Origin` | 允许的来源（如 `http://localhost:5173`） |\n| `Access-Control-Allow-Methods` | 允许的 HTTP 方法 |\n| `Access-Control-Allow-Headers` | 允许的请求头（如 `Authorization`） |\n| `Access-Control-Allow-Credentials` | 是否允许携带 Cookie |\n\n#### django-cors-headers 配置\n\n`django-cors-headers` 是 Django 处理 CORS 的第三方库，通过中间件在响应中自动添加 CORS 头。\n\n\n\n**拓展安装**：\n\n```shell\npip install django-cors-headers -i https://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn\n```\n\n\n\n**配置三步走**：\n\n1. 将 `corsheaders` 添加到 `INSTALLED_APPS`\n2. 将 `CorsMiddleware` 添加到 `MIDDLEWARE` 的**最前面**（需在 `CommonMiddleware` 之前）\n3. 配置允许的来源\n\n```python\n# 开发环境：允许所有来源\nCORS_ALLOW_ALL_ORIGINS = True\n\n# 生产环境：显式配置允许的来源\nCORS_ALLOWED_ORIGINS = ['https://crm.example.com']\n\n# 允许携带凭证（Cookie）\nCORS_ALLOW_CREDENTIALS = True\n```\n\n> 要点：`CorsMiddleware` 必须尽量靠前，尤其是在 `django.middleware.common.CommonMiddleware` 之前，否则 CORS 头可能无法正确添加。\n\n\n\n### Vite 开发代理\n\n除了后端配置 CORS，前端也可以使用开发服务器代理来解决跨域。Vite 的 `server.proxy` 配置可以将特定路径的请求转发到后端，对浏览器来说请求是发往同源的 Vite 服务器，不存在跨域问题。\n\n```mermaid\nsequenceDiagram\n participant Browser as 浏览器\n participant Vite as Vite Dev Server\n participant Django as Django后端\n\n Note over Browser:页面域名：localhost:5173\n Browser->>Vite:GET /api/customers/\n Note over Browser,Vite:同域名同源，浏览器不触发CORS拦截\n Vite->>Django:GET http://127.0.0.1:8000/api/customers/\n Note over Vite,Django:Node服务发起请求，不受浏览器同源策略约束\n Django-->>Vite:返回接口数据\n Vite-->>Browser:数据原路回传给浏览器\n```\n\n\n\n代理配置在 `vite.config.js` 的 `server.proxy` 中：\n\n```javascript\nserver:{\n proxy:{\n '/api':{\n target:'http://127.0.0.1:8000',// 后端地址\n changeOrigin:true // 修改 Host 头\n}\n}\n}\n```\n\n**`要点`**：`代理仅用于开发环境。生产环境前端和后端通常部署在同一域名下（通过 Nginx 反向代理），或通过 CORS 配置解决。`\n\n\n\n### 前后端统一响应格式\n\n本项目约定所有 API 统一返回如下 JSON 结构：\n\n```json\n{\n \"code\":200,\n \"msg\":\"success\",\n \"data\":{...}\n}\n```\n\n| 字段 | 说明 |\n|------|------|\n| `code` | 业务状态码，`200` 表示成功，失败时取 HTTP 状态码 |\n| `msg` | 提示信息，成功为 `success`，失败为错误描述 |\n| `data` | 实际数据，可以是对象、数组或 null |\n\n后端通过 `utils/response.py` 中的 `success()` 和 `fail()` 函数统一封装：\n\n```python\n# 成功\nreturn success(data=customer_list,msg='查询成功')\n# 输出:{\"code\":200,\"msg\":\"查询成功\",\"data\":[...]}\n\n# 失败\nreturn fail(msg='参数错误',http_code=400)\n# 输出:{\"code\":400,\"msg\":\"参数错误\",\"data\":null}\n```\n\n前端在 Axios 响应拦截器中统一处理：`res.code === 200` 时返回数据，否则弹出 `ElMessage.error` 错误提示。\n\n### Element Plus 表单验证\n\nElement Plus 的 `el-form` 组件内置表单验证功能，通过 `rules` 属性配置校验规则，通过 `ref` 属性设置校验对象名 `formRef`：\n\n```javascript\nconst formRules ={\n name:[{required:true,message:'请输入客户名称',trigger:'blur'}],\n phone:[\n{required:true,message:'请输入联系电话',trigger:'blur'},\n{pattern:/^1[3-9]\\d{9}$/,message:'请输入正确的手机号',trigger:'blur'}\n ],\n email:[{type:'email',message:'邮箱格式不正确',trigger:'blur'}]\n}\n```\n\n| 规则属性 | 说明 |\n|----------|------|\n| `required` | 是否必填 |\n| `message` | 校验失败提示文案 |\n| `trigger` | 触发方式：`blur`（失焦）/ `change`（值变化） |\n| `pattern` | 正则校验 |\n| `type` | 类型校验（如 `email`、`number`） |\n| `min` / `max` | 长度范围 |\n\n调用 `formRef.value.validate()` 返回 Promise，校验通过则 resolve，失败则 reject。\n\n### 错误提示（ElMessage）\n\nElement Plus 提供 `ElMessage` 全局消息组件，用于轻量级操作反馈：\n\n```javascript\nimport{ElMessage}from 'element-plus'\n\nElMessage.success('操作成功')// 绿色成功提示\nElMessage.error('操作失败')// 红色错误提示\nElMessage.warning('请完善表单')// 黄色警告提示\nElMessage.info('提示信息')// 蓝色信息提示\n```\n\n在 Axios 响应拦截器中，所有业务错误（`code !== 200`）和网络错误都会自动调用 `ElMessage.error()` 弹出提示，组件内无需重复处理。\n\n\n\n## CRM项目实操\n\n### Django CORS 配置（settings.py）\n\n```python\n# crm_project_root/settings.py\n\nINSTALLED_APPS = [\n 'django.contrib.admin',\n 'django.contrib.auth',\n 'django.contrib.contenttypes',\n 'django.contrib.sessions',\n 'django.contrib.messages',\n 'django.contrib.staticfiles',\n # 第三方\n 'rest_framework',\n 'rest_framework_simplejwt',\n 'rest_framework_simplejwt.token_blacklist',\n 'corsheaders',# 跨域处理应用\n 'django_filters',\n 'django_redis',\n # 本项目应用\n 'apps.users',\n 'apps.customers',\n 'apps.follow_ups',\n 'apps.comments',\n 'apps.statistics',\n]\n\nMIDDLEWARE = [\n 'corsheaders.middleware.CorsMiddleware',# 跨域中间件，需尽量靠前\n 'django.middleware.security.SecurityMiddleware',\n 'django.contrib.sessions.middleware.SessionMiddleware',\n 'django.middleware.common.CommonMiddleware',\n 'django.middleware.csrf.CsrfViewMiddleware',\n 'django.contrib.auth.middleware.AuthenticationMiddleware',\n 'django.contrib.messages.middleware.MessageMiddleware',\n 'django.middleware.clickjacking.XFrameOptionsMiddleware',\n]\n\n# 允许携带凭证（Cookie）\nCORS_ALLOW_CREDENTIALS = True\n\n# 开发环境：允许所有来源\nCORS_ALLOW_ALL_ORIGINS = True\n\"\"\"\nCORS_ALLOWED_ORIGINS = [\n \"http://127.0.0.1:5173\",\n \"http://localhost:5173\"\n]\n\"\"\"\n# 生产环境：显式配置允许的来源 \n# CORS_ALLOWED_ORIGINS = ['https://www.mycrm.com']\n```\n\n\n\n### Vite 代理配置（vite.config.js）\n\n这个配置之前就已经配置了，为了防止浏览器跨域拦截问题，之前配置的仅仅是代理所有 api/开头的请求。\n\n```javascript\n// frontend/vite.config.js\nimport{defineConfig,loadEnv}from 'vite'\nimport vue from '@vitejs/plugin-vue'\nimport AutoImport from 'unplugin-auto-import/vite'\nimport Components from 'unplugin-vue-components/vite'\nimport{ElementPlusResolver}from 'unplugin-vue-components/resolvers'\nimport path from 'path'\n\nexport default defineConfig(({mode})=>{\n const env = loadEnv(mode,process.cwd(),'')\n return{\n plugins:[\n vue(),\n // Element Plus 按需自动导入\n AutoImport({\n resolvers:[ElementPlusResolver({importStyle:'sass'})]\n}),\n Components({\n resolvers:[ElementPlusResolver({importStyle:'sass'})]\n})\n ],\n resolve:{\n alias:{\n '@':path.resolve(__dirname,'src')\n}\n},\n server:{\n host:'0.0.0.0',\n port:5173,\n open:false,\n proxy:{\n // 将 /api 请求代理到 Django 后端，解决开发环境跨域\n '/api':{\n target:'http://127.0.0.1:8000',\n changeOrigin:true\n}\n}\n}\n}\n})\n```\n\n修改`.env.development` 文件内容，让所有的请求都加上完整的路径，这样请求就不走 Vite 的代理，不在自动拼接地址前缀，验证Django跨域配置是否成功。\n\n```py\n# 开发环境接口基础地址\nVITE_API_BASE_URL=http://127.0.0.1:8000/api\n```\n\n**为何不用 Vite 代理**：原因是Vite代理只在开发中有效果，生产环境是咩有 Vite 代理服务器的\n\n\n\n### 登录联调（LoginView.vue）\n\n#### 创建并编写 api/user.js\n\n```js\n// 用户相关api\nimport request from '@/utils/request'\n\n\n// 用户登录\nexport function login(data){\n return request({\n url:'/login/',\n method:'post',\n data // GET 请求参数，拼接在 URL 上\n})\n}\n\n// 用户注册\nexport function register(data){\n return request({\n url:'/register/',\n method:'post',\n data\n})\n}\n\n\n// 获取登录用户信息\nexport function getUserInfo(){\n return request({\n url:'/user/info/',\n method:'get'\n})\n}\n\n\n// 退出登录（需传 refresh token）\nexport function logout(data){\n return request({\n url:'/logout/',\n method:'post',\n data\n})\n}\n\n// 刷新 token\nexport function refreshToken(refresh){\n return request({\n url:'/token/refresh/',\n method:'post',\n data:{refresh}\n})\n}\n\n// 获取用户列表（管理后台）\nexport function getUserList(params){\n return request({\n url:'/users/',\n method:'get',\n params\n})\n}\n\n// 更新用户信息\nexport function updateUser(id,data){\n return request({\n url:`/users/${id}/`,\n method:'patch',\n data\n})\n}\n```\n\n\n\n#### router/router.js 配置登录组件路由\n\n```js\nconst routes = [\n{\n // 访问 /login 匹配登录页\n path:'/login',\n name:'Login',\n component:()=> import('@/views/LoginView.vue')\n},\n{\n path:'/',\n component:Layout,\n redirect:'/dashboard',\n children:[\n ...\n ]\n}\n]\n```\n\n\n\n#### 登录视图组件配置\n\n```vue\n<template>\n <div class=\"login-container\">\n <div class=\"login-box\">\n <div class=\"login-header\">\n <h2>CRM 客户管理系统</h2>\n <p>请登录您的账号</p>\n </div>\n <!-- el-form 绑定模型与校验规则 -->\n <el-form ref=\"loginFormRef\":model=\"loginForm\":rules=\"loginRules\" size=\"large\" @keyup.enter=\"handleLogin\">\n <!-- v-model 双向绑定用户名 -->\n <el-form-item prop=\"username\">\n <el-input v-model=\"loginForm.username\" placeholder=\"请输入用户名\":prefix-icon=\"User\" clearable />\n </el-form-item>\n <!-- v-model 双向绑定密码 -->\n <el-form-item prop=\"password\">\n <el-input\n v-model=\"loginForm.password\"\n type=\"password\"\n placeholder=\"请输入密码\"\n:prefix-icon=\"Lock\"\n show-password\n clearable\n />\n </el-form-item>\n <el-form-item>\n <el-button type=\"primary\" class=\"login-btn\":loading=\"loading\" @click=\"handleLogin\"> 登 录 </el-button>\n </el-form-item>\n </el-form>\n </div>\n </div>\n</template>\n\n<script setup>\nimport{ref,reactive}from 'vue';\nimport{User,Lock}from '@element-plus/icons-vue';\nimport{ElMessage}from 'element-plus';\nimport{useRouter,useRoute}from 'vue-router'\n\nimport{login}from '@/api/user'\nimport{setToken}from '@/utils/auth'\n\n\nconst router = useRouter()\nconst route = useRoute()\n\n// 表单引用，用于调用 validate 方法\nconst loginFormRef = ref(null);\n// 登录按钮 loading 状态\nconst loading = ref(false);\n\n// 登录表单数据（reactive 管理对象）\nconst loginForm = reactive({\n username:'',\n password:'',\n});\n\n// 表单校验规则\nconst loginRules ={\n username:[\n{required:true,message:'请输入用户名',trigger:'blur'},\n{min:2,max:30,message:'用户名长度在 2 到 30 个字符',trigger:'blur'},\n ],\n password:[\n{required:true,message:'请输入密码',trigger:'blur'},\n{min:6,max:30,message:'密码长度在 6 到 30 个字符',trigger:'blur'},\n ],\n};\n\n// 登录处理\nasync function handleLogin(){\n if(!loginFormRef.value)return;\n try{\n // 调用表单校验\n await loginFormRef.value.validate();\n loading.value = true;\n // TODO:Day 9 接入后端登录 API\n const res = await login(loginForm)\n \n // 登录成功，保存 token 和 refresh\n setToken(res.data.access,res.data.refresh)\n // 跳转到首页\n // window.location.href = '/'\n // 支持重定向：登录后跳回原页面\n const redirect = route.query.redirect || '/'\n router.push(redirect)\n\n // ElMessage.success('表单校验通过');\n // console.log('提交的表单数据：',loginForm);\n}catch(e){\n // 校验失败\n ElMessage.warning('请完善表单信息');\n}finally{\n loading.value = false;\n}\n}\n</script>\n\n<style scoped>\n .login-container{\n height:100vh;\n display:flex;\n align-items:center;\n justify-content:center;\n background:linear-gradient(135deg,#667eea 0%,#764ba2 100%);\n}\n .login-box{\n width:400px;\n padding:40px;\n background:#fff;\n border-radius:8px;\n box-shadow:0 8px 30px rgba(0,0,0,0.2);\n}\n .login-header{\n text-align:center;\n margin-bottom:30px;\n}\n .login-btn{\n width:100%;\n}\n</style>\n```\n\n#### 修改后端登录响应格式:\n\n```py\n# users/views.py\n\n# 统一登录放回的数据格式\nfrom rest_framework_simplejwt.views import TokenObtainPairView\nfrom rest_framework_simplejwt.serializers import TokenObtainPairSerializer\n\nclass MyTokenObtainPairView(TokenObtainPairView):\n serializer_class = TokenObtainPairSerializer\n\n def post(self,request,*args,**kwargs):\n # 调用父类获取原始 token\n response = super().post(request,*args,**kwargs)\n access = response.data['access']\n refresh = response.data['refresh']\n return success(msg=\"登录成功\",data={\n \"access\":access,\n \"refresh\":refresh\n});\n```\n\n```py\n# users/url.py\nfrom .views import RegisterView,UserInfoView,LogoutView,MyTokenObtainPairView\n\nurlpatterns = [\n path('login/',MyTokenObtainPairView.as_view(),name='login'),\n path('token/refresh/',TokenRefreshView.as_view(),name='token_refresh'),\n path('register/',RegisterView.as_view(),name='register'),\n path('logout/',LogoutView.as_view(),name='logut'),\n path('user/info/',UserInfoView.as_view(),name='register'),\n]\n```\n\n\n\n#### 路由守卫配置/router/index.js\n\n所有的访问，再进到组件前都需要先做登录验证，未登录需要先跳去登录\n\n```js\nrouter.beforeEach((to,from,next)=>{\n /**\n * document.title 设置浏览器标签页标题\n * 如果路由有 meta.title 则拼接，否则用默认标题\n * 使用模板字符串（``）拼接字符串和变量\n */\n document.title = to.meta.title\n ? `${to.meta.title}- CRM系统`\n:'CRM客户管理系统'\n\n // ========================================================\n // Token 检查与路由放行\n // ========================================================\n // 获取本地存储 token\n const token = getToken()\n // 去往登录页直接放行\n if(to.path === '/login' || to.path === '/register'){\n next()\n}\n else{\n // 有 token 放行，没有跳转登录\n // token ? next():next('/login')\n // 重定向回原本的页面\n token ? next():next({path:'/login',query:{redirect:to.fullPath}})\n}\n}\n```\n\n\n\n### 客户管理实现\n\n#### 过滤实现\n\n**修改customer.vue**\n\n增加过滤标签，绑定过滤属性`v-model`，状态数据`CUSTOMER_STATUS_OPTIONS`，绑定事件`@keyup.enter`,`@clear`,`@change`，设置过滤函数`handleSearch`\n\n```vue\n<!-- 搜索筛选栏 -->\n<div class=\"app-card filter-bar\">\n <el-input\n v-model=\"filters.search\"\n placeholder=\"搜索客户名称/电话/公司\"\n clearable\n style=\"width:240px\"\n @keyup.enter=\"handleSearch\"\n @clear=\"handleSearch\"\n />\n <el-select\n v-model=\"filters.status\"\n placeholder=\"客户状态\"\n clearable\n style=\"width:140px\"\n @change=\"handleSearch\">\n <el-option\n v-for=\"item in CUSTOMER_STATUS_OPTIONS\"\n:key=\"item.value\"\n:label=\"item.label\"\n:value=\"item.value\"\n />\n </el-select>\n <el-button type=\"primary\" @click=\"handleSearch\">查询</el-button>\n <el-button @click=\"handleReset\">重置</el-button>\n <el-button type=\"success\" @click=\"handleAdd\">新增客户</el-button>\n</div>\n```\n\n**js 实现**\n\n定义 `filters` 数据，调整`fetchList`函数，在`loadData`加入过滤条件属性，定义`handleSearch`函数，发起查询请求\n\n```js\n...\n...\n\n// 加载数据： 到后端请求数据并封装结果\nasync function fetchList(){\n // const res = await getCustomerList()\n // list.value = res.data.results\n await loadData({\n // 增加筛选条件\n search:filters.search,\n status:filters.status\n});\n}\n\n\n// === 过滤查询\n// 筛选条件声明\nconst filters = reactive({\n search:'',\n status:''\n});\n\n// 客户状态下拉选项\nconst CUSTOMER_STATUS_OPTIONS = [\n{label:'线索',value:'lead'},\n{label:'已联系',value:'contacted'},\n{label:'已成交',value:'qualified'},\n{label:'已流失',value:'won'},\n{label:'已流失',value:'lost'},\n];\n// 查询：重置到第一页后加载\nfunction handleSearch(){\n resetPage();\n fetchList();\n}\n// 重置筛选\nfunction handleReset(){\n filters.search = '';\n filters.status = '';\n filters.source = '';\n resetPage();\n fetchList();\n}\n```\n\n\n\n#### 删除实现\n\n修改按钮标签：\n\n```vue\n<el-table-column label=\"操作\" min-width=\"120\" fixed=\"right\" align=\"center\">\n \t<!-- 从scope中解构出每行对象 -->\n <template #default=\"{row}\">\n <el-button type=\"primary\" link size=\"small\" @click=\"goDetail(row)\">详情</el-button>\n <el-button type=\"warning\" link size=\"small\" @click=\"handleEdit(row)\">编辑</el-button>\n <el-button type=\"danger\" link size=\"small\" @click=\"handleDelete(row)\">删除</el-button>\n </template>\n</el-table-column>\n```\n\n新增 js 代码\n\n```js\n// === 删除功能实现\nimport{ElMessage,ElMessageBox}from 'element-plus';\nasync function handleDelete(row){\n try{\n await ElMessageBox.confirm('确定要删除该条记录吗？','提示',{\n confirmButtonText:'确定',\n cancelButtonText:'取消',\n type:'warning',\n});\n await deleteCustomer(row.id);\n ElMessage.success('删除成功');\n // 刷新列表\n fetchList();\n}catch(e){\n // 用户取消或删除失败\n}\n}\n```\n\n\n\n#### 添加/编辑实现\n\n抽取 source和 status 的下拉选项值到 @utils/constants.js 中\n\n```js\n/** 客户状态文本映射（与后端 Customer.Status choices 保持一致） */\nexport const CUSTOMER_STATUS ={\n lead:'线索',// 刚获取的潜在客户\n contacted:'已联系',// 已联系上\n qualified:'有意向',// 表现出购买意向\n won:'已成交',// 已完成销售\n lost:'已流失' // 确定不购买\n}\n/** 客户状态选项数组（供下拉选择框使用） */\nexport const CUSTOMER_STATUS_OPTIONS = Object.keys(CUSTOMER_STATUS).map((key)=>({\n value:key,\n label:CUSTOMER_STATUS[key]\n}))\n\n\n/** 客户来源文本映射 */\nexport const CUSTOMER_SOURCE ={\n online:'线上推广',// 百度广告、社交媒体等\n referral:'转介绍',// 老客户推荐\n exhibition:'展会',// 行业展会获取\n cold_call:'电话开发',// 主动电话营销\n other:'其他' // 其他渠道\n}\n\n/** 客户来源选项数组 */\nexport const CUSTOMER_SOURCE_OPTIONS = Object.keys(CUSTOMER_SOURCE).map((key)=>({\n value:key,\n label:CUSTOMER_SOURCE[key]\n}))\n```\n\n\n\n**新增 CustomerForm.vue 作为添加或编辑的弹出窗表单**\n\n步骤： 1 实现弹出窗dialog，2 加入表单并实现数据数据传输\n\n```vue\n<!-- frontend/src/views/CustomerForm.vue -->\n<template>\n <!-- 弹出框配置 -->\n <el-dialog\n v-model=\"visible\"\n:title=\"title\"\n width=\"600px\"\n:close-on-click-modal=\"false\"\n @close=\"handleClose\"\n >\n <!-- 表单配置 -->\n <el-form\n ref=\"formRef\"\n:model=\"formData\"\n:rules=\"formRules\"\n label-width=\"100px\"\n >\n <el-form-item label=\"客户名称\" prop=\"name\">\n <el-input v-model=\"formData.name\" placeholder=\"请输入客户名称\" clearable />\n </el-form-item>\n <el-form-item label=\"联系电话\" prop=\"phone\">\n <el-input v-model=\"formData.phone\" placeholder=\"请输入联系电话\" clearable />\n </el-form-item>\n <el-form-item label=\"邮箱\" prop=\"email\">\n <el-input v-model=\"formData.email\" placeholder=\"请输入邮箱\" clearable />\n </el-form-item>\n <el-form-item label=\"公司名称\" prop=\"company\">\n <el-input v-model=\"formData.company\" placeholder=\"请输入公司名称\" clearable />\n </el-form-item>\n <el-form-item label=\"客户状态\" prop=\"status\">\n <el-select v-model=\"formData.status\" placeholder=\"请选择状态\" style=\"width:100%\">\n <el-option\n v-for=\"item in CUSTOMER_STATUS_OPTIONS\"\n:key=\"item.value\"\n:label=\"item.label\"\n:value=\"item.value\"\n />\n </el-select>\n </el-form-item>\n <el-form-item label=\"客户来源\" prop=\"source\">\n <el-select v-model=\"formData.source\" placeholder=\"请选择来源\" style=\"width:100%\">\n <el-option\n v-for=\"item in CUSTOMER_SOURCE_OPTIONS\"\n:key=\"item.value\"\n:label=\"item.label\"\n:value=\"item.value\"\n />\n </el-select>\n </el-form-item>\n <el-form-item label=\"地址\" prop=\"address\">\n <el-input v-model=\"formData.address\" type=\"textarea\":rows=\"2\" placeholder=\"请输入地址\" />\n </el-form-item>\n <el-form-item label=\"备注\" prop=\"remark\">\n <el-input v-model=\"formData.remark\" type=\"textarea\":rows=\"3\" placeholder=\"请输入备注信息\" />\n </el-form-item>\n </el-form>\n <template #footer>\n <el-button @click=\"visible = false\">取消</el-button>\n <el-button type=\"primary\":loading=\"loading\" @click=\"submit\">确定</el-button>\n </template>\n </el-dialog>\n</template>\n\n<script setup>\nimport{ref,reactive,watch}from 'vue'\nimport{ElMessage}from 'element-plus'\n\nimport{\n CUSTOMER_STATUS_OPTIONS,\n CUSTOMER_SOURCE_OPTIONS\n}from '@/utils/constants'\n\n\n// 接受父组件传过来的属性\nconst props = defineProps({\n modelValue:{type:Boolean,default:false},\n title:{type:String,default:'新增客户'},\n data:{type:Object,default:null},\n loading:{type:Boolean,default:false}\n})\n\n// 声明 emit 回调事件submit\nconst emit = defineEmits(['update:modelValue','submit'])\n\n\n// 双向绑定控制弹出框显示：modelValue为true，显示该子组件，visible为true,显示弹出窗\nconst visible = ref(props.modelValue)\nwatch(\n()=> props.modelValue,\n(val)=>{\n visible.value = val\n if(val)initForm()\n}\n)\n// 监听弹出窗改变，同步通知父组件设置 v-model 的数据\nwatch(visible,(val)=> emit('update:modelValue',val))\n\n\n// === 表单功能实现 ====\n// 表单数据双向绑定\nconst formData = reactive({\n name:'',\n phone:'',\n email:'',\n company:'',\n status:'lead',\n source:'online',\n owner:'',\n address:'',\n remark:''\n})\n\n// 表单验证对象声明\nconst formRef = ref(null)\n// 表单校验规则\nconst formRules ={\n name:[{required:true,message:'请输入客户名称',trigger:'blur'}],\n phone:[\n{required:true,message:'请输入联系电话',trigger:'blur'},\n{\n pattern:/^1[3-9]\\d{9}$|^0\\d{2,3}-?\\d{7,8}$/,\n message:'请输入正确的电话号码',\n trigger:'blur'\n}\n ],\n email:[{type:'email',message:'请输入正确的邮箱地址',trigger:'blur'}],\n status:[{required:true,message:'请选择客户状态',trigger:'change'}],\n source:[{required:true,message:'请选择客户来源',trigger:'change'}]\n}\n\n\n// 初始化表单数据（编辑时回填）\nfunction initForm(){\n if(props.data){\n Object.keys(formData).forEach((key)=>{\n // a ?? b ：空值合并运算符,a 为空，返回b，否则返回a\n formData[key] = props.data[key] ?? formData[key]\n})\n}else{\n resetForm()\n}\n}\n\n\n// 重置表单\nfunction resetForm(){\n // assign(a,b),拷贝 b 中的属性数据设置给a中属性\n Object.assign(formData,{\n name:'',phone:'',email:'',company:'',\n status:'lead',source:'online',owner:'',address:'',remark:''\n})\n // 清空之前的校验报错提示\n formRef.value?.clearValidate()\n}\n\n// 提交表单：先校验再提交\nasync function submit(){\n if(!formRef.value)return\n try{\n await formRef.value.validate()\n // 通知父组件submit事件，并浅拷贝一份表单数据\n emit('submit',{...formData})\n}catch(e){\n ElMessage.warning('请完善表单信息')\n}\n}\n\nfunction handleClose(){\n resetForm()\n}\n</script>\n```\n\n\n\n**CustomerList.vue中引用表单子组件**\n\n```vue\n</template>\n<div>\n ...\n\t<!-- 弹窗子组件 CustomerForm -->\n <CustomerForm\n v-model=\"dialogVisible\"\n:title=\"dialogTitle\"\n:data=\"currentRow\"\n:loading=\"submitLoading\"\n @submit=\"handleSubmit\"/>\n </div>\n</template>\n```\n\n**CustomerList.vue 中js实现**\n\n1 点击按钮打开弹窗并设置标题和行数据\n\n2 实现表单提交发起接口请求\n\n```js\n<script setup>\n ...\n // 新增和修改\n import CustomerForm from './CustomerForm.vue'\n \t// 传递给子组件的基本信息\n \t// 弹窗显示\n const dialogVisible = ref(false)\n // 弹窗标题\n const dialogTitle = ref('')\n // 当前操作的数据\n const currentRow = ref(null)\n // 提交中状态\n const submitLoading = ref(false)\n\n // 新增按钮设置数据给子组件\n function handleAdd(){\n currentRow.value = null\n dialogTitle.value = '新增'\n dialogVisible.value = true\n}\n\n // 编辑按钮设置数据给子组件\n function handleEdit(row){\n currentRow.value ={...row}\n dialogTitle.value = '编辑'\n dialogVisible.value = true\n}\n \n // 提交（新增或编辑）:子组件点击提交时通知父组件调用该函数\n async function handleSubmit(formData){\n submitLoading.value = true\n try{\n if(currentRow.value?.id){\n await updateCustomer(currentRow.value.id,formData)\n ElMessage.success('更新成功')\n}else{\n await createCustomer(formData)\n ElMessage.success('新增成功')\n}\n dialogVisible.value = false\n // 刷新列表\n fetchList()\n return true\n}catch(e){\n return false\n}finally{\n submitLoading.value = false\n}\n}\n</script>\n```\n\n\n\n**后端统一响应格式：解决添加和修改成功但是报错的问题**\n\n添加统一响应处理器： utils/renderers.py\n\n```py\nfrom rest_framework.renderers import JSONRenderer\n\nclass CustomJSONRenderer(JSONRenderer):\n def render(self,data,accepted_media_type=None,renderer_context=None):\n response = renderer_context[\"response\"]\n status_code = response.status_code\n\n # 如果已经是{code,msg,data}结构，说明视图调用了success/fail，直接透传，不再包装\n if isinstance(data,dict)and \"code\" in data and \"msg\" in data and \"data\" in data:\n return super().render(data,accepted_media_type,renderer_context)\n\n # ------------------- 没有包装过，才执行统一封装 -------------------\n if status_code >= 400:\n msg = data.get(\"detail\",\"请求异常\")if isinstance(data,dict)else str(data)\n wrap ={\n \"code\":status_code,\n \"msg\":msg,\n \"data\":None\n}\n else:\n wrap ={\n \"code\":status_code,\n \"msg\":\"操作成功\",\n \"data\":data\n}\n return super().render(wrap,accepted_media_type,renderer_context)\n```\n\n**settings.py 中启用**\n\n```py\nREST_FRAMEWORK ={\n # ...\n\t# ...\n # 统一异常处理，序列化校验错误、权限拒绝、认证失败\n 'EXCEPTION_HANDLER':'utils.exceptions.custom_exception_handler',\n # ============ 新增：全局统一JSON返回格式 ============\n 'DEFAULT_RENDERER_CLASSES':[\n 'utils.renderers.CustomJSONRenderer',\n ],\n}\n```\n\n\n\n### 客户列表 CRUD 联调（CustomerList.vue 完整代码）\n\n```vue\n<template>\n <div class=\"customer-list page-container\">\n <!-- 搜索筛选栏 -->\n <div class=\"app-card filter-bar\">\n <el-input\n v-model=\"filters.search\"\n placeholder=\"搜索客户名称/电话/公司\"\n clearable\n style=\"width:240px\"\n @keyup.enter=\"handleSearch\"\n @clear=\"handleSearch\"\n />\n <el-select v-model=\"filters.status\" placeholder=\"客户状态\" clearable style=\"width:140px\" @change=\"handleSearch\">\n <el-option v-for=\"item in CUSTOMER_STATUS_OPTIONS\":key=\"item.value\":label=\"item.label\":value=\"item.value\" />\n </el-select>\n <el-button type=\"primary\" @click=\"handleSearch\">查询</el-button>\n <el-button @click=\"handleReset\">重置</el-button>\n <el-button type=\"success\" @click=\"handleAdd\">新增客户</el-button>\n </div>\n\n <!-- 列表 -->\n <div class=\"list-card\">\n <el-table v-loading=\"loading\" border:data=\"list\" style=\"width:100%\">\n <el-table-column type=\"index\" label=\"编号\" width=\"55\" align=\"center\" />\n <el-table-column prop=\"name\" label=\"客户名称\" min-width=\"120\" align=\"center\" />\n <<el-table-column prop=\"phone\" label=\"联系电话\" min-width=\"120\" align=\"center\" />\n <el-table-column prop=\"company\" label=\"公司\" min-width=\"120\" align=\"center\" />\n <el-table-column prop=\"status\" label=\"状态\" min-width=\"120\" align=\"center\" />\n <el-table-column label=\"操作\" min-width=\"120\" fixed=\"right\" align=\"center\">\n <!-- 从scope中解构出每行对象 -->\n <template #default=\"{row}\">\n <el-button type=\"primary\" link size=\"small\" @click=\"goDetail(row)\">详情</el-button>\n <el-button type=\"warning\" link size=\"small\" @click=\"handleEdit(row)\">编辑</el-button>\n <el-button type=\"danger\" link size=\"small\" @click=\"handleDelete(row)\">删除</el-button>\n </template>\n </el-table-column>\n </el-table>\n </div>\n <!-- 分页 -->\n <div class=\"pagination-wrap\">\n <el-pagination\n v-model:current-page=\"pagination.page\"\n v-model:page-size=\"pagination.page_size\"\n:total=\"pagination.total\"\n:page-sizes=\"[3,5,10,20]\"\n layout=\"total,sizes,prev,pager,next,jumper\"\n background\n @size-change=\"handleSizeChange\"\n @current-change=\"handleCurrentChange\"\n />\n </div>\n\n <!-- 弹窗子组件 CustomerForm -->\n <CustomerForm\n v-model=\"dialogVisible\"\n:title=\"dialogTitle\"\n:data=\"currentRow\"\n:loading=\"submitLoading\"\n @submit=\"handleSubmit\"\n />\n </div>\n</template>\n<script setup>\n import{ref,reactive,onMounted}from 'vue';\n // 导入客户 API\n import{getCustomerList,createCustomer,updateCustomer,deleteCustomer}from '@/api/customer';\n\n // 数据列表\n // const list = ref([])\n // 加载数据： 到后端请求数据并封装结果\n async function fetchList(){\n // const res = await getCustomerList()\n // list.value = res.data.results\n await loadData({\n // 筛选条件\n search:filters.search,\n status:filters.status,\n source:filters.source,\n});\n}\n\n // 页面加载时获取数据\n onMounted(()=>{\n fetchList();\n});\n\n // 分页查询，带分页封装\n // 封装请求函数，当调用loadData 时，获取结果封装为分页与数据\n import{usePagination}from '@/composables/usePagination';\n const{pagination,list,loading,loadData,handleCurrentChange,handleSizeChange,resetPage}=\n usePagination(getCustomerList);\n\n // === 分页条功能：监听 page,page_size 的变化来发起新的分页请求\n watch(\n()=> [pagination.page,pagination.page.page_size],\n()=>{\n fetchList();\n}\n);\n\n // === 过滤查询\n // 筛选条件声明\n const filters = reactive({\n search:'',\n status:'',\n});\n\n // 客户状态下拉选项\n const CUSTOMER_STATUS_OPTIONS = [\n{label:'线索',value:'lead'},\n{label:'已联系',value:'contacted'},\n{label:'已成交',value:'qualified'},\n{label:'已流失',value:'won'},\n{label:'已流失',value:'lost'},\n ];\n // 查询：重置到第一页后加载\n function handleSearch(){\n resetPage();\n fetchList();\n}\n // 重置筛选\n function handleReset(){\n filters.search = '';\n filters.status = '';\n filters.source = '';\n resetPage();\n fetchList();\n}\n\n // === 删除功能实现\n import{ElMessage,ElMessageBox}from 'element-plus';\n async function handleDelete(row){\n try{\n await ElMessageBox.confirm('确定要删除该条记录吗？','提示',{\n confirmButtonText:'确定',\n cancelButtonText:'取消',\n type:'warning',\n});\n await deleteCustomer(row.id);\n ElMessage.success('删除成功');\n // 刷新列表\n fetchList();\n}catch(e){\n // 用户取消或删除失败\n}\n}\n \n // 新增和修改\n import CustomerForm from './CustomerForm.vue'\n \t// 传递给子组件的基本信息\n \t// 弹窗显示\n const dialogVisible = ref(false)\n // 弹窗标题\n const dialogTitle = ref('')\n // 当前操作的数据\n const currentRow = ref(null)\n // 提交中状态\n const submitLoading = ref(false)\n\n // 新增按钮设置数据给子组件\n function handleAdd(){\n currentRow.value = null\n dialogTitle.value = '新增'\n dialogVisible.value = true\n}\n\n // 编辑按钮设置数据给子组件\n function handleEdit(row){\n currentRow.value ={...row}\n dialogTitle.value = '编辑'\n dialogVisible.value = true\n}\n \n // 提交（新增或编辑）:子组件点击提交时通知父组件调用该函数\n async function handleSubmit(formData){\n submitLoading.value = true\n try{\n if(currentRow.value?.id){\n await updateCustomer(currentRow.value.id,formData)\n ElMessage.success('更新成功')\n}else{\n await createCustomer(formData)\n ElMessage.success('新增成功')\n}\n dialogVisible.value = false\n // 刷新列表\n fetchList()\n return true\n}catch(e){\n return false\n}finally{\n submitLoading.value = false\n}\n}\n\n // 跳转详情（动态路由参数）\n import{useRouter}from 'vue-router';\n const router = useRouter();\n function goDetail(row){\n router.push(`/customers/${row.id}`);\n}\n</script>\n\n<style scoped>\n.page-container{\n padding:16px 20px;\n}\n.pagination-wrap{\n display:flex;\n justify-content:flex-end;\n margin-top:16px;\n}\n\n.filter-bar{\n margin-bottom:20px;\n}\n</style>\n\n```\n\n\n\n### 客户详情页实现\n\n**格式化工具类抽取： utils/format.js**\n\n```js\n/**\n * =============================================================================\n * 格式化工具函数(format.js)\n * =============================================================================\n *\n * 【模块功能】\n * 提供日期时间格式化、业务枚举文本映射、数字格式化等工具函数。\n */\n\nimport{\n CUSTOMER_STATUS,\n CUSTOMER_SOURCE,\n FOLLOWUP_TYPE,\n USER_ROLE\n}from './constants'\n\n/**\n * 格式化日期时间\n * @param{string|number|Date}value 日期值（ISO 字符串、时间戳、Date 对象）\n * @param{string}fmt 格式模板，默认 'YYYY-MM-DD HH:mm:ss'\n * @returns{string}格式化后的字符串，无效值返回 '-'\n *\n * 【使用示例】\n * formatDateTime('2026-01-15T10:30:00')→ '2026-01-15 10:30:00'\n * formatDateTime('2026-01-15','YYYY年MM月DD日')→ '2026年01月15日'\n * formatDateTime(null)→ '-'\n */\nexport function formatDateTime(value,fmt = 'YYYY-MM-DD HH:mm:ss'){\n if(!value)return '-'\n const date = new Date(value)\n if(isNaN(date.getTime()))return '-'\n\n const pad =(n)=> String(n).padStart(2,'0')\n\n const map ={\n YYYY:date.getFullYear(),\n MM:pad(date.getMonth()+ 1),// getMonth()返回 0-11，需要 +1\n DD:pad(date.getDate()),\n HH:pad(date.getHours()),\n mm:pad(date.getMinutes()),\n ss:pad(date.getSeconds())\n}\n\n return fmt.replace(/YYYY|MM|DD|HH|mm|ss/g,(match)=> map[match])\n}\n\n/** 仅格式化日期（不含时间） */\nexport function formatDate(value){\n return formatDateTime(value,'YYYY-MM-DD')\n}\n\n/** 获取客户状态的中文文本 */\nexport function getStatusText(status){\n return CUSTOMER_STATUS[status] || status || '-'\n}\n\n/** 获取客户来源的中文文本 */\nexport function getSourceText(source){\n return CUSTOMER_SOURCE[source] || source || '-'\n}\n\n/** 获取跟进类型的中文文本 */\nexport function getFollowupTypeText(type){\n return FOLLOWUP_TYPE[type] || type || '-'\n}\n\n/** 获取用户角色的中文文本 */\nexport function getRoleText(role){\n return USER_ROLE[role] || role || '-'\n}\n\n/** 格式化百分比（乘以 100 后保留指定位数） */\nexport function formatPercent(value,digits = 1){\n if(value === null || value === undefined || isNaN(value))return '-'\n return(Number(value)* 100).toFixed(digits)+ '%'\n}\n\n/** 数字千分位格式化 */\nexport function formatNumber(value){\n if(value === null || value === undefined)return '0'\n return Number(value).toLocaleString('zh-CN')\n}\n```\n\n\n\n**/views/CustomerDetail.vue**\n\n```vue\n<template>\n <div class=\"customer-detail page-container\" v-loading=\"loading\">\n <!-- 返回按钮 -->\n <div class=\"back-bar mb-16\">\n <el-button @click=\"goBack\">\n <el-icon><ArrowLeft /></el-icon>返回列表\n </el-button>\n </div>\n\n <!-- 客户基本信息 -->\n <el-card class=\"mb-16\">\n <template #header>\n <div class=\"flex-between\">\n <span class=\"card-header-title\">客户信息</span>\n <el-button type=\"primary\" size=\"small\" @click=\"handleEdit\">编辑</el-button>\n </div>\n </template>\n <el-descriptions:column=\"3\" border>\n <el-descriptions-item label=\"客户名称\">{{customer.name}}</el-descriptions-item>\n <el-descriptions-item label=\"联系电话\">{{customer.phone}}</el-descriptions-item>\n <el-descriptions-item label=\"邮箱\">{{customer.email || '-'}}</el-descriptions-item>\n <el-descriptions-item label=\"公司\">{{customer.company || '-'}}</el-descriptions-item>\n <el-descriptions-item label=\"状态\">\n <StatusTag:status=\"customer.status\" />\n </el-descriptions-item>\n <el-descriptions-item label=\"来源\">{{CUSTOMER_SOURCE[customer.source]}}</el-descriptions-item>\n <el-descriptions-item label=\"负责销售\">\n{{customer.owner_name || customer.owner || '-'}}\n </el-descriptions-item>\n <el-descriptions-item label=\"创建时间\">{{formatDateTime(customer.created_at)}}</el-descriptions-item>\n <el-descriptions-item label=\"更新时间\">{{formatDateTime(customer.updated_at)}}</el-descriptions-item>\n <el-descriptions-item label=\"地址\":span=\"3\">{{customer.address || '-'}}</el-descriptions-item>\n <el-descriptions-item label=\"备注\":span=\"3\">{{customer.description || '-'}}</el-descriptions-item>\n </el-descriptions>\n </el-card>\n\n <!-- 跟进记录区域：保留卡片头部，删除内部列表、弹窗、相关业务逻辑 -->\n <el-card class=\"mb-16\">\n <template #header>\n <div class=\"flex-between\">\n <span class=\"card-header-title\">跟进记录</span>\n <el-button type=\"success\" size=\"small\" disabled>\n <el-icon><Plus /></el-icon>新增跟进\n </el-button>\n </div>\n </template>\n <el-empty description=\"跟进记录功能待开发\"></el-empty>\n </el-card>\n\n <!-- 评论区域：保留卡片头部，删除CommentSection组件 -->\n <el-card>\n <template #header>\n <span class=\"card-header-title\">评论</span>\n </template>\n <el-empty description=\"评论功能待开发\"></el-empty>\n </el-card>\n\n <!-- 编辑客户弹窗 -->\n <CustomerForm\n v-model=\"editVisible\"\n title=\"编辑客户\"\n:data=\"customer\"\n:loading=\"editSubmitting\"\n @submit=\"handleUpdateSubmit\"\n />\n </div>\n</template>\n\n<script setup>\nimport{ref,computed,onMounted}from 'vue'\nimport{useRoute,useRouter}from 'vue-router'\nimport{ArrowLeft}from '@element-plus/icons-vue'\nimport{ElMessage}from 'element-plus'\n\nimport StatusTag from '@/components/common/StatusTag.vue'\nimport CustomerForm from './CustomerForm.vue'\nimport{getCustomerDetail,updateCustomer}from '@/api/customer'\nimport{formatDateTime}from '@/utils/format'\nimport{CUSTOMER_SOURCE}from '@/utils/constants'\n\nconst route = useRoute()\nconst router = useRouter()\n\nconst customerId = computed(()=> route.params.id)\n\nconst loading = ref(false)\nconst customer = ref({})\n\n// 编辑客户相关\nconst editVisible = ref(false)\nconst editSubmitting = ref(false)\n\n// 获取客户详情\nasync function fetchCustomer(){\n loading.value = true\n try{\n const res = await getCustomerDetail(customerId.value)\n \n customer.value = res.data\n}finally{\n loading.value = false\n}\n}\n\n// 编辑客户\nfunction handleEdit(){\n editVisible.value = true\n}\n\n// 提交编辑\nasync function handleUpdateSubmit(formData){\n editSubmitting.value = true\n try{\n await updateCustomer(customerId.value,formData)\n ElMessage.success('更新成功')\n editVisible.value = false\n fetchCustomer()\n}catch(e){\n // 错误已由拦截器处理\n}finally{\n editSubmitting.value = false\n}\n}\n\n// 返回列表\nfunction goBack(){\n router.push('/customer')\n}\n\nonMounted(()=>{\n fetchCustomer()\n})\n</script>\n\n<style scoped lang=\"scss\">\n.back-bar{\n display:flex;\n}\n.card-header-title{\n font-size:16px;\n font-weight:bold;\n}\n.mb-16{\n margin-bottom:16px;\n}\n</style>\n```\n\n**路由配置：router/router.js**\n\n```\nconst routes = [\n{\n // 访问 /login 匹配登录页\n path:'/login',\n name:'Login',\n component:()=> import('@/views/LoginView.vue')\n},\n // 客户详情【动态路由】:id\n{\n path:'/customers/:id',\n name:'CustomerDetail',\n component:()=> import('@/views/CustomerDetail.vue'),\n meta:{title:'客户详情'}\n},\n ...\n ...\n]\n```\n\n\n\n### 工具抽取(拓展)\n\ncomposables/userCRUD.js 抽取\n\n```js\n// CRUD 操作复用 composable\nimport{ref}from 'vue'\nimport{ElMessage,ElMessageBox}from 'element-plus'\n\n/**\n * CRUD 操作复用\n * @param{Object}api API 模块，含 getList/create/update/remove\n */\nexport function useCRUD(api){\n // 弹窗显示\n const dialogVisible = ref(false)\n // 弹窗标题\n const dialogTitle = ref('')\n // 当前操作的数据\n const currentRow = ref(null)\n // 提交中状态\n const submitting = ref(false)\n\n // 新增\n function handleCreate(){\n currentRow.value = null\n dialogTitle.value = '新增'\n dialogVisible.value = true\n}\n\n // 编辑\n function handleEdit(row){\n currentRow.value ={...row}\n dialogTitle.value = '编辑'\n dialogVisible.value = true\n}\n\n // 提交（新增或编辑）\n async function handleSubmit(formData){\n submitting.value = true\n try{\n if(currentRow.value?.id){\n await api.update(currentRow.value.id,formData)\n ElMessage.success('更新成功')\n}else{\n await api.create(formData)\n ElMessage.success('新增成功')\n}\n dialogVisible.value = false\n return true\n}catch(e){\n return false\n}finally{\n submitting.value = false\n}\n}\n\n // 删除\n async function handleDelete(row,refreshFn){\n try{\n await ElMessageBox.confirm('确定要删除该条记录吗？','提示',{\n confirmButtonText:'确定',\n cancelButtonText:'取消',\n type:'warning'\n})\n await api.remove(row.id)\n ElMessage.success('删除成功')\n if(refreshFn)refreshFn()\n}catch(e){\n // 用户取消或删除失败\n}\n}\n\n return{\n dialogVisible,\n dialogTitle,\n currentRow,\n submitting,\n handleCreate,\n handleEdit,\n handleSubmit,\n handleDelete\n}\n}\n```\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n\n完成 CRM 系统前后端联调配置与客户管理模块的完整 CRUD 功能，打通从前端页面到后端数据库的全链路。\n\n### 实现步骤\n\n1. **配置 CORS**：在 `backend/crm_project/settings/base.py` 中将 `corsheaders` 添加到 `INSTALLED_APPS`，将 `CorsMiddleware` 添加到 `MIDDLEWARE` 最前面，设置 `CORS_ALLOW_CREDENTIALS = True`\n2. **配置开发环境 CORS**：在 `dev.py` 中设置 `CORS_ALLOW_ALL_ORIGINS = True`\n3. **配置 Vite 代理**：在 `frontend/vite.config.js` 的 `server.proxy` 中将 `/api` 代理到 `http://127.0.0.1:8000`\n4. **配置前端环境变量**：在 `.env.development` 中设置 `VITE_API_BASE_URL=/api`\n5. **实现统一响应**：后端 `utils/response.py` 提供 `success()`/`fail()` 函数，前端 `utils/request.js` 拦截器统一处理\n6. **登录联调**：`LoginView.vue` 调用 `userStore.login()` 获取 JWT Token，存入 localStorage\n7. **客户列表联调**：`CustomerList.vue` 通过 `getCustomerList` API 获取分页数据，支持搜索/状态/来源筛选\n8. **新增/编辑联调**：`CustomerForm.vue` 表单验证通过后调用 `createCustomer`/`updateCustomer` API\n9. **删除联调**：使用 `ElMessageBox.confirm` 确认后调用 `deleteCustomer` API\n10. **状态切换**：通过 `patchCustomer` API 部分更新客户状态字段\n\n### 联调验证清单\n\n- [ ] 启动 Django 后端：`python manage.py runserver`\n- [ ] 启动 Vite 前端：`npm run dev`\n- [ ] 访问 `http://localhost:5173/login`，输入用户名密码登录\n- [ ] 登录成功后检查 localStorage 中是否有 `crm_access_token`\n- [ ] 进入客户列表页面，验证数据正常加载\n- [ ] 测试搜索功能（按名称/电话/公司）\n- [ ] 测试新增客户功能\n- [ ] 测试编辑客户功能\n- [ ] 测试删除客户功能\n- [ ] 测试状态筛选功能\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n\n使用 AI 辅助调试前后端联调中的跨域问题和接口对接问题，学习编写\"调试型 Prompt\"。\n\n### 工具\n\nTRAE AI 编程助手 / ChatGPT / Claude\n\n### 场景\n\n前后端联调过程中最常见的报错是 CORS 跨域错误和接口数据格式不匹配。手动排查需要查看浏览器控制台、网络请求、后端日志，耗时且容易遗漏。AI 可以快速分析错误信息并给出排查方向。\n\n### Prompt 示例（调试型）\n\n```\n我在进行 Django + Vue 3 前后端联调时遇到跨域问题：\n\n【错误信息】\n浏览器控制台报错：\nAccess to XMLHttpRequest at 'http://127.0.0.1:8000/api/customers/' \nfrom origin 'http://localhost:5173' has been blocked by CORS policy:\nNo 'Access-Control-Allow-Origin' header is present on the requested resource.\n\n【上下文】\n- 后端：Django 5.2 + DRF，已安装 django-cors-headers==4.4.0\n- 前端：Vue 3 + Vite，前端运行在 localhost:5173，后端运行在 127.0.0.1:8000\n- settings.py 中 MIDDLEWARE 已添加 'corsheaders.middleware.CorsMiddleware'\n- INSTALLED_APPS 中已添加 'corsheaders'\n- dev.py 中设置了 CORS_ALLOW_ALL_ORIGINS = True\n\n【期望行为】\n前端能够正常访问后端 /api/customers/ 接口，不报 CORS 错误。\n\n请帮我分析可能的原因并给出排查步骤。\n```\n\n### 实践要点\n\n- **调试型 Prompt 三要素**：错误信息（完整报错文本）+ 上下文（技术栈、相关配置）+ 期望行为（预期结果）\n- 将完整的错误信息粘贴给 AI，不要只描述\"报错了\"\n- 提供相关配置文件内容，帮助 AI 快速定位问题\n- AI 给出的方案需结合实际项目验证，不能盲目照搬\n- 常见 CORS 问题排查顺序：中间件顺序 -> INSTALLED_APPS 注册 -> ALLOW_ALL_ORIGINS 配置 -> 生产环境 CORS_ALLOWED_ORIGINS\n\n## 课堂练习\n\n**练习 1**（基础）：在前端项目中创建一个 `HealthCheck.vue` 组件，调用后端 `/api/health/` 接口（已在 `crm_project/urls.py` 中定义），展示后端返回的 `{code,msg,data}` 信息。验证 Vite 代理是否生效。\n\n思路提示：使用 `import request from '@/utils/request'`，调用 `request({url:'/health/',method:'get'})`，观察浏览器 Network 面板中请求是否经过 Vite 代理（请求 URL 应为 `localhost:5173/api/health/`）。\n\n**练习 2**（中等）：在 `CustomerList.vue` 的表格操作列中新增一个\"状态切换\"按钮，点击后弹出下拉选择框（使用 `el-dropdown`），选择新状态后调用 `patchCustomer(id,{status:newStatus})` 更新客户状态，并刷新列表。\n\n思路提示：\n1. 引入 `patchCustomer` API\n2. 在操作列添加 `<el-dropdown>` 组件，遍历 `CUSTOMER_STATUS_OPTIONS` 生成选项\n3. 在 `@command` 事件中调用 `patchCustomer(row.id,{status:command})`\n4. 成功后调用 `ElMessage.success` 并 `fetchList()` 刷新\n\n## 课后作业\n\n### 必做\n\n1. 完成客户管理模块的完整 CRUD 联调：列表展示、搜索筛选、新增、编辑、删除，确保所有操作前后端数据一致\n2. 完成登录联调：输入用户名密码后获取 Token 并存入 localStorage，刷新页面后 Token 仍然有效\n3. 在 `CustomerForm.vue` 中增加手机号格式校验（11 位手机号或带区号的座机号），校验不通过时弹出 `ElMessage.warning` 提示\n4. 截图记录联调过程中的 Network 请求和响应，验证统一响应格式 `{code,msg,data}`\n\n### 选做（进阶）\n\n1. 在客户列表中实现\"批量删除\"功能：表格添加多选列，选中后点击批量删除按钮一次性删除多条记录\n2. 使用 AI 辅助排查一个实际遇到的联调问题，记录 Prompt 和 AI 给出的解决方案\n\n### 提交方式\n\n将前端和后端项目分别打包为 zip 或推送到 Git 仓库，附上联调成功后的运行截图（登录页、客户列表、新增/编辑弹窗）。\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[前后端联调] --> B[CORS 跨域]\n A --> C[Vite 代理]\n A --> D[统一响应格式]\n A --> E[表单验证]\n A --> F[CRUD 联调]\n B --> B1[同源策略]\n B --> B2[django-cors-headers]\n B --> B3[CorsMiddleware]\n B --> B4[CORS_ALLOW_ALL_ORIGINS]\n C --> C1[server.proxy]\n C --> C2[changeOrigin]\n C --> C3[仅开发环境]\n D --> D1[success/fail]\n D --> D2[code/msg/data]\n D --> D3[请求拦截器]\n D --> D4[响应拦截器]\n E --> E1[rules 规则]\n E --> E2[validate 校验]\n E --> E3[ElMessage 提示]\n F --> F1[客户列表]\n F --> F2[搜索筛选]\n F --> F3[新增编辑]\n F --> F4[删除确认]\n F --> F5[状态切换]\n```\n\n## 下节预告\n\n- 下一节主题：评论功能与用户状态\n- 需要预习的内容：\n - Pinia 状态管理的基本概念（state / getters / actions）\n - Vue 3 递归组件的原理与用法\n - Django ORM 的自关联外键（`ForeignKey('self')`）\n - localStorage API 基础用法\n - JWT Token 的刷新机制原理\n","order":10},{"chapterName":"11-评论功能与用户状态","content":"# Day 11 - 评论功能与用户状态\n\n## 学习目标\n\n1. **知识目标**：理解 Pinia 状态管理的核心概念（state / getters / actions），掌握树形数据结构与递归序列化原理，理解 Vue 递归组件的自引用机制，掌握 JWT Token 持久化与刷新机制\n2. **技能目标**：能使用 Pinia setup store 模式管理用户状态，能编写 DRF 递归 Serializer 实现嵌套评论数据返回，能开发 Vue 递归组件渲染层级评论，能实现 Token 的 localStorage 持久化与自动刷新\n3. **项目目标**：创建评论模块的 DRF Serializer（含递归 `replies` 字段）和 ViewSet，实现 `CommentSection.vue`（层级评论 + 回复功能），配置 Pinia user store（登录状态 / 用户信息 / Token 管理）\n4. **AI 实践目标**：学习用 AI 生成 Pinia store 模板代码，掌握描述状态管理需求的 Prompt 编写方法\n\n## 知识点梳理\n\n### Pinia 状态管理\n\nPinia 是 Vue 3 官方推荐的状态管理库，用来管理**跨组件共享的全局状态**。它采用组合式 API 风格，提供更简洁的 API 和完整的 TypeScript 支持。\n\n> 场景：多个页面、多个组件需要访问同一份数据。\n>\n> 例如：登录用户信息、token、侧边栏折叠状态。\n>\n> 如果只用组件`ref/reactive`，组件之间传参非常麻烦（多层组件透传 props、事件层层向上抛出）。\n\nPinia 把共享数据抽离到独立的`Store`，**任意组件都可以直接读取、修改**。\n\n**核心概念**：\n\n| 概念 | 说明 | 类比 |\n|------|------|------|\n| state | 响应式状态数据 | 组件中的 `ref` / `reactive` |\n| getters | 计算属性，派生状态 | 组件中的 `computed` |\n| actions | 方法，可修改状态 | 组件中的函数 |\n\nPinia 支持两种写法：**Options Store**（类似 Vuex）和 **Setup Store**（组合式 API 风格）。本项目使用 Setup Store：\n\n```javascript\n// Setup Store 模式\nexport const useUserStore = defineStore('user',()=>{\n // state（用 ref 定义）\n const token = ref('')\n // getters（用 computed 定义）\n const isLoggedIn = computed(()=> !!token.value)\n // actions（用函数定义）\n async function login(loginForm){...}\n\n return{token,isLoggedIn,login}\n})\n```\n\n> ┌────────────────────────────────────────────────┐\n> │ Pinia Store 架构 \t\t\t\t\t \t\t\t\t\t│\n> ├────────────────────────────────────────────────┤\n> │ \t\t\t\t\t \t\t\t\t\t\t │\n> │ ┌─────────────┐ \t┌──────────────┐ \t │\n> │ │ `State` \t\t │────►│ `Getters` \t\t │ \t │\n> │ │ \ttoken \t │ \t│ \tisLoggedIn \t │ \t │\n> │ │ \tuserInfo \t │ \t │ \tusername \t │ \t │\n> │ │ \trefreshToken\t │ \t│ \tisAdmin \t │ \t │\n> │ └──────┬──────┘ \t└──────────────┘ \t │\n> │ \t │ \t\t\t\t \t\t\t\t\t │\n> │ \t ▼ \t\t\t\t \t\t\t\t\t │\n> │ ┌─────────────┐ \t\t\t \t\t\t\t │\n> │ │ `Actions` \t\t │ login()/ logout()/ fetchUserInfo()\t\t\t \t │\n> │ │ \t\t \t │ ── 调用 API ── 修改 State \t\t\t \t│\n> │ └─────────────┘ \t\t\t \t\t\t\t │\n> │ \t\t\t\t\t \t\t\t\t\t\t │\n> │ 持久化：Token 存入 localStorage，刷新页面后恢复 \t\t\t\t\t │\n> │ \t\t\t\t\t\t\t\t\t\t\t\t│\n> └────────────────────────────────────────────────┘\n\n**示例**\n\n用户信息共享：stores/user.js\n\n```js\n// frontend/src/stores/user.js\n// 用户状态管理 - Pinia setup store\nimport{defineStore}from 'pinia'\nimport{ref,computed}from 'vue'\nimport{login as loginApi,getUserInfo,logout as logoutApi}from '@/api/user'\nimport{getToken,setToken,removeToken,getRefreshToken}from '@/utils/auth'\n\nexport const useUserStore = defineStore('user',()=>{\n // ========== State（状态）==========\n // 从 localStorage 恢复，实现刷新后状态持久化\n const token = ref(getToken()|| '')\n const refreshToken = ref(getRefreshToken()|| '')\n const userInfo = ref(\n JSON.parse(localStorage.getItem('crm_user_info')|| 'null')\n)\n\n // ========== Getters（计算属性）==========\n const isLoggedIn = computed(()=> !!token.value)\n const username = computed(()=> userInfo.value?.username || '')\n const role = computed(()=> userInfo.value?.role || '')\n const isAdmin = computed(()=> role.value === 'admin')\n const isManager = computed(()=> role.value === 'manager')\n const isSales = computed(()=> role.value === 'sales')\n\n // ========== Actions（方法）==========\n // 登录：获取 Token → 保存 → 获取用户信息\n async function login(loginForm){\n const res = await loginApi(loginForm)\n const{access,refresh}= res.data\n token.value = access\n refreshToken.value = refresh\n // 持久化到 localStorage\n setToken(access,refresh)\n // 登录成功后获取用户信息\n await fetchUserInfo()\n return res\n}\n\n // 获取用户信息并持久化\n async function fetchUserInfo(){\n const res = await getUserInfo()\n userInfo.value = res.data\n localStorage.setItem('crm_user_info',JSON.stringify(res.data))\n return res.data\n}\n\n // 退出登录：通知后端注销 → 清理本地状态\n async function logout(){\n try{\n // 通知后端将 refresh token 加入黑名单\n if(refreshToken.value){\n await logoutApi({refresh:refreshToken.value})\n}\n}catch(e){\n // 即使后端注销失败也清理本地状态\n console.warn('退出登录接口异常',e)\n}finally{\n resetState()\n}\n}\n\n // 重置状态：清空内存与 localStorage\n function resetState(){\n token.value = ''\n refreshToken.value = ''\n userInfo.value = null\n removeToken()\n localStorage.removeItem('crm_user_info')\n}\n\n return{\n // state\n token,\n refreshToken,\n userInfo,\n // getters\n isLoggedIn,\n username,\n role,\n isAdmin,\n isManager,\n isSales,\n // actions\n login,\n fetchUserInfo,\n logout,\n resetState\n}\n})\n```\n\n\n\n### 嵌套评论数据结构（树形）\n\n评论系统支持层级回复，一条评论可以有多个回复，回复也可以有回复，形成树形结构。数据库层面通过**自关联外键**实现：\n\n```\n评论 A(parent=null)\n├── 回复 A-1(parent=A)\n│ └── 回复 A-1-1(parent=A-1)\n├── 回复 A-2(parent=A)\n评论 B(parent=null)\n├── 回复 B-1(parent=B)\n```\n\nDjango Model 中使用 `ForeignKey('self')` 实现自关联：\n\n```python\nparent = models.ForeignKey(\n 'self',on_delete=models.CASCADE,null=True,blank=True,\n related_name='replies',verbose_name='父评论',\n)\n```\n\n- `parent` 指向父评论，顶层评论 `parent` 为 `null`\n- `related_name='replies'` 使得可以通过 `comment.replies` 反向查询所有直接回复\n\n### DRF 递归序列化\n\n要让 API 返回树形评论数据，Serializer 需要递归地嵌套自身。核心技巧是使用 `SerializerMethodField` 在 `get_replies` 方法中递归调用自身：\n\n```python\nclass CommentSerializer(serializers.ModelSerializer):\n replies = serializers.SerializerMethodField()\n\n def get_replies(self,obj):\n # 查询直接子评论，递归序列化\n children = obj.replies.filter(is_active=True).order_by('created_at')\n return CommentSerializer(children,many=True,context=self.context).data\n```\n\n```\n序列化过程（递归展开）：\n\nCommentSerializer(评论A)\n├── 字段:id,content,user_name,...\n├── get_replies(评论A)\n│ ├── CommentSerializer(回复A-1)\n│ │ ├── 字段:id,content,...\n│ │ └── get_replies(回复A-1)\n│ │ └── CommentSerializer(回复A-1-1)\n│ │ └── get_replies(回复A-1-1)→ []\n│ └── CommentSerializer(回复A-2)\n│ └── get_replies(回复A-2)→ []\n└── 输出:{id,content,replies:[{id,content,replies:[...]},... ]}\n```\n\n> 要点：递归序列化会为每条评论查询其子评论，层级较深时可能产生 N+1 查询问题。生产环境可使用 `prefetch_related` 优化。\n\n### Vue 递归组件\n\nVue 组件可以在自己的模板中引用自身，实现递归渲染。递归组件必须满足两个条件：\n\n1. **显式声明 name**：组件需要一个名字才能在模板中自引用\n2. **有终止条件**：必须有 `v-if` 或 `v-for` 判断，否则无限递归\n\n```vue\n<script setup>\n// 使用 defineOptions 声明组件名（Vue 3.3+）\ndefineOptions({\n name:'CommentItem'\n})\n</script>\n\n<template>\n <div class=\"comment-item\">\n <!-- 渲染当前评论 -->\n <div>{{comment.content}}</div>\n <!-- 递归渲染子评论 -->\n <div v-if=\"comment.children && comment.children.length\">\n <CommentItem\n v-for=\"child in comment.children\"\n:key=\"child.id\"\n:comment=\"child\"\n />\n </div>\n </div>\n</template>\n```\n\n`v-if=\"comment.children && comment.children.length\"` 是递归终止条件：当没有子评论时停止递归。\n\n### 用户状态持久化（localStorage Token）\n\nJWT Token 获取后需要持久化存储，使刷新页面后用户仍保持登录状态。本项目使用 `localStorage` 存储：\n\n| 存储键 | 内容 | 用途 |\n|--------|------|------|\n| `crm_access_token` | JWT Access Token | 请求头 Authorization |\n| `crm_refresh_token` | JWT Refresh Token | 刷新 Access Token |\n| `crm_user_info` | 用户信息 JSON | 展示用户名/角色等 |\n\nPinia Store 初始化时从 `localStorage` 读取，实现状态恢复：\n\n```javascript\nconst token = ref(getToken()|| '')// 从 localStorage 恢复\nconst userInfo = ref(\n JSON.parse(localStorage.getItem('crm_user_info')|| 'null')\n)\n```\n\n### Token 刷新机制\n\nJWT Access Token 有效期较短（本项目配置 60 分钟），过期后需使用 Refresh Token 获取新的 Access Token，避免用户重新登录。\n\n```\n┌────────────────────────────────────┐\n│ Token 刷新流程 │\n├────────────────────────────────────┤\n│ │\n│ 1. 请求 API → 返回 401(Access Token 过期)│\n│ │\n│ 2. 拦截器检测到 401，检查是否有 Refresh Token │\n│ ├─ 有 → 发起刷新请求 POST /api/token/refresh/ │\n│ 3. 刷新成功 → 更新 localStorage 中的 Token │\n│ ├─ 重放队列中的请求（用新 Token 重新请求） │\n│ └─ 重试原请求 │\n│ │\n│ 4. 刷新失败 → 清除 Token，跳转登录页 │\n│ │\n└────────────────────────────────────┘\n```\n\n在 Axios 响应拦截器中实现自动刷新逻辑，使用 `isRefreshing` 标志位避免重复刷新，用请求队列暂存刷新期间的请求。\n\n## 核心代码示例\n\n### 评论模型（comments/models.py）\n\n```python\n# backend/apps/comments/models.py\nfrom django.conf import settings\nfrom django.db import models\n\nfrom apps.customers.models import Customer\nfrom apps.follow_ups.models import FollowUp\n\n\nclass Comment(models.Model):\n \"\"\"评论：可对客户或跟进记录发表，并通过 parent 自关联实现层级回复。\"\"\"\n\n content = models.TextField('评论内容')\n user = models.ForeignKey(\n settings.AUTH_USER_MODEL,\n on_delete=models.CASCADE,\n related_name='comments',\n verbose_name='评论人',\n)\n customer = models.ForeignKey(\n Customer,on_delete=models.CASCADE,null=True,blank=True,\n related_name='comments',verbose_name='关联客户',\n)\n follow_up = models.ForeignKey(\n FollowUp,on_delete=models.CASCADE,null=True,blank=True,\n related_name='comments',verbose_name='关联跟进记录',\n)\n # 自关联外键：parent 指向父评论，顶层评论 parent 为 null\n parent = models.ForeignKey(\n 'self',on_delete=models.CASCADE,null=True,blank=True,\n related_name='replies',verbose_name='父评论',\n)\n is_active = models.BooleanField('是否有效',default=True)\n created_at = models.DateTimeField('创建时间',auto_now_add=True)\n updated_at = models.DateTimeField('更新时间',auto_now=True)\n\n class Meta:\n db_table = 'comments_comment'\n verbose_name = '评论'\n verbose_name_plural = verbose_name\n ordering = ['-created_at']\n\n def __str__(self):\n return f'{self.user}：{self.content[:20]}'\n```\n\n\n\n### 递归序列化器（comments/serializers.py）\n\n```python\n# backend/apps/comments/serializers.py\nfrom rest_framework import serializers\n\nfrom .models import Comment\n\n\nclass CommentSerializer(serializers.ModelSerializer):\n \"\"\"评论序列化器，通过 replies 字段递归返回回复树。\"\"\"\n # 附带评论人用户名\n author_name = serializers.CharField(source='user.username',read_only=True)\n # replies 字段使用 SerializerMethodField 递归获取子评论\n children = serializers.SerializerMethodField()\n\n class Meta:\n model = Comment\n fields = '__all__'\n read_only_fields =('user','created_at','updated_at')\n\n def get_children(self,obj):\n \"\"\"返回该评论的直接回复（仅有效的），递归序列化。\"\"\"\n # 通过 related_name='replies' 反向查询子评论\n children = obj.replies.filter(is_active=True).order_by('created_at')\n # 递归调用自身序列化子评论，传入 context 保持请求上下文\n return CommentSerializer(children,many=True,context=self.context).data\n```\n\n### 评论过滤器（comments/filters.py）\n\n```py\n# backend/apps/comments/filters.py\nimport django_filters\nfrom .models import Comment\n\n\nclass CommentFilter(django_filters.FilterSet):\n # 父评论ID，查询该评论的子回复，对应模型 related_name=\"replies\"\n parent = django_filters.NumberFilter(field_name=\"parent__id\")\n # 按客户过滤评论\n customer_id = django_filters.NumberFilter(field_name=\"customer__id\")\n # 按跟进记录过滤评论，模型字段名 follow_up\n follow_up_id = django_filters.NumberFilter(field_name=\"follow_up__id\")\n is_active = django_filters.BooleanFilter(field_name=\"is_active\")\n\n class Meta:\n model = Comment\n fields = [\"parent\",\"customer_id\",\"follow_up_id\",\"is_active\"]\n```\n\n\n\n### 评论视图集（comments/views.py）\n\n```python\n# backend/apps/comments/views.py\nfrom rest_framework import viewsets,filters\nfrom django_filters.rest_framework import DjangoFilterBackend\nfrom utils.response import success\nfrom .models import Comment\nfrom .serializers import CommentSerializer\nfrom .filters import CommentFilter\n\n\nclass CommentViewSet(viewsets.ModelViewSet):\n \"\"\"评论增删改查，支持按客户/跟进记录筛选。\"\"\"\n serializer_class = CommentSerializer\n filterset_class = CommentFilter\n filter_backends = [DjangoFilterBackend,filters.OrderingFilter]\n ordering_fields = ['created_at']\n\n\n def get_queryset(self):\n qs = Comment.objects.filter(is_active=True)\n\n # 没有parent参数，只取根评论\n if not self.request.query_params.get(\"parent\"):\n qs = qs.filter(parent__isnull=True)\n\n # 所有场景统一预加载第一层 replies\n qs = qs.prefetch_related(\"replies\")\n qs = qs.select_related('user')\n return qs\n\n def perform_create(self,serializer):\n # 评论人自动设为当前登录用户\n serializer.save(user=self.request.user)\n\n def destroy(self,request,*args,**kwargs):\n instance = self.get_object()\n self.perform_destroy(instance)\n return success(msg='删除成功')\n```\n\n\n\n### 评论路由（comments/urls.py）\n\n```python\n# backend/apps/comments/urls.py\nfrom django.urls import path,include\nfrom rest_framework.routers import DefaultRouter\n\nfrom .views import CommentViewSet\n\nrouter = DefaultRouter()\nrouter.register(r'comments',CommentViewSet,basename='comment')\n\nurlpatterns = [\n path('',include(router.urls)),\n]\n```\n\n跟路由配置\n\n```py\npath('api/',include('apps.comments.urls')),\n```\n\n\n\n### 评论 API（api/comment.js）\n\n```javascript\n// frontend/src/api/comment.js\nimport request from '@/utils/request'\n\n// 获取评论列表（支持 customer/follow_up/parent 筛选）\nexport function getCommentList(params){\n return request({\n url:'/comments/',\n method:'get',\n params\n})\n}\n\n// 获取评论详情\nexport function getCommentDetail(id){\n return request({\n url:`/comments/${id}/`,\n method:'get'\n})\n}\n\n// 新增评论（发表评论或回复时调用）\nexport function createComment(data){\n return request({\n url:'/comments/',\n method:'post',\n data\n})\n}\n\n// 更新评论\nexport function updateComment(id,data){\n return request({\n url:`/comments/${id}/`,\n method:'put',\n data\n})\n}\n\n// 删除评论\nexport function deleteComment(id){\n return request({\n url:`/comments/${id}/`,\n method:'delete'\n})\n}\n```\n\n\n\n### 递归评论组件（CommentItem.vue）\n\n```vue\n<!-- frontend/src/views/CommentItem.vue -->\n<template>\n <!-- 递归评论项组件 -->\n <div class=\"comment-item\">\n <div class=\"comment-content\">\n <el-avatar:size=\"32\" class=\"comment-avatar\">\n{{avatarText}}\n </el-avatar>\n <div class=\"comment-body\">\n <div class=\"comment-header\">\n <span class=\"comment-author\">\n{{comment.author_name || comment.author || '匿名用户'}}\n </span>\n <span class=\"comment-time\">{{formatDateTime(comment.created_at)}}</span>\n </div>\n <div class=\"comment-text\">{{comment.content}}</div>\n <div class=\"comment-actions\">\n <el-button text size=\"small\" @click=\"toggleReply\">\n <el-icon><ChatRound /></el-icon>回复\n </el-button>\n <el-button\n v-if=\"canDelete\"\n text\n size=\"small\"\n type=\"danger\"\n @click=\"handleDelete\"\n >\n <el-icon><Delete /></el-icon>删除\n </el-button>\n </div>\n <!-- 回复输入框 -->\n <div v-if=\"replying\" class=\"reply-input\">\n <el-input\n v-model=\"replyContent\"\n type=\"textarea\"\n:rows=\"2\"\n:placeholder=\"`回复 ${comment.author_name || comment.author || '匿名用户'}：`\"\n />\n <div class=\"reply-actions\">\n <el-button size=\"small\" @click=\"replying = false\">取消</el-button>\n <el-button type=\"primary\" size=\"small\":loading=\"replyingSubmitting\" @click=\"submitReply\">\n 发表\n </el-button>\n </div>\n </div>\n </div>\n </div>\n <!-- 递归渲染子评论：v-if 是终止条件 -->\n <div v-if=\"comment.children && comment.children.length\" class=\"comment-children\">\n <CommentItem\n v-for=\"child in comment.children\"\n:key=\"child.id\"\n:comment=\"child\"\n:customer-id=\"customerId\"\n:current-user-id=\"currentUserId\"\n @reply=\"$emit('reply',$event)\"\n @refresh=\"$emit('refresh')\"\n />\n </div>\n </div>\n</template>\n\n<script setup>\nimport{ref,computed}from 'vue'\nimport{ChatRound,Delete}from '@element-plus/icons-vue'\nimport{ElMessage}from 'element-plus'\nimport{createComment,deleteComment}from '@/api/comment'\nimport{formatDateTime}from '@/utils/format'\n\n// 递归组件需要显式声明 name，才能在模板中自引用\ndefineOptions({\n name:'CommentItem'\n})\n\nconst props = defineProps({\n // 单条评论数据\n comment:{\n type:Object,\n required:true\n},\n // 关联的客户ID\n customerId:{\n type:[String,Number],\n default:null\n},\n // 当前用户ID（用于判断是否可删除）\n currentUserId:{\n type:[String,Number],\n default:null\n}\n})\n\nconst emit = defineEmits(['reply','refresh'])\n\nconst replying = ref(false)\nconst replyContent = ref('')\nconst replyingSubmitting = ref(false)\n\n// 头像文字：取用户名首字母\nconst avatarText = computed(()=>{\n const name = props.comment.author_name || props.comment.author || 'U'\n return String(name).charAt(0).toUpperCase()\n})\n\n// 是否可删除（仅自己的评论）\nconst canDelete = computed(()=>{\n return props.currentUserId && props.comment.author === props.currentUserId\n})\n\n// 切换回复框显示\nfunction toggleReply(){\n replying.value = !replying.value\n if(!replying.value){\n replyContent.value = ''\n}\n}\n\n// 提交回复：创建子评论，parent 指向当前评论\nasync function submitReply(){\n if(!replyContent.value.trim()){\n ElMessage.warning('请输入回复内容')\n return\n}\n replyingSubmitting.value = true\n try{\n await createComment({\n content:replyContent.value,\n customer:props.customerId,\n parent:props.comment.id // 关键：设置父评论ID\n})\n ElMessage.success('回复成功')\n replyContent.value = ''\n replying.value = false\n emit('refresh')// 通知父组件刷新评论列表\n}catch(e){\n // 错误已由拦截器处理\n}finally{\n replyingSubmitting.value = false\n}\n}\n\n// 删除评论\nasync function handleDelete(){\n try{\n await deleteComment(props.comment.id)\n ElMessage.success('删除成功')\n emit('refresh')\n}catch(e){\n // 错误已由拦截器处理\n}\n}\n</script>\n\n<style scoped lang=\"scss\">\n.comment-item{\n padding:12px 0;\n}\n.comment-content{\n display:flex;\n gap:12px;\n}\n.comment-avatar{\n background-color:#409eff;\n color:#fff;\n flex-shrink:0;\n}\n.comment-body{\n flex:1;\n min-width:0;\n}\n.comment-header{\n display:flex;\n align-items:center;\n gap:12px;\n margin-bottom:4px;\n}\n.comment-author{\n font-size:14px;\n font-weight:600;\n color:#303133;\n}\n.comment-time{\n font-size:12px;\n color:#909399;\n}\n.comment-text{\n font-size:14px;\n color:#606266;\n line-height:1.6;\n word-break:break-word;\n}\n.comment-actions{\n margin-top:4px;\n display:flex;\n gap:8px;\n}\n.reply-input{\n margin-top:8px;\n}\n.reply-actions{\n display:flex;\n justify-content:flex-end;\n gap:8px;\n margin-top:8px;\n}\n.comment-children{\n margin-left:44px;\n border-left:2px solid #ebeef5;\n padding-left:12px;\n}\n</style>\n```\n\n### 评论区容器（CommentSection.vue）\n\n```vue\n<!-- frontend/src/views/CommentSection.vue -->\n<template>\n <!-- 评论区容器：发表评论 + 层级评论列表 -->\n <div class=\"comment-section\">\n <!-- 发表评论输入框 -->\n <div class=\"comment-input-box\">\n <el-input\n v-model=\"commentContent\"\n type=\"textarea\"\n:rows=\"3\"\n placeholder=\"请输入评论内容...\"\n maxlength=\"500\"\n show-word-limit\n />\n <div class=\"comment-submit-bar\">\n <el-button type=\"primary\":loading=\"submitting\" @click=\"submitComment\">\n 发表评论\n </el-button>\n </div>\n </div>\n\n <!-- 评论列表（递归渲染） -->\n <div class=\"comment-list\" v-loading=\"loading\">\n <template v-if=\"commentTree.length\">\n <CommentItem\n v-for=\"item in commentTree\"\n:key=\"item.id\"\n:comment=\"item\"\n:customer-id=\"customerId\"\n:current-user-id=\"currentUserId\"\n @refresh=\"fetchComments\"\n />\n </template>\n <el-empty v-else description=\"暂无评论，快来发表第一条吧\":image-size=\"80\" />\n </div>\n </div>\n</template>\n\n<script setup>\nimport{ref,computed,onMounted}from 'vue'\nimport{ElMessage}from 'element-plus'\nimport CommentItem from './CommentItem.vue'\nimport{getCommentList,createComment}from '@/api/comment'\nimport{useUserStore}from '@/stores/user'\n\nconst props = defineProps({\n // 关联的客户ID\n customerId:{\n type:[String,Number],\n required:true\n}\n})\n\nconst userStore = useUserStore()\n\nconst commentContent = ref('')\nconst submitting = ref(false)\nconst loading = ref(false)\nconst commentList = ref([])\n\n// 当前用户ID（从 Pinia store 获取）\nconst currentUserId = computed(()=> userStore.userInfo?.id)\n\n// 评论树（带 children 字段）\nconst commentTree = ref([])\n\n// 获取评论列表\nasync function fetchComments(){\n loading.value = true\n try{\n const res = await getCommentList({customer:props.customerId,page_size:200})\n // results 已经是带 replies 的树形，直接赋值给 commentTree，不需要buildTree\n commentTree.value = res.data?.results || []\n}catch(e){\n console.error(e)\n}finally{\n loading.value = false\n}\n}\n\n\n// 发表评论\nasync function submitComment(){\n if(!commentContent.value.trim()){\n ElMessage.warning('请输入评论内容')\n return\n}\n submitting.value = true\n try{\n await createComment({\n content:commentContent.value,\n customer:props.customerId\n})\n ElMessage.success('评论发表成功')\n commentContent.value = ''\n fetchComments()\n}catch(e){\n // 错误已由拦截器处理\n}finally{\n submitting.value = false\n}\n}\n\nonMounted(()=>{\n fetchComments()\n})\n</script>\n\n<style scoped lang=\"scss\">\n.comment-section{\n width:100%;\n}\n.comment-input-box{\n margin-bottom:16px;\n}\n.comment-submit-bar{\n display:flex;\n justify-content:flex-end;\n margin-top:8px;\n}\n.comment-list{\n min-height:60px;\n}\n</style>\n```\n\n\n\n### 客户详情加入评论容器（CustomerDetail.vue）\n\n```vue\n<template> \n\t<el-card>\n <template #header>\n <span class=\"card-header-title\">评论</span>\n </template>\n <!-- <el-empty description=\"评论功能待开发\"></el-empty> -->\n <!-- 评论展示区 -->\n <CommentSection:customer-id=\"customerId\" />\n </el-card>\n</template>\n<script setup>\nimport CommentSection from '@/views/CommentSection.vue'\n</script>\n```\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n\n实现 CRM 系统的评论功能模块（支持层级回复）和用户状态管理（Pinia store + Token 持久化 + 自动刷新），完成从后端递归序列化到前端递归渲染的完整链路。\n\n### 实现步骤\n\n1. **创建评论模型**：在 `comments/models.py` 中定义 `Comment` 模型，使用 `ForeignKey('self')` 实现自关联层级回复\n2. **编写递归序列化器**：在 `comments/serializers.py` 中编写 `CommentSerializer`，使用 `SerializerMethodField` 的 `get_replies` 方法递归返回子评论\n3. **创建评论视图集**：在 `comments/views.py` 中编写 `CommentViewSet`，默认只返回顶层评论，回复随 `replies` 字段递归返回\n4. **注册评论路由**：在 `comments/urls.py` 中使用 `DefaultRouter` 注册评论路由\n5. **配置 Pinia user store**：在 `stores/user.js` 中管理 token、userInfo 状态，提供 login/logout/fetchUserInfo 方法，初始化时从 localStorage 恢复状态\n6. **开发递归评论组件**：在 `CommentItem.vue` 中使用 `defineOptions({name:'CommentItem'})` 声明组件名，模板中自引用渲染子评论\n7. **开发评论区容器**：在 `CommentSection.vue` 中加载评论列表、构建树形结构、发表评论\n8. **实现 Token 刷新**：在 `utils/request.js` 响应拦截器中处理 401 状态码，自动使用 Refresh Token 获取新 Access Token\n\n### 功能验证清单\n\n- [ ] 在客户详情页发表评论，评论出现在列表顶部\n- [ ] 对已有评论点击\"回复\"，输入内容后提交，回复嵌套显示在父评论下方\n- [ ] 对回复再次回复，形成多级嵌套\n- [ ] 删除自己的评论，评论从列表中消失\n- [ ] 刷新页面后，Token 和用户信息仍然保持\n- [ ] Token 过期后，自动刷新并重试请求，用户无感知\n- [ ] Refresh Token 也过期时，弹出重新登录提示\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n\n使用 AI 生成 Pinia store 模板代码，学习描述状态管理需求的 Prompt 编写方法。\n\n### 工具\n\nTRAE AI 编程助手 / ChatGPT / Claude\n\n### 场景\n\nPinia store 的结构相对固定（state + getters + actions），但每个项目的状态字段、API 调用和业务逻辑不同。可以让 AI 根据需求描述快速生成 store 骨架代码，然后在此基础上修改完善。\n\n### Prompt 示例\n\n```\n你是一位 Vue 3 前端工程师。请用 Pinia 的 setup store 模式帮我创建一个用户状态管理 store。\n\n【需求描述】\n1. 状态（state）：\n - token：JWT access token（从 localStorage 的 'crm_access_token' 键初始化）\n - refreshToken：JWT refresh token（从 localStorage 的 'crm_refresh_token' 键初始化）\n - userInfo：用户信息对象（从 localStorage 的 'crm_user_info' 键 JSON.parse 初始化）\n\n2. 计算属性（getters）：\n - isLoggedIn：token 是否存在\n - username：从 userInfo 中取 username\n - role：从 userInfo 中取 role\n - isAdmin / isManager / isSales：判断角色\n\n3. 方法（actions）：\n - login(loginForm)：调用 loginApi 获取 token，保存到 state 和 localStorage，再调用 fetchUserInfo\n - fetchUserInfo()：调用 getUserInfo API，保存到 state 和 localStorage\n - logout()：调用 logoutApi 通知后端注销，然后清理本地状态\n - resetState()：清空所有 state 和 localStorage\n\n【已有依赖】\n- API 模块：@/api/user 导出 login,getUserInfo,logout\n- 工具模块：@/utils/auth 导出 getToken,setToken,removeToken,getRefreshToken\n\n请输出完整的 store 代码，包含中文注释。\n```\n\n### 实践要点\n\n- **描述状态管理需求的 Prompt 四要素**：状态字段（含初始值来源）+ 计算属性（派生逻辑）+ 方法（调用哪些 API、做什么操作）+ 已有依赖（import 路径）\n- AI 生成的 store 代码需要检查：localStorage 键名是否与项目一致、API 方法名是否匹配、错误处理是否完善\n- 可以追加\"请确保登录成功后自动获取用户信息\"等业务约束\n- 生成的代码需实际运行验证，特别是异步方法的错误处理逻辑\n\n## 课堂练习\n\n**练习 1**（基础）：在 `CommentSection.vue` 中添加评论总数统计，在评论区顶部显示\"共 X 条评论\"。利用 `computed` 从 `commentList` 计算总条数（包括所有层级的回复）。\n\n思路提示：编写一个递归计算函数 `countComments(tree)`，遍历树形结构累加每个节点及其子节点数量。或者直接使用扁平列表 `commentList.value.length` 获取总数（因为后端返回的扁平列表包含所有评论）。\n\n**练习 2**（中等）：在 `CommentItem.vue` 中添加\"编辑评论\"功能。点击编辑按钮后，评论内容变为可编辑的输入框，修改后调用 `updateComment(id,{content})` API 更新。\n\n思路提示：\n1. 添加 `editing` ref 控制编辑模式\n2. 编辑模式下显示 `el-input` 替换纯文本展示\n3. 添加\"保存\"和\"取消\"按钮\n4. 保存时调用 `updateComment(props.comment.id,{content:editContent.value})`\n5. 成功后 `emit('refresh')` 刷新列表\n6. 编辑按钮仅在 `canDelete` 为 true 时显示（即自己的评论）\n\n## 课后作业\n\n### 必做\n\n1. 完成评论模块的后端：`Comment` 模型、`CommentSerializer`（递归 replies）、`CommentViewSet`、路由注册，确保 `GET /api/comments/?customer=1` 返回树形评论数据\n2. 完成评论模块的前端：`CommentItem.vue` 递归组件、`CommentSection.vue` 评论区，实现发表评论、回复评论、删除评论功能\n3. 完成 Pinia user store：登录时保存 Token 和用户信息，退出时清理状态，刷新页面后状态自动恢复\n4. 在客户详情页（`CustomerDetail.vue`）中引入 `CommentSection` 组件，展示该客户的评论\n\n### 选做（进阶）\n\n1. 为评论添加\"@提及\"功能：在评论内容中输入 `@用户名` 时，自动关联用户并发送通知\n2. 优化递归序列化的 N+1 查询问题：使用 `prefetch_related('replies')` 优化数据库查询\n3. 添加评论点赞功能\n\n### 提交方式\n\n将前端和后端项目分别打包为 zip 或推送到 Git 仓库，附上评论功能的运行截图（层级评论展示、回复操作、删除操作）。\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[评论功能与用户状态] --> B[Pinia 状态管理]\n A --> C[嵌套评论数据结构]\n A --> D[递归组件]\n A --> E[Token 持久化]\n A --> F[Token 刷新]\n B --> B1[state]\n B --> B2[getters]\n B --> B3[actions]\n B --> B4[setup store]\n C --> C1[自关联外键]\n C --> C2[树形结构]\n C --> C3[递归序列化]\n C --> C4[get_replies]\n D --> D1[defineOptions name]\n D --> D2[自引用]\n D --> D3[v-if 终止条件]\n D --> D4[CommentItem]\n E --> E1[localStorage]\n E --> E2[auth.js]\n E --> E3[状态恢复]\n F --> F1[401 拦截]\n F --> F2[isRefreshing]\n F --> F3[请求队列]\n F --> F4[重放请求]\n```\n\n## 下节预告\n\n- 下一节主题：缓存与会话管理\n- 需要预习的内容：\n - Redis 基本概念与常用命令（`SET`/`GET`/`EXPIRE`/`DEL`）\n - Django 缓存框架的基本用法（`cache.get`/`cache.set`）\n - Django ORM 聚合查询（`Count`、`annotate`）\n - ECharts 基本图表配置（饼图、折线图、柱状图）\n - JWT Token 黑名单机制\n","order":11},{"chapterName":"12-缓存与会话管理","content":"# Day 12 - 缓存与会话管理\n\n## 学习目标\n\n1. **知识目标**：理解 Redis 数据库的基本概念与数据结构，掌握 django-redis 缓存配置方法，理解 Django 缓存框架的三层策略（全站缓存/视图缓存/底层缓存 API），掌握 JWT Token 黑名单注销原理，理解会话超时机制\n2. **技能目标**：能配置 Redis 作为 Django 缓存后端并实现 LocMem 回退，能使用 `cache.get`/`cache.set` 实现统计数据缓存，能编写 Django ORM 聚合查询（`Count`/`annotate`），能实现 Token 黑名单注销功能，能使用 ECharts 渲染统计图表\n3. **项目目标**：配置 Redis 缓存并实现 LocMem 回退机制，实现统计 API（客户总数/状态分布/跟进趋势）并缓存 60 秒，实现 Token 黑名单注销功能，完成 DashboardView 展示 ECharts 图表（饼图/折线图/柱状图）\n4. **AI 实践目标**：学习用 AI 生成缓存装饰器和统计聚合查询代码，掌握描述缓存策略需求的 Prompt 编写方法\n\n## 知识点梳理\n\n### Redis 基础\n\n[Redis](https://github.com/tporadowski/redis/releases)（Remote Dictionary Server）是开源的内存键值数据库，以极高的读写性能著称（单机 QPS 可达 10 万+）。它支持多种数据结构，常用于缓存、消息队列、排行榜、会话存储等场景。\n\n| 特性 | 说明 |\n|------|------|\n| 内存存储 | 数据存储在内存中，读写极快 |\n| 键值结构 | 以 key-value 方式存储，支持过期时间 |\n| 数据结构 | String、Hash、List、Set、Sorted Set |\n| 持久化 | 支持 RDB（快照）和 AOF（日志）两种持久化方式 |\n| 单线程 | 核心操作单线程执行，避免锁竞争 |\n\n**常用命令**：\n\n```bash\nredis-cli # 连接 Redis\nSET key value # 设置键值\nGET key # 获取值\nEXPIRE key 60 # 设置 60 秒过期\nTTL key # 查看剩余过期时间\nDEL key # 删除键\nKEYS * # 查看所有键（生产环境慎用）\n```\n\n\n\n### django-redis 配置\n\n`django-redis` 是 Django 连接 Redis 的第三方库，提供缓存后端实现。配置方式是在 `settings.py` 的 `CACHES` 字典中指定：\n\n```python\nCACHES ={\n 'default':{\n 'BACKEND':'django_redis.cache.RedisCache',\n 'LOCATION':'redis://127.0.0.1:6379/1',# 使用 1 号数据库\n 'OPTIONS':{'CLIENT_CLASS':'django_redis.client.DefaultClient'},\n}\n}\n```\n\n**拓展安装**：\n\n```bat\npip install django‑redis==5.4.0 redis==4.6.0\n```\n\n\n\n本项目采用**优雅降级策略**：优先使用 Redis，未配置时自动回退到本地内存缓存（LocMem），确保无 Redis 环境也能运行：\n\n```mermaid\n%% 缓存后端选择策略\nflowchart TD\n title[缓存后端选择策略]\n\n A[\"读取环境变量 REDIS_URL\"] --> B{是否有值?}\n B -- 有值 --> C[\"Redis<br/>(生产级)\"]\n B -- 无值 --> D[\"LocMemCache<br/>(开发回退)\"]\n\n C:::prod --> C1[\"数据持久\"]\n C --> C2[\"多进程共享\"]\n C --> C3[\"重启不丢失\"]\n\n D:::dev --> D1[\"数据仅存内存\"]\n D --> D2[\"单进程有效\"]\n D --> D3[\"重启丢失\"]\n\n classDef prod fill:#e1f5fe,stroke:#0277bd\n classDef dev fill:#fff8e1,stroke:#f57f17\n```\n\n\n\n### Django 缓存策略\n\nDjango 提供三种层级的缓存策略\n\n| 策略 | 装饰器/方法 | 适用场景 | 粒度 |\n|------|------------|----------|------|\n| 全站缓存 | `django.middleware.cache.UpdateCacheMiddleware` | 静态内容为主的站点 | 最粗 |\n| 视图缓存 | `@cache_page(60)` | 特定视图的输出缓存 | 中等 |\n| 底层缓存 API | `cache.get(key)` / `cache.set(key,value,timeout)` | 精细控制缓存逻辑 | 最细 |\n\n本项目使用**底层缓存 API**，因为它可以精确控制缓存键、缓存内容和过期时间，适合统计数据缓存场景：\n\n```python\nfrom django.core.cache import cache\n\n# 读缓存：未命中返回 None\ndata = cache.get('crm_stats_admin')\n\n# 写缓存：设置 60 秒过期\ncache.set('crm_stats_admin',data,60)\n\n# 删除缓存\ncache.delete('crm_stats_admin')\n```\n\n### 统计数据缓存\n\nDashboard 统计页面需要聚合查询大量数据（客户总数、状态分布、来源分布、跟进趋势），每次请求都执行这些查询会给数据库带来压力。通过缓存统计结果 60 秒，可以显著降低数据库负载：\n\n```mermaid\nflowchart LR\n title[统计接口缓存逻辑]\n A[\"请求统计 API\"] --> B[\"cache.get('crm_stats_{role}')\"]\n B --> C{data 是否不为 None?}\n C -- 命中 ✅ --> D[\"直接返回缓存数据（快速）\"]\n C -- 未命中 ❌ --> E[\"执行 build_statistics()聚合查询\"]\n E --> F[\"cache.set('crm_stats_{role}',data,60)<br/># 缓存 60 秒\"]\n F --> G[\"返回数据\"]\n\n classDef start fill:#e8f4f8,stroke:#2b7bba\n classDef cachehit fill:#d4edda,stroke:#28a745\n classDef cachemiss fill:#fff3cd,stroke:#ffc107\n class A start\n class D cachehit\n class E,F cachemiss\n```\n\n> 要点：缓存键中包含 `role`（用户角色），不同角色看到不同维度的统计数据。缓存过期后自动重新查询，保证数据的准实时性。\n\n\n\n### Django ORM 聚合查询\n\nDjango ORM 提供强大的聚合查询功能，无需手写 SQL：\n\n```python\nfrom django.db.models import Count\n\n# 统计各状态的客户数量\n# SQL:SELECT status,COUNT(id)FROM customers GROUP BY status\nCustomer.objects.values('status').annotate(count=Count('id'))\n\n# 统计总数\nCustomer.objects.count()\n\n# 按日期过滤统计\nFollowUp.objects.filter(created_at__date=day).count()\n```\n\n| 方法 | 说明 | SQL 对应 |\n|------|------|----------|\n| `count()` | 统计总数 | `COUNT(*)` |\n| `values('field')` | 按字段分组 | `GROUP BY field` |\n| `annotate(count=Count('id'))` | 聚合计算 | `COUNT(id)` |\n| `filter(field__date=day)` | 按日期过滤 | `WHERE DATE(field)= ...` |\n\n\n\n### JWT Token 黑名单（注销）\n\nJWT 是无状态 Token，服务端不保存 Token 状态，因此无法直接\"注销\"一个尚未过期的 Token。`djangorestframework-simplejwt` 提供了 **Token 黑名单**机制来解决这个问题：\n\n```mermaid\nflowchart LR\n title[JWT Token 注销流程]\n A[\"1. 用户点击&quot;退出登录&quot;\"] --> B[\"2. 前端发送 POST /api/logout/<br>{refresh:&quot;xxx&quot;}\"]\n B --> C[\"3. 后端 LogoutView<br>RefreshToken(refresh).blacklist()<br>将 Refresh Token 加入黑名单表\"]\n C --> D[\"4. 该 RefreshToken 禁止刷新 AccessToken<br>• Access Token 自然过期无法续期<br>• 用户被强制退出\"]\n D --> E[\"5. 前端清理 localStorage 中的 Token\"]\n\n classDef step fill:#e8f4f8,stroke:#2b7bba\n class A,B,C,D,E step\n```\n\n需要在 `INSTALLED_APPS` 中注册 `rest_framework_simplejwt.token_blacklist` 并执行 `migrate` 创建黑名单表。\n\n\n\n### 会话超时\n\nJWT Token 本身带有过期时间，本项目配置：\n\n```python\nSIMPLE_JWT ={\n 'ACCESS_TOKEN_LIFETIME':timedelta(minutes=60),# Access Token 60 分钟\n 'REFRESH_TOKEN_LIFETIME':timedelta(days=7),# Refresh Token 7 天\n}\n```\n\n- Access Token 过期后，前端自动使用 Refresh Token 获取新的 Access Token（Day 11 已实现）\n- Refresh Token 过期或被加入黑名单后，用户需重新登录\n- 注销时将 Refresh Token 加入黑名单，即使未过期也无法使用\n\n\n\n## 数据缓存实现\n\n### 环境准备\n\n安装 Redis 软件\n\n安装 Django-redis\n\n```bat\npip install django-redis==5.4.0 redis==4.6.0\n```\n\n\n\n### CACHES 配置（settings.py 缓存部分）\n\n```python\n# crm_project/settings.py\n\n# ----------------------------- 缓存 ----------------------------------- \n# 若配置了 REDIS_URL 则使用 Redis，否则回退到本地内存缓存（便于无 Redis 环境运行）\nimport os\n\n_redis_url = os.environ.get('REDIS_URL')\nif _redis_url:\n # 生产/有 Redis 的环境：使用 Redis 缓存\n CACHES ={\n 'default':{\n 'BACKEND':'django_redis.cache.RedisCache',\n 'LOCATION':_redis_url,\n 'OPTIONS':{'CLIENT_CLASS':'django_redis.client.DefaultClient'},\n}\n}\nelse:\n # 无 Redis 的开发环境：回退到本地内存缓存\n CACHES ={\n 'default':{\n 'BACKEND':'django.core.cache.backends.locmem.LocMemCache',\n 'LOCATION':'crm-default',\n}\n}\n```\n\n### 环境变量配置（.env.example）\n\n```bash\n# backend/.env.redis\n\n# Redis 配置（Day 12 缓存使用，不配置则自动回退到本地内存缓存）\nREDIS_URL=redis://127.0.0.1:6379/1\t # 使用 1 号数据库\n```\n\n### INSTALLED_APPS 注册（settings.py 应用部分）\n\n```python\n# crm_project/settings.py\n\nINSTALLED_APPS = [\n 'django.contrib.admin',\n 'django.contrib.auth',\n 'django.contrib.contenttypes',\n 'django.contrib.sessions',\n 'django.contrib.messages',\n 'django.contrib.staticfiles',\n # 第三方\n 'rest_framework',\n 'rest_framework_simplejwt',\n 'rest_framework_simplejwt.token_blacklist',# JWT 黑名单（注销功能）\n 'corsheaders',\n 'django_filters',\n 'django_redis',# Redis 缓存后端\n # 本项目应用\n 'apps.users',\n 'apps.customers',\n 'apps.follow_ups',\n 'apps.comments',\n 'apps.statistics',\n]\n```\n\n\n\n## 系统看板后端实现\n\n### 统计聚合查询（statistic/services.py）\n\n```python\n# backend/apps/statistics/services.py\nfrom datetime import timedelta\n\nfrom django.db.models import Count\nfrom django.utils import timezone\n\nfrom apps.customers.models import Customer\nfrom apps.follow_ups.models import FollowUp\n\n\ndef build_statistics(user=None):\n \"\"\"\n 构建 Dashboard 所需统计数据。\n\n 参数：\n - user：当前登录用户，用于按用户筛选跟进趋势\n 为 None 时不过滤（用于管理员查看全局数据）\n\n 返回值：\n - dict：包含 total,by_status,by_source,followup_trend,conversion_rate\n \"\"\"\n # ========================================================\n # 1. 客户总数\n # ========================================================\n total = Customer.objects.count()\n\n # ========================================================\n # 2. 按状态分组统计\n # ========================================================\n # values_list('status')：获取所有状态值,返回元组集[(won,10),(lost,10)]，\n # values 返回字典集[{status:won,count:10}]\n # annotate(count=Count('id'))：按状态分组计数\n # dict()：转为{lead:5,contacted:3,won:2,...}\n by_status = dict(\n Customer.objects.values_list('status').annotate(count=Count('id')).order_by()\n)\n\n # ========================================================\n # 3. 按来源分组统计\n # ========================================================\n by_source = dict(\n Customer.objects.values_list('source').annotate(count=Count('id')).order_by()\n)\n\n # ========================================================\n # 4. 近 7 天跟进数量趋势\n # ========================================================\n today = timezone.now().date()\n trend = []\n\n # 获取跟进记录查询集\n followup_qs = FollowUp.objects\n # 如果指定了用户（看销售自己的信息），只统计该用户的跟进记录\n if user is not None:\n followup_qs = followup_qs.filter(user=user)\n\n for i in range(6,-1,-1):\n # 最近7天的日期\n day = today - timedelta(days=i)\n # 数据量少，可用created_at__date 转 date来判断\n count = FollowUp.objects.filter(created_at__date=day).count()\n trend.append({'date':day.isoformat(),'count':count})\n\n # ========================================================\n # 5. 转化率\n # ========================================================\n won = Customer.objects.filter(status='won').count()\n conversion_rate = round(won / total * 100,2)if total else 0\n\n # ========================================================\n # 返回汇总数据\n # ========================================================\n return{\n 'total':total,\n 'by_status':by_status,\n 'by_source':by_source,\n 'followup_trend':trend,\n 'conversion_rate':conversion_rate,\n}\n```\n\n### 缓存视图（statistic/views.py）\n\n```python\n# backend/apps/statistics/views.py\nfrom django.core.cache import cache\nfrom rest_framework.views import APIView\n\nfrom utils.response import success\nfrom .services import build_statistics\n\n\nclass StatisticsView(APIView):\n \"\"\"Dashboard 统计接口，结果缓存 60 秒以减轻数据库压力。\"\"\"\n\n CACHE_TTL = 60 # 缓存有效期 60 秒\n\n def get(self,request):\n # 缓存键按用户角色区分，不同角色看到不同维度数据\n role = request.user.role\n cache_key = f'crm_stats_{role}'\n\n # 先从缓存读取\n data = cache.get(cache_key)\n if data is None:\n # 缓存未命中，执行聚合查询\n data = build_statistics()\n # 写入缓存，60 秒后自动过期\n cache.set(cache_key,data,self.CACHE_TTL)\n\n return success(data=data)\n```\n\n\n\n### 统计路由（statistic/urls.py）\n\n```python\n# backend/apps/statistics/urls.py\nfrom django.urls import path\n\nfrom .views import StatisticsView\n\nurlpatterns = [\n path('statistics/',StatisticsView.as_view(),name='statistics'),\n]\n```\n\n\n\n### 根路由注册（crm_project/urls.py）\n\n```python\n# backend/crm_project/urls.py\n\"\"\"根路由：后台管理 + 健康检查 + 各应用 API（统一挂载在 /api/ 下）。\"\"\"\nfrom django.contrib import admin\nfrom django.urls import path,include\nfrom django.http import JsonResponse\n\n\ndef health(request):\n \"\"\"简单健康检查接口，用于部署探活。\"\"\"\n return JsonResponse({'code':200,'msg':'success','data':{'status':'ok'}})\n\n\nurlpatterns = [\n path('admin/',admin.site.urls),\n path('health/',health),\n # 各业务应用自行定义子路由，统一以 /api/ 为前缀\n path('api/',include('apps.users.urls')),\n path('api/',include('apps.customers.urls')),\n path('api/',include('apps.follow_ups.urls')),\n path('api/',include('apps.comments.urls')),\n path('api/',include('apps.statistic.urls')),\n]\n```\n\n\n\n## 前端系统看板\n\n### 统计 API 前端调用（api/statistics.js）\n\n```javascript\n// frontend/src/api/statistics.js\nimport request from '@/utils/request'\n\n// 获取统计数据\n// 返回{total,by_status,by_source,followup_trend,conversion_rate}\nexport function getStatistics(params){\n return request({\n url:'/statistics/',\n method:'get',\n params\n})\n}\n```\n\n\n\n### Dashboard 图表渲染（DashboardView.vue 核心脚本）\n\n#### 基本看板\n\n**vue 组件**：https://element-plus.org/zh-CN/component/layout\n\n```vue\n<!-- DashboardView.vue -->\n<template>\n <div class=\"dashboard page-container\">\n <!-- 顶部统计卡片 -->\n <el-row:gutter=\"16\" class=\"stat-row\">\n <el-col:xs=\"24\":sm=\"12\":md=\"6\">\n <el-card shadow=\"hover\">\n <h3>总客户数</h3>\n <p style=\"font-size:26px;color:#409eff\">{{statData.total || 0}}</p>\n </el-card>\n </el-col>\n <el-col:xs=\"24\":sm=\"12\":md=\"6\">\n <el-card shadow=\"hover\">\n <h3>转化率</h3>\n <p style=\"font-size:26px;color:#67c23a\">{{formatPercent(statData.conversion_rate)}}</p>\n </el-card>\n </el-col>\n <el-col:xs=\"24\":sm=\"12\":md=\"6\">\n <el-card shadow=\"hover\">\n <h3>已签约</h3>\n <p style=\"font-size:26px;color:#e6a23c\">{{getSignedCount}}</p>\n </el-card>\n </el-col>\n <el-col:xs=\"24\":sm=\"12\":md=\"6\">\n <el-card shadow=\"hover\">\n <h3>跟进中</h3>\n <p style=\"font-size:26px;color:#f56c6c\">{{getFollowingCount}}</p>\n </el-card>\n </el-col>\n </el-row>\n <!-- 图表区域 -->\n \n </div>\n</template>\n```\n\njs 和 css\n\n```vue\n<script setup>\n import{ref,reactive,computed,onMounted,onBeforeUnmount,nextTick}from 'vue'\n import{User,TrendCharts,CircleCheck,ChatLineRound}from '@element-plus/icons-vue';\n\n import{getStatistics}from '@/api/statistics'\n import{formatPercent}from '@/utils/format'\n\n // 统计数据\n const statData = reactive({\n total:0,\n by_status:{},\n by_source:{},\n followup_trend:[],\n conversion_rate:0\n})\n\n // 已签约客户数\n const getSignedCount = computed(()=> statData.by_status?.won || 0)\n // 跟进中客户数\n const getFollowingCount = computed(()=> statData.by_status?.contracted || 0)\n\n\n // 获取统计数据\n async function fetchData(){\n try{\n const res = await getStatistics()\n // 将 res.data 赋值给 statData\n Object.assign(statData,res.data)\n}catch(e){\n console.error('获取统计数据失败',e)\n}\n}\n \n onMounted(()=>{\n fetchData()\n})\n</script>\n```\n\n\n\n#### 图表看板\n\n**Dashboard 模板部分（DashboardView.vue template）**\n\n```vue\n<!-- frontend/src/views/DashboardView.vue -->\n<template>\n <div class=\"dashboard page-container\">\n <!-- 顶部统计卡片 -->\n\n <!-- 图表区域 -->\n <el-row:gutter=\"16\" class=\"chart-row\">\n <!-- 户状态分布 -->\n <el-col:xs=\"24\":lg=\"12\">\n <div class=\"app-card chart-card\">\n <h3 class=\"chart-title\">客户状态分布</h3>\n <div ref=\"pieChartRef\" class=\"chart-box\"></div>\n </div>\n </el-col>\n <!-- 跟进趋势 -->\n <el-col:xs=\"24\":lg=\"12\">\n <div class=\"app-card chart-card\">\n <h3 class=\"chart-title\">跟进趋势</h3>\n <div ref=\"lineChartRef\" class=\"chart-box\"></div>\n </div>\n </el-col>\n </el-row>\n\n <!-- 客户来源分布 -->\n <el-row:gutter=\"16\" class=\"chart-row\">\n <el-col:span=\"24\">\n <div class=\"app-card chart-card\">\n <h3 class=\"chart-title\">客户来源分布</h3>\n <div ref=\"barChartRef\" class=\"chart-box\"></div>\n </div>\n </el-col>\n </el-row>\n \n </div>\n</template>\n```\n\n**Dashboard样式部分(DashboardView.vue style)**\n\n```css\n<style scoped>\n /* 图表样式 */\n .app-card{\n background-color:#fff;\n margin-top:12px;\n}\n .chart-box{\n width:100%;\n height:340px;\n}\n .chart-card{\n padding:16px;\n}\n .chart-title{\n margin:0 0 12px 0;\n font-size:16px;\n}\n\n .chart-row{\n margin-bottom:16px;\n}\n</style>\n```\n\n**Dashboard 核心代码部分(DashboardView.vue script)**\n\n**echarts 使用手册**： https://echarts.apache.org/handbook/zh/get-started/\n\n**echarts 配置项手册**： https://echarts.apache.org/zh/option.html#title\n\n```vue\n<script setup>\n // ...\n // === 需增加代码：获取统计数据\n async function fetchData(){\n try{\n const res = await getStatistics()\n // 将 res.data 赋值给 statData\n Object.assign(statData,res.data)\n \n // ==== 图表渲染入口 ======\n // 等待 Vue 更新完DOM\n await nextTick()\n // 渲染echarts图表\n renderCharts()\n // ======================\n}catch(e){\n console.error('获取统计数据失败',e)\n}\n}\n\n // === 需增加代码\n onMounted(()=>{\n fetchData()\n // === 添加窗口大小改变时重新绘制图表 ===\n window.addEventListener('resize',handleResize)\n})\n\n // ...\n // 图表实现代码\n import * as echarts from 'echarts'\n import{CUSTOMER_STATUS,CUSTOMER_SOURCE}from '@/utils/constants'\n\n // 图表 DOM 引用声明\n const pieChartRef = ref(null)\n const lineChartRef = ref(null)\n const barChartRef = ref(null)\n // 图表实例声明\n let pieChart = null\n let lineChart = null\n let barChart = null\n\n // 渲染所有图表\n function renderCharts(){\n renderPieChart()\n renderLineChart()\n renderBarChart()\n}\n\n // 客户状态饼图\n function renderPieChart(){\n if(!pieChartRef.value)return\n if(!pieChart){\n pieChart = echarts.init(pieChartRef.value)\n}\n const statusData = statData.by_status ||{}\n // 后端返回 [{status:'lead',count:5},...]，转换为 ECharts 需要的格式\n const data = statusData.map\n ? statusData.map((item)=>({\n name:CUSTOMER_STATUS[item.status] || item.status,\n value:item.count\n}))\n:Object.keys(statusData).map((key)=>({\n name:CUSTOMER_STATUS[key] || key,\n value:statusData[key]\n}))\n pieChart.setOption({\n // 提示框配置a:系列，b数据名，c数据值，d比例\n tooltip:{trigger:'item',formatter:'{a}<br/>{b}:{c}({d}%)'},\n // 图表位置配置\n legend:{bottom:0,left:'center'},\n // 图表信息配置\n series:[\n{\n name:'客户状态',\n type:'pie',\n radius:['40%','70%'],\n // 标签重叠策略，扇形必须为false\n avoidLabelOverlap:false,\n itemStyle:{borderRadius:8,borderColor:'#fff',borderWidth:2},\n label:{show:false,position:'center'},\n // 高亮状态的扇区和标签样式\n emphasis:{label:{show:true,fontSize:18,fontWeight:'bold'}},\n labelLine:{show:false},\n data\n}\n ]\n})\n}\n\n // 跟进趋势折线图\n function renderLineChart(){\n if(!lineChartRef.value)return\n if(!lineChart){\n lineChart = echarts.init(lineChartRef.value)\n}\n const trend = statData.followup_trend || []\n const dates = trend.map((item)=> item.date || item.day)\n const counts = trend.map((item)=> item.count || item.value)\n lineChart.setOption({\n tooltip:{trigger:'axis'},\n grid:{left:'3%',right:'4%',bottom:'3%',containLabel:true},\n xAxis:{type:'category',boundaryGap:false,data:dates},\n yAxis:{type:'value',minInterval:1},\n series:[\n{\n name:'跟进次数',\n type:'line',\n smooth:true,\n data:counts,\n areaStyle:{\n color:new echarts.graphic.LinearGradient(0,0,0,1,[\n{offset:0,color:'rgba(64,158,255,0.5)'},\n{offset:1,color:'rgba(64,158,255,0.05)'}\n ])\n},\n itemStyle:{color:'#409eff'},\n lineStyle:{width:2}\n}\n ]\n})\n}\n\n // 客户来源柱状图\n function renderBarChart(){\n if(!barChartRef.value)return\n if(!barChart){\n barChart = echarts.init(barChartRef.value)\n}\n const sourceData = statData.by_source ||{}\n const items = sourceData.map\n ? sourceData\n:Object.keys(sourceData).map((key)=>({source:key,count:sourceData[key]}))\n const names = items.map((item)=> CUSTOMER_SOURCE[item.source] || item.source)\n const values = items.map((item)=> item.count)\n barChart.setOption({\n tooltip:{trigger:'axis',axisPointer:{type:'shadow'}},\n grid:{left:'3%',right:'4%',bottom:'3%',containLabel:true},\n xAxis:{type:'category',data:names,axisTick:{alignWithLabel:true}},\n yAxis:{type:'value',minInterval:1},\n series:[\n{\n name:'客户数量',\n type:'bar',\n barWidth:'40%',\n data:values,\n itemStyle:{\n color:new echarts.graphic.LinearGradient(0,0,0,1,[\n{offset:0,color:'#67c23a'},\n{offset:1,color:'#95d475'}\n ]),\n borderRadius:[4,4,0,0]\n}\n}\n ]\n})\n}\n\t\n // 窗口尺寸变化时重绘图表\n function handleResize(){\n pieChart?.resize()\n lineChart?.resize()\n barChart?.resize()\n}\n\n // 组件卸载时清理图表实例和事件监听\n onBeforeUnmount(()=>{\n window.removeEventListener('resize',handleResize)\n pieChart?.dispose()\n lineChart?.dispose()\n barChart?.dispose()\n})\n</script>\n```\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n\n配置 Redis 缓存系统并实现统计仪表盘与安全的用户注销功能，完成从缓存配置、聚合查询、统计 API、ECharts 可视化到 Token 黑名单注销的完整链路。\n\n### 实现步骤\n\n1. **安装依赖**：确认 `requirements.txt` 中包含 `django-redis==5.4.0`\n2. **注册应用**：在 `INSTALLED_APPS` 中注册 `django_redis` 和 `rest_framework_simplejwt.token_blacklist`\n3. **配置缓存**：在 `settings/base.py` 中配置 `CACHES`，实现 Redis 优先 + LocMem 回退策略\n4. **配置环境变量**：在 `.env` 中设置 `REDIS_URL=redis://127.0.0.1:6379/1`\n5. **执行迁移**：`python manage.py migrate` 创建 Token 黑名单表\n6. **编写统计服务**：在 `statistics/services.py` 中编写 `build_statistics()` 聚合查询函数\n7. **实现缓存视图**：在 `statistics/views.py` 中编写 `StatisticsView`，使用 `cache.get`/`cache.set` 缓存 60 秒\n8. **注册路由**：在 `statistics/urls.py` 中注册统计路由\n9. **实现注销视图**：在 `users/views.py` 中编写 `LogoutView`，使用 `RefreshToken.blacklist()` 将 Token 加入黑名单\n10. **前端 Dashboard**：在 `DashboardView.vue` 中调用统计 API，使用 ECharts 渲染饼图/折线图/柱状图\n\n### 功能验证清单\n\n- [ ] 启动 Redis 服务：`redis-server` 或 Docker 启动\n- [ ] 执行 `python manage.py migrate` 创建黑名单表\n- [ ] 访问 Dashboard 页面，统计卡片和图表正常显示\n- [ ] 60 秒内多次刷新 Dashboard，后端日志只查询一次数据库（缓存命中）\n- [ ] 60 秒后刷新，后端重新查询并更新缓存\n- [ ] 登录后点击退出登录，Refresh Token 被加入黑名单\n- [ ] 退出后使用旧 Refresh Token 刷新，返回 401 错误\n- [ ] 未配置 `REDIS_URL` 时，系统自动回退到 LocMem 缓存，功能正常\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n\n使用 AI 生成缓存装饰器和统计聚合查询代码，学习描述缓存策略需求的 Prompt 编写方法。\n\n### 工具\n\nTRAE AI 编程助手 / ChatGPT / Claude\n\n### 场景\n\n缓存策略和聚合查询有一定的模式可循，但需要根据具体业务需求调整缓存键、过期时间和查询逻辑。AI 可以根据需求描述快速生成代码骨架，并帮助编写可复用的缓存装饰器。\n\n### Prompt 示例一：生成统计聚合查询\n\n```\n你是一位 Django 后端工程师。请帮我编写一个统计聚合查询函数。\n\n【需求描述】\n编写 build_statistics()函数，返回 CRM Dashboard 所需的统计数据：\n1. total：客户总数（Customer 模型的 count）\n2. by_status：按客户状态分组统计数量，返回 [{status:'lead',count:5},...]\n3. by_source：按客户来源分组统计数量，格式同上\n4. followup_trend：最近 7 天每天的跟进记录数量，返回 [{date:'2025-01-01',count:3},...]\n5. conversion_rate：转化率 = 已成交客户数 / 总客户数 * 100，保留 1 位小数\n\n【已有模型】\n- Customer：有 status 字段（choices:lead/contacted/qualified/won/lost）和 source 字段\n- FollowUp：有 created_at 字段（DateTimeField）\n\n请使用 Django ORM 的 values + annotate + Count 实现聚合查询，输出完整代码含中文注释。\n```\n\n### Prompt 示例二：生成缓存装饰器\n\n```\n你是一位 Django 后端工程师。请帮我编写一个可复用的缓存装饰器。\n\n【需求描述】\n编写一个 cache_with_timeout(timeout,key_func)装饰器：\n1. 参数 timeout：缓存过期时间（秒）\n2. 参数 key_func：一个函数，接收 request 参数，返回缓存键字符串\n3. 装饰 DRF 的 APIView 的 get 方法\n4. 逻辑：先从 cache.get 读取，命中则直接返回；未命中则执行原方法，结果存入 cache.set\n\n【已有依赖】\n- from django.core.cache import cache\n- from utils.response import success\n\n请输出完整的装饰器代码，包含中文注释和使用示例。\n```\n\n### 实践要点\n\n- **描述缓存策略需求的 Prompt 要素**：缓存键设计（区分维度）+ 过期时间 + 缓存逻辑（读-判-写）+ 数据来源（哪些查询）+ 依赖（模型、工具函数）\n- AI 生成的聚合查询需检查：字段名是否与模型一致、分组字段是否正确、日期过滤是否准确\n- 缓存键设计要有区分度（如按角色、按用户 ID），避免不同用户看到错误数据\n- AI 生成的装饰器需实际测试缓存命中和未命中两种情况\n- 缓存不是越久越好：统计数据缓存 60 秒是平衡实时性和性能的折中，需根据业务需求调整\n\n## 课堂练习\n\n**练习 1**（基础）：在 `statistics/services.py` 中新增 `build_followup_stats()` 函数，统计最近 7 天各跟进类型（电话/拜访/邮件/微信/会议）的数量分布。在 `StatisticsView` 中将其加入返回数据，并在 Dashboard 中用饼图展示。\n\n思路提示：使用 `FollowUp.objects.values('type').annotate(count=Count('id'))` 按类型分组统计。前端在 `renderPieChart` 中参考状态饼图的实现方式渲染。\n\n**练习 2**（中等）：在 `StatisticsView` 中添加缓存失效机制。当客户数据发生变化（新增/编辑/删除）时，自动清除对应的统计缓存。可以在 `CustomerViewSet` 的 `perform_create`、`perform_update`、`perform_destroy` 方法中调用 `cache.delete(f'crm_stats_{role}')` 清除缓存。\n\n思路提示：\n1. 在 `customers/views.py` 中导入 `from django.core.cache import cache`\n2. 在 `perform_create`、`perform_update`、`destroy` 方法中添加缓存清理逻辑\n3. 需要清理所有角色的缓存键（admin/manager/sales），可以遍历角色列表删除\n4. 或使用 `cache.delete_many(['crm_stats_admin','crm_stats_manager','crm_stats_sales'])`\n\n## 课后作业\n\n### 必做\n\n1. 配置 Redis 缓存：在 `.env` 中设置 `REDIS_URL`，验证缓存命中（60 秒内多次请求只查询一次数据库）\n2. 完成统计 API：实现 `build_statistics()` 聚合查询和 `StatisticsView` 缓存视图，返回客户总数/状态分布/来源分布/跟进趋势/转化率\n3. 完成 Token 黑名单注销：实现 `LogoutView`，退出登录后 Refresh Token 加入黑名单，无法再刷新\n4. 完成 Dashboard 图表：在 `DashboardView.vue` 中使用 ECharts 渲染三个图表（状态饼图、趋势折线图、来源柱状图）\n\n### 选做（进阶）\n\n1. 实现缓存主动失效：在客户 CRUD 操作时自动清除统计缓存，使 Dashboard 数据实时更新\n2. 添加缓存命中率统计：记录缓存命中和未命中次数，在 Admin 后台或日志中展示\n3. 使用 AI 生成缓存装饰器并应用到 `StatisticsView`，替换手写的 `cache.get`/`cache.set` 逻辑\n\n### 提交方式\n\n将前端和后端项目分别打包为 zip 或推送到 Git 仓库，附上 Dashboard 运行截图（统计卡片 + 三个图表）和注销功能验证截图。\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[缓存与会话管理] --> B[Redis 基础]\n A --> C[django-redis 配置]\n A --> D[缓存策略]\n A --> E[聚合查询]\n A --> F[JWT 黑名单]\n A --> G[ECharts 图表]\n B --> B1[内存键值数据库]\n B --> B2[SET/GET/EXPIRE]\n B --> B3[数据结构]\n C --> C1[CACHES 配置]\n C --> C2[Redis 优先]\n C --> C3[LocMem 回退]\n C --> C4[REDIS_URL]\n D --> D1[全站缓存]\n D --> D2[视图缓存]\n D --> D3[底层缓存 API]\n D --> D4[cache.get/set]\n D --> D5[缓存键设计]\n E --> E1[Count 聚合]\n E --> E2[values 分组]\n E --> E3[annotate]\n E --> E4[日期过滤]\n E --> E5[services.py]\n F --> F1[Token 黑名单表]\n F --> F2[RefreshToken.blacklist]\n F --> F3[LogoutView]\n F --> F4[会话超时]\n G --> G1[饼图]\n G --> G2[折线图]\n G --> G3[柱状图]\n G --> G4[resize]\n G --> G5[dispose]\n```\n\n## 下节预告\n\n- 下一节主题：项目部署与总结\n- 需要预习的内容：\n - Docker 基本概念与 Dockerfile 编写\n - Nginx 反向代理配置\n - Gunicorn / uWSGI Python WSGI 服务器\n - 静态文件收集与 CDN 配置\n - HTTPS 证书配置\n - 回顾全栈开发流程，准备项目演示\n","order":12},{"chapterName":"13-项目总结与扩展","content":"# Day 13 - 项目总结与扩展\n\n## 学习目标\n\n1. **知识目标**：回顾 Django + Vue 全栈开发的完整流程，理解项目架构设计原则，掌握部署方案与性能优化要点\n2. **技能目标**：能对项目进行代码审查与优化，能配置 uWSGI + Nginx 部署 Django，能构建打包 Vue 项目\n3. **项目目标**：完成 CRM 项目的最终审查与优化，编写项目说明文档，规划扩展功能\n4. **AI 实践目标**：用 AI 做 Code Review，学习从安全/性能/可维护性三个维度审查代码\n\n## 知识点梳理\n\n### 项目架构复盘\n\nCRM 系统采用前后端分离架构，整体分为三层：\n\n| 层级 | 技术栈 | 职责 |\n|------|--------|------|\n| 前端 | Vue 3 + Vite + Element Plus | 用户界面渲染、交互逻辑、API 调用 |\n| 后端 | Django + DRF + SimpleJWT | 业务逻辑、数据处理、API 接口 |\n| 数据层 | MySQL + Redis | 持久化存储、缓存加速 |\n\n数据流：用户操作 → Vue 组件 → Axios 请求 → Django ViewSet → ORM → MySQL → 返回 JSON → Vue 渲染。认证环节通过 JWT Token 在前后端之间传递用户身份。\n\n### 代码规范审查要点\n\n| 维度 | 审查点 | 示例 |\n|------|--------|------|\n| 安全 | 密码是否哈希存储 | `create_user` 自动哈希 |\n| 安全 | 敏感配置是否使用环境变量 | `.env` 管理 DB 密码 |\n| 安全 | SQL 注入防护 | ORM 自动参数化 |\n| 性能 | 是否使用 `select_related` 减少查询 | `Customer.objects.select_related('owner')` |\n| 性能 | 是否对热点数据缓存 | 统计接口缓存 60 秒 |\n| 性能 | 是否有 N+1 查询问题 | 评论序列化递归查询 |\n| 规范 | 统一响应格式 | `{code,msg,data}` |\n| 规范 | 统一异常处理 | `custom_exception_handler` |\n| 规范 | 代码注释与命名 | 中文 verbose_name |\n\n### 部署方案了解\n\n**后端部署（uWSGI + Nginx）**：\n\n```\n用户 → Nginx(80/443)→ uWSGI(socket)→ Django(crm_project)\n ↘ 静态文件直接返回\n```\n\nuWSGI 是应用服务器，负责运行 Django 并与 Nginx 通信。Nginx 作为反向代理，处理静态文件分发和负载均衡。\n\n**前端部署（构建 + Nginx）**：\n\n```bash\n# 构建生产包\ncd frontend && npm run build\n\n# 产物在 frontend/dist/，由 Nginx 托管\n```\n\n### 性能优化方向\n\n| 方向 | 手段 | 课程对应 |\n|------|------|----------|\n| 数据库层 | 添加索引、`select_related`/`prefetch_related` | Day 2、Day 5 |\n| 缓存层 | Redis 缓存热点数据 | Day 12 |\n| 接口层 | 分页限制返回数据量 | Day 5 |\n| 前端层 | 路由懒加载、组件按需加载 | Day 8 |\n| HTTP 层 | Gzip 压缩、CDN 静态资源 | 部署配置 |\n\n### 扩展方向\n\n| 方向 | 技术方案 | 业务场景 |\n|------|----------|----------|\n| 实时通知 | Django Channels + WebSocket | 跟进提醒实时推送 |\n| 文件上传 | Django Media + 前端上传组件 | 客户合同附件管理 |\n| 数据导出 | openpyxl / Pandas | 客户列表导出 Excel |\n| 定时任务 | Celery + Beat | 每日跟进提醒邮件 |\n| 全文搜索 | Elasticsearch / Whoosh | 客户备注全文检索 |\n| 容器化 | Docker + docker-compose | 一键部署开发环境 |\n\n## 部署关联介绍\n\n### uWSGI 配置文件\n\n```ini\n# crm_project_root/uwsgi.ini\n[uwsgi]\n# Django 项目目录\nchdir = /opt/crm_project_root\n# WSGI 模块\nmodule = crm_project_root.wsgi:application\n# 使用虚拟环境\nhome = /opt/crm_project_root/.venv\n# socket 模式（与 Nginx 配合）\nsocket =:8001\n# 进程配置\nprocesses = 4\nthreads = 2\nmaster = true\n# 自动清理\nvacuum = true\n# 守护进程日志\ndaemonize = /var/log/uwsgi/crm_project_root.log\n# 生产环境使用 prod 配置\nenv = DJANGO_SETTINGS_MODULE=crm_project_root.settings\n```\n\n### Nginx 配置\n\n```nginx\n# /etc/nginx/conf.d/crm_project_root.conf\nserver{\n listen 80;\n server_name your-domain.com;\n\n # 前端静态资源\n location /{\n root /opt/crm_project_root/dist;\n try_files $uri $uri/ /index.html;\n}\n\n # 后端 API 代理\n location /api/{\n uwsgi_pass 127.0.0.1:8001;\n include /etc/nginx/uwsgi_params;\n}\n\n # Django Admin\n location /admin/{\n uwsgi_pass 127.0.0.1:8001;\n include /etc/nginx/uwsgi_params;\n}\n\n # Django 静态文件\n location /static/{\n alias /opt/crm_project_root/staticfiles/;\n}\n\n # Django 媒体文件\n location /media/{\n alias /opt/crm_project_root/media/;\n}\n}\n```\n\n### Vue 生产构建\n\n```bash\n# 1. 配置生产环境 API 地址\n# frontend/.env.production\nVITE_API_BASE_URL=/api\n\n# 2. 构建生产包\ncd frontend\nnpm run build\n\n# 3. 产物在 dist/ 目录\n# 将 dist/ 目录内容部署到 Nginx 的 root 路径\n```\n\n### Django 收集静态文件\n\n```bash\n# 在生产环境中收集所有静态文件到 STATIC_ROOT\ncd crm_project_root\npython manage.py collectstatic --noinput\n```\n\n### 生产环境安全检查清单\n\n```python\n# crm_project/settings/prod.py\nimport os\nfrom .base import * # noqa\n\nDEBUG = False\nALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS','').split(',')\n\n# HTTPS 相关安全头\nSECURE_SSL_REDIRECT = True\nSESSION_COOKIE_SECURE = True\nCSRF_COOKIE_SECURE = True\n\n# HSTS\nSECURE_HSTS_SECONDS = 31536000\nSECURE_HSTS_INCLUDE_SUBDOMAINS = True\nSECURE_HSTS_PRELOAD = True\n\n# 跨域：生产环境显式配置允许的前端来源\n_cors = os.environ.get('CORS_ALLOWED_ORIGINS','')\nCORS_ALLOWED_ORIGINS = [o.strip()for o in _cors.split(',')if o.strip()]\n```\n\n\n\n## AI编程工具 traeCode 使用\n\ntraeCode 是字节开发的一款 AI 编程工具，可以在该工具中实现代码的自动创建和修改\n\n[traeCode 注册地址](https://www.trae.cn/work-fission/J3LZAE63CSWG?utm_source=copy_link&utm_medium=friends_invite)：https://www.trae.cn/work-fission/J3LZAE63CSWG?utm_source=copy_link&utm_medium=friends_invite\n\n使用以上链接可额外获得500积分\n\n### 客户页面里的跟进记录功能实现\n\n**prompt 指令**\n\n```txt\n根据CRM项目,后端:#crm_project_root 前端 #crm-frontend 现有的功能和架构逻辑，实现客户页面里的跟进记录功能实现，要求先给出实现的计划，然后询问是否开始执行，等待开始执行指令，接收到开始执行指令之后，开始实现跟进记录功能。\n```\n\n\n\n### 用户管理模块实现\n\n**prompt 指令**\n\n```txt\n根据CRM项目,后端:#crm_project_root 前端 #crm-frontend 现有的功能和架构逻辑，实现 用户管理 模块的完整功能，要求先给出实现的计划，然后询问是否开始执行，等待开始执行指令，接收到开始执行指令之后，开始实现跟进记录功能。\n```\n\n\n\n### 跟进记录管理\n\n**prompt 指令**\n\n```txt\n根据CRM项目,后端:#crm_project_root 前端 #crm-frontend 现有的功能和架构逻辑，实现 跟进记录管理 模块的完整功能，要求先给出实现的计划，然后询问是否开始执行，等待开始执行指令，接收到开始执行指令之后，开始实现跟进记录功能。\n```\n\n\n\n## CRM 项目实战\n\n### 本节项目目标\n完成项目的最终审查、优化与部署文档。\n\n### 实现步骤\n1. 运行全项目代码审查，修复发现的规范问题\n2. 编写 `README.md` 项目启动指南\n3. 编写 `docs/api/api-docs.md` API 接口文档\n4. 了解部署相关内容\n5. 规划扩展功能路线图\n6. 使用 TraeCode 实现其他拓展功能\n\n## AI 辅助编程实践\n\n### 本节 AI 实践主题\n用 AI 做 Code Review，从安全、性能、可维护性三个维度审查项目代码。\n\n### Prompt 示例\n\n```\n你是一位资深 Python 全栈工程师，请对以下 Django 项目的代码进行 Code Review。\n\n审查维度：\n1. 安全性：密码存储、SQL 注入、权限控制、敏感信息泄露\n2. 性能：N+1 查询、缓存策略、分页、索引使用\n3. 可维护性：代码结构、命名规范、注释完整性、异常处理\n\n项目结构：\n- backend/apps/users/（用户认证）\n- backend/apps/customers/（客户管理，含 ViewSet + 权限控制）\n- backend/apps/comments/（评论，含递归序列化）\n- backend/apps/statistics/（统计，含缓存）\n- backend/utils/（统一响应、分页、异常处理）\n\n[粘贴关键代码文件]\n\n请按\"严重/警告/建议\"三个级别输出审查结果，并给出修复代码示例。\n```\n\n### 审查型 Prompt 要点\n- 明确审查维度（安全/性能/可维护性），避免泛泛而谈\n- 提供完整代码上下文，让 AI 能发现实际问题\n- 要求分级输出（严重/警告/建议），便于排优先级\n- 对 AI 提出的建议进行人工判断，不是所有建议都需采纳\n\n## 课堂练习\n\n**练习 2**（中等）：用 AI 对 `customers/views.py` 的 `CustomerViewSet` 进行 Code Review，记录 AI 提出的所有建议，判断哪些建议值得采纳并实施修改。\n\n思路提示：重点关注 `get_queryset` 的数据权限逻辑和 `perform_create` 的 owner 自动赋值是否有安全漏洞。\n\n## 课后作业\n\n### 必做\n1. 执行 Vue 生产构建，确认 `dist/` 产物可正常访问\n2. 用 AI 对整个项目做一次 Code Review，输出审查报告\n3. 用 AI 实现其他的拓张功能\n\n## 本节知识图谱\n\n```mermaid\ngraph LR\n A[项目总结与扩展] --> B[架构复盘]\n A --> C[代码审查]\n A --> D[部署方案介绍]\n A --> E[扩展方向]\n B --> B1[前后端分离]\n B --> B2[三层架构]\n C --> C1[安全审查]\n C --> C2[性能审查]\n C --> C3[规范审查]\n D --> D1[uWSGI]\n D --> D2[Nginx]\n D --> D3[Vue构建]\n E --> E1[WebSocket]\n E --> E2[文件上传]\n E --> E3[数据导出]\n E --> E4[容器化]\n```\n\n## 全课程回顾与后续建议\n\n### 13 天学习回顾\n\n| 阶段 | 天数 | 核心能力 |\n|------|------|----------|\n| Django 基础 | 1–3 | 项目搭建、ORM 建模、Admin 管理 |\n| DRF 接口 | 4–6 | RESTful API、ViewSet、JWT 认证 |\n| Vue 前端 | 7–9 | 组件化开发、路由管理、API 对接 |\n| 联调集成 | 10–12 | 跨域配置、CRUD 联调、缓存优化 |\n| 总结部署 | 13 | 代码审查、生产部署、扩展规划 |\n\n### 后续学习建议\n\n- 深入学习 Django 源码，理解 ORM 和中间件的工作原理\n- 学习 TypeScript，提升前端代码类型安全\n- 学习 Docker 与 CI/CD，实现自动化部署\n- 探索微服务架构，理解服务拆分与通信\n- 持续实践 AI 辅助编程，将 Prompt 工程融入日常开发\n\n恭喜完成全部 13 天课程。你已具备使用 Django + Vue 进行前后端分离全栈开发的核心能力。\n","order":13}];